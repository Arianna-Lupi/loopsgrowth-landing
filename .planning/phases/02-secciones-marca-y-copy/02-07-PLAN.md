---
phase: 02-secciones-marca-y-copy
plan: 07
type: execute
wave: 6
depends_on: [02-06]
files_modified:
  - src/styles/motion.css
  - src/styles/global.css
  - src/components/collage/HeroCollage.astro
  - src/components/sections/Faq.astro
  - src/layouts/BaseLayout.astro
  - src/components/SiteHeader.astro
  - src/components/brand/Logo.astro
  - src/content/landing.es.yaml
  - scripts/lib/copy-rules.mjs
  - tests/guards/copy.test.mjs
  - tests/guards/fixtures/verificar-nota.yaml
  - tests/guards/motion-budget.test.mjs
  - tests/guards/phase2-static.test.mjs
  - tests/e2e/lib/motion.ts
  - tests/e2e/motion.spec.ts
  - tests/e2e/phase-closing.spec.ts
  - tests/e2e/page-structure.spec.ts
  - tests/e2e/a11y-base.spec.ts
  - tests/e2e/sections-problem-solution.spec.ts
  - tests/e2e/results-cases.spec.ts
  - tests/e2e/team-includes-how.spec.ts
  - tests/e2e/closing-sections.spec.ts
  - package.json
  - package-lock.json
  - PENDING-COPY.md
  - .planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md
  - .planning/phases/02-secciones-marca-y-copy/02-ARI-FINDINGS.md
autonomous: true
requirements: [DSGN-03, DSGN-04, DSGN-05, COPY-01]

estimate:
  tokens: 98000
  raw_tokens: 98000
  tasks: 5
  confidence: low

must_haves:
  truths:
    - "El collage del hero (`#inicio [data-piece]`, 6 grupos) entra una sola vez con `@keyframes hc-enter`, solo bajo `@media (prefers-reduced-motion: no-preference)` y solo con `transform`: de `scale(0.94) rotate(var(--r-from))` a `scale(1) rotate(var(--r))`, 600 ms por pieza, retraso de `--i` por 80 ms (de 0 a 400 ms), una iteración, relleno `backwards` y fin a 1000 ms como máximo (menos de 1,2 s); cada pieza es visible con opacidad 1 desde el primer cuadro (DSGN-05)"
    - "Las pupilas (`.hc-pupil`, o `[data-pupil]` si el archivo vigente de 02-02 usa ese atributo) hacen un solo desplazamiento de 4 unidades del viewBox hacia el CTA con `@keyframes hc-look`: 400 ms, retraso igual al fin de la entrada (1000 ms), una iteración y relleno `backwards`, así que al terminar quedan en su posición estática, igual que con `reduce`; `--look-dx` y `--look-dy` apuntan al CTA del hero a 390 y 1280 px (producto punto con el vector pupila a CTA mayor que 0) (DSGN-05)"
    - "El icono del FAQ gira en 150 ms (`var(--dur-1)`): con `details[open]`, la barra `.faq-icon-v` pasa de vertical (más alta que ancha) a horizontal (relación ancho a alto de 2 o más) con `transform: rotate(90deg)` y el icono queda como minus; la transición de `transform` existe solo bajo `no-preference` (`getAnimations()` muestra un `CSSTransition` de 150 ms tras abrir); con `reduce` el estado abierto es el mismo pero inmediato y sin transición; Enter y Espacio siguen abriendo y cerrando, también sin JavaScript (DSGN-05, CONT-11)"
    - "Con `prefers-reduced-motion: reduce`, `document.getAnimations().length` es 0 en `/` y en `/privacidad/` al cargar, 2,5 s después, tras abrir un resumen del FAQ y tras pasar el ratón por un CTA; con `no-preference` es 0 en reposo (2,5 s después de cargar) y mientras corre solo existen las animaciones de la lista blanca de `tests/e2e/lib/motion.ts`; `/privacidad/` y `/marca/hoja/` no tienen ninguna con ninguna preferencia (DSGN-05; UI Considerations: a11y-motion)"
    - "Todo el movimiento nuevo vive en `src/styles/motion.css`, importado desde `global.css`; `tests/guards/motion-budget.test.mjs` prueba sobre `src/` que cada declaración `animation` o `transition` está dentro de `@media (prefers-reduced-motion: no-preference)`, que cada `@keyframes` declara solo `transform`, que no hay `infinite` ni `animation-timeline` y que ninguna animación de `@keyframes` apunta al h1, al subtítulo ni al CTA; se prueba por mutación con cadenas sintéticas (DSGN-05)"
    - "El movimiento nunca esconde contenido: ni `opacity` menor a 1, ni `visibility`, ni `display` en los cuadros; con JavaScript desactivado el hero y las 12 secciones se ven; el único JS propio sigue siendo `cta-focus.ts` (menos de 3 KB en línea) y `dist` no gana ningún archivo `.js` (DSGN-05; UI Considerations: loading)"
    - "La regla VERIFICAR de `scripts/lib/copy-rules.mjs` marca `[VERIFICAR` seguido de cualquier texto hasta `]` (o hasta el final si no cierra): `[VERIFICAR]`, `[VERIFICAR: nota]`, `[VERIFICAR rango]`, minúsculas y notas de varias líneas; no marca 'verificar' sin corchete, `[VERIFICADO]` ni el campo `reason`; con la regla nueva el YAML real en producción reporta solo PENDING y MISSING y ninguna marca (COPY-01)"
    - "`a11y-base.spec.ts` ya no menciona la clase heredada del texto de marca de la fase 1 y su aserto de 'textos de Ari visibles' deriva del YAML cuántas ranuras 'FALTA CONFIRMAR' tiene `/`; la prueba 9b de `copy.test.mjs` exige que producción reporte solo PENDING y MISSING, que toda afirmación con el relleno sea `pending` y que las rutas esperadas salgan del YAML con `walkClaims` (COPY-01)"
    - "Conexiones de fase: `BaseLayout` enlaza `favicon.svg` (`image/svg+xml`) y `favicon.ico` (`sizes=any`) en `/` y en `/privacidad/` y ambos responden 200; el sprite del collage aparece una vez por página (se queda en `SiteHeader`, decisión 3 de 02-02) y todo `use[href^='#']` resuelve a un id del mismo documento; `/` tiene 1 collage del hero, 1 de `#agenda` y 4 avatares; hay 4 CTA con `href='#agenda'`, el mismo nombre accesible que su texto visible y sin `aria-label`; ningún contenido invade el área de salvado `--logo-clear` del logo del header ni del footer a 390 y 1280 px (DSGN-03, DSGN-04)"
    - "`page-structure.spec.ts` afirma las 12 secciones de `PAGE_ORDER` exactas y en orden, 11 `h2` con textos que vienen del YAML, un solo `h1` dentro de `#inicio` y ningún salto de nivel (DSGN-03)"
    - "`tests/guards/phase2-static.test.mjs` prueba sobre `src/`: sin inyección de HTML en bruto (lista de excepciones vacía), sin colores hex en los `.astro` de componentes, páginas y layouts, sin `outline` en `none` o `0`, sin `line-height` con `!important` y con los únicos dos `<script>` de `AgendaSection.astro`; se prueba por mutación con cadenas sintéticas (DSGN-04)"
    - "La matriz de cierre pasa: sin scroll horizontal a 320, 390, 640 (zoom 200 % de 1280), 768, 1024 y 1280 px en `/` y en `/privacidad/` con `reduce` y con `no-preference`; con espaciado de texto forzado (SC 1.4.12) a 320 y 1280 px nada se recorta; sin JavaScript las 12 secciones se ven y el FAQ abre; con ClickUp bloqueado la página está completa y el enlace de respaldo se ve; todo enlace, resumen y botón visible mide 44 px o más de alto; ninguna sombra computada tiene desenfoque y no hay degradados (DSGN-03, DSGN-04)"
    - "axe de humo (`@axe-core/playwright` 4.13.0, etiquetas wcag2a, wcag2aa, wcag21a, wcag21aa y wcag22aa, sin el iframe) no reporta violaciones `critical` ni `serious` en `/` y en `/privacidad/` a 320, 390 y 1280 px; la compuerta completa de accesibilidad y Lighthouse son de la fase 3 (DSGN-03)"
    - "Peso y peticiones de `/`: HTML de menos de 61440 bytes sin comprimir, la suma de los `svg[data-collage]` de menos de 40960 bytes y solo se piden localhost, forms.clickup.com y app-cdn.clickup.com (DSGN-03)"
    - "Cierre visual: `critique` de página completa contra las 15 filas de 'Reference Synthesis' y los 8 rasgos de vibra con `impeccable`, seguido de `polish`, `harden`, `adapt` y `audit`, y la lista de antidefectos de `design-taste-frontend` (diales 7, 3 y 4), todos invocados con la herramienta Skill; `02-VISUAL-LOG.md` consolida el lote 0 de 02-02 y registra cada ronda `CIERRE` con verbos, hallazgos, correcciones, capturas y la tabla de los 8 rasgos con evidencia numérica; los 8 rasgos se cumplen en 3 ciclos como máximo o el plan se detiene y se muestra a Juan con capturas (DSGN-04)"
    - "Auditoría de copy y producción: `npm run pending` regenera `PENDING-COPY.md` de forma determinista y `node scripts/list-pending.mjs --check` sale 0; `npm run build` sin producción sale 0; con `PUBLIC_ENV=production` la guarda falla solo por reglas PENDING y MISSING (y por 'FALTA CONFIRMAR' en `dist`) (COPY-01)"
    - "`02-ARI-FINDINGS.md` reúne todo lo que las guardas rechazaron y lo que los seis SUMMARY reportaron: decisiones de alto impacto (morado #73187F frente a #4228D1 del logo, 20 frente a 30 minutos, AEO, licencia web de Hurme y aprobación de Outfit, consentimiento del equipo, avatares y favicon), texto que falta, texto que se muestra pero debe confirmarse, erratas del doc, afirmaciones de mercado ausentes y cómo se aplica cada respuesta; cada primer segmento de ruta con afirmaciones `pending` aparece citado (COPY-01)"
    - "Suite completa en verde: `node --test tests/guards/*.test.mjs`, `node scripts/check-contrast.mjs`, `npm run build`, `npx playwright test` (proyectos `chromium` y `live`; si ClickUp no responde, `live` se reintenta más tarde sin culpar al código) y `npm run test:e2e:isolated` (DSGN-03)"
    - statement: "Sensación real del movimiento y estado abierto o cerrado del `<summary>`: una persona mira la entrada del hero con `reduce` y con `no-preference` en un navegador real y comprueba con VoiceOver en Safari que el FAQ anuncia su estado; es comprobación manual, anotada en `02-VISUAL-LOG.md` como pendiente para Juan (UI Considerations: interaction)"
      verification: backstop
    - statement: "Composición de `HeroCollage` a 320 px sin cortar piezas ni tapar el CTA: se comprueba con la captura de cierre a 320 px y con la medición del primer pantallazo de `page-structure.spec.ts` (UI Considerations: visual)"
      verification: backstop
    - "Supuesto explícito (UI Considerations: copy y assets, sin resolver aquí): el texto que falta y los colores del logo dependen de Ari; este plan no los decide, los reúne en `02-ARI-FINDINGS.md`, y la página sigue mostrando 'FALTA CONFIRMAR' donde falta el dato"
  artifacts:
    - path: "src/styles/motion.css"
      provides: "Único archivo de movimiento de la fase 2: `hc-enter` (entrada del collage), `hc-look` (pupilas) y la transición de 150 ms del icono del FAQ, todo bajo `no-preference`"
      contains: "hc-enter"
    - path: "src/styles/global.css"
      provides: "Importa `motion.css` justo después de `tokens.css`"
      contains: "motion.css"
    - path: "src/components/sections/Faq.astro"
      provides: "Estado abierto del icono por rotación de `.faq-icon-v` (misma posición final con ambas preferencias)"
      contains: "faq-icon-v"
    - path: "src/components/collage/HeroCollage.astro"
      provides: "Seis grupos `[data-piece]` con `--i`, `--r` y `--r-from` (solo se agrega `--i` si falta)"
      contains: "data-piece"
    - path: "src/layouts/BaseLayout.astro"
      provides: "Enlaces de favicon (`favicon.svg` y `favicon.ico`) en el head"
      contains: "favicon.svg"
    - path: "scripts/lib/copy-rules.mjs"
      provides: "Regla VERIFICAR que reconoce `[VERIFICAR: nota]` y `[VERIFICAR rango]`, las formas reales del doc de Ari"
      contains: "VERIFICAR"
    - path: "tests/guards/copy.test.mjs"
      provides: "Pruebas 3b a 3e de la regla VERIFICAR con las cadenas reales del doc, y la 9b terminada"
      contains: "VERIFICAR rango"
    - path: "tests/guards/fixtures/verificar-nota.yaml"
      provides: "Fixture con `[VERIFICAR: nota]` para la prueba de CLI"
      contains: "VERIFICAR:"
    - path: "tests/guards/motion-budget.test.mjs"
      provides: "Guarda estática del presupuesto de movimiento con pruebas por mutación"
      contains: "keyframes"
    - path: "tests/guards/phase2-static.test.mjs"
      provides: "Guarda estática de higiene de fase (HTML en bruto, hex, outline, line-height, scripts)"
      contains: "RAW_HTML_ALLOW"
    - path: "tests/e2e/lib/motion.ts"
      provides: "Política de movimiento compartida por los specs: lista blanca, `motionSnapshot`, `expectNoMotion`, `expectMotionWithinBudget`"
      contains: "expectMotionWithinBudget"
    - path: "tests/e2e/motion.spec.ts"
      provides: "Medición de la entrada del hero, las pupilas y el icono del FAQ con ambas preferencias y sin JavaScript"
      contains: "hc-enter"
    - path: "tests/e2e/phase-closing.spec.ts"
      provides: "Conexiones de fase, matriz de cierre, axe de humo, peso y peticiones, y hoja de contacto por lote"
      contains: "AxeBuilder"
    - path: "tests/e2e/page-structure.spec.ts"
      provides: "Aserto de las 12 secciones exactas y los 11 h2 derivados del YAML"
      contains: "H2_SOURCE"
    - path: "package.json"
      provides: "`@axe-core/playwright` 4.13.0 como dependencia de desarrollo (la compuerta completa es de la fase 3)"
      contains: "@axe-core/playwright"
    - path: ".planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md"
      provides: "Registro consolidado: lote 0 de 02-02, rondas CIERRE, tabla de los 8 rasgos y de las 15 filas de síntesis"
      contains: "CIERRE1"
    - path: ".planning/phases/02-secciones-marca-y-copy/02-ARI-FINDINGS.md"
      provides: "Lista única de decisiones, faltantes y erratas para Ari"
      contains: "#73187F"
    - path: "PENDING-COPY.md"
      provides: "Lista regenerada de forma determinista al cierre de la fase"
      contains: "faq.items"
  key_links:
    - from: "src/styles/global.css"
      to: "src/styles/motion.css"
      via: "`@import './motion.css'` justo después del de `tokens.css`; el CSS resultante contiene `hc-enter`"
    - from: "src/styles/motion.css"
      to: "src/components/collage/HeroCollage.astro"
      via: "selectores `#inicio [data-piece]` y `#inicio` con `.hc-pupil` o `[data-pupil]`, y las propiedades `--i`, `--r` y `--r-from` de cada grupo"
    - from: "src/styles/motion.css"
      to: "src/components/sections/Faq.astro"
      via: "`.faq-icon-v` con `transition: transform var(--dur-1)` solo bajo `no-preference`; `Faq.astro` fija el estado abierto con `details[open]`"
    - from: "tests/e2e/lib/motion.ts"
      to: "tests/e2e/page-structure.spec.ts"
      via: "los cinco specs que antes afirmaban cero animaciones usan `expectNoMotion` con `reduce` y `expectMotionWithinBudget` con `no-preference`"
    - from: "scripts/lib/copy-rules.mjs"
      to: "scripts/check-copy.mjs"
      via: "`checkCopy` agrega violaciones VERIFICAR al arreglo `content`, que `prebuild` bloquea en producción"
    - from: "src/layouts/BaseLayout.astro"
      to: "public/favicon.svg"
      via: "`<link rel='icon'>` con `/favicon.svg` y `/favicon.ico` (archivos de 02-02)"
    - from: "tests/e2e/phase-closing.spec.ts"
      to: "@axe-core/playwright"
      via: "`new AxeBuilder({ page })` con las etiquetas wcag2a a wcag22aa y `exclude('iframe')`"
    - from: ".planning/phases/02-secciones-marca-y-copy/02-ARI-FINDINGS.md"
      to: "PENDING-COPY.md"
      via: "cada primer segmento de ruta con afirmaciones `pending` está citado en el archivo de hallazgos"
  prohibitions:
    - requirement_id: COPY-01
      category: transparency
      status: resolved
      verification: judgment
      statement: "MUST NOT editar, humanizar, corregir ni reordenar el texto del doc de Ari en ninguna tarea, ni siquiera durante `polish`: la iteración visual cambia estilo y disposición, no palabras; la única edición posible de `landing.es.yaml` es convertir una marca `[VERIFICAR` que la regla ampliada detecte en un `text` al patrón de 02-03 (relleno 'FALTA CONFIRMAR', `pending`, `confirm_by: Ari` y `reason` con el texto literal del doc)"
    - requirement_id: COPY-01
      category: quality
      status: resolved
      verification: test
      statement: "MUST NOT aflojar la guarda de producción: `PUBLIC_ENV=production` debe seguir fallando por PENDING y MISSING; la regla VERIFICAR solo se amplía y las pruebas por mutación de VOSEO, DASH, AEO, INVERSION y VERIFICAR siguen en verde"
    - requirement_id: DSGN-05
      category: motion
      status: resolved
      verification: test
      statement: "MUST NOT animar `opacity`, `visibility` ni `display`, ni el h1, el subtítulo o el CTA, ni agregar marquee, carruseles, animaciones infinitas, parpadeo, parallax sobre texto, scroll-jacking, ojos que siguen el puntero (diferido a v2 por el UI-SPEC) ni `animation-timeline`; el movimiento total sigue siendo finito y menor a 1,4 s"
    - requirement_id: DSGN-05
      category: motion
      status: resolved
      verification: test
      statement: "MUST NOT declarar `animation` ni `transition` fuera de `@media (prefers-reduced-motion: no-preference)` ni agregar JavaScript, `IntersectionObserver` o `ClientRouter` para mover algo: el único JS propio sigue siendo `cta-focus.ts`"
    - requirement_id: DSGN-04
      category: quality
      status: resolved
      verification: judgment
      statement: "MUST NOT saltarse `impeccable` ni `design-taste-frontend`: las tareas 1 y 4 los invocan con la herramienta Skill (verbos `animate`, `critique`, `polish`, `harden`, `adapt` y `audit`) y registran en `02-VISUAL-LOG.md` lo que hicieron; la tarea 4 no se cierra con un rasgo de vibra fallando"
    - requirement_id: DSGN-04
      category: scope
      status: resolved
      verification: test
      statement: "MUST NOT modificar el bloque del formulario de ClickUp (iframe, atributos, `min-height`, script, enlace de respaldo, `noscript`, borde, radio, sombra y `overflow` de `.form-embed`), `cta-focus.ts`, la geometría o los colores de las piezas del collage, ni los tokens de color; `polish` solo toca archivos que ya existen en `src/components`, `src/styles` y `src/layouts`, sin dependencias nuevas ni JS nuevo"
    - requirement_id: DSGN-04
      category: quality
      status: resolved
      verification: test
      statement: "MUST NOT usar inyección de HTML en bruto, colores hex en `src/components` o `src/pages`, `outline: none`, `line-height` con `!important` ni sombras difusas o degradados: `tests/guards/phase2-static.test.mjs` y el bloque de la matriz de cierre lo vigilan"
    - requirement_id: DSGN-03
      category: quality
      status: resolved
      verification: test
      statement: "MUST NOT aceptar scroll horizontal, texto recortado con espaciado forzado ni elementos interactivos de menos de 44 px de alto en `/` o `/privacidad/`: se corrige el layout, no la prueba"
    - requirement_id: DSGN-03
      category: scope
      status: resolved
      verification: judgment
      statement: "MUST NOT correr Lighthouse ni la compuerta completa de accesibilidad (`REPORT.md`, `EXCEPTIONS.md`, lector de pantalla), ni tocar sitemap, JSON-LD o `noindex` de `/privacidad/`: son de la fase 3; axe aquí es solo una prueba de humo"
    - requirement_id: DSGN-04
      category: safety
      status: resolved
      verification: test
      statement: "MUST NOT instalar ninguna dependencia distinta de `@axe-core/playwright` 4.13.0 ni instalarla si `gsd-tools query package-legitimacy check --ecosystem npm @axe-core/playwright` (sin sufijo de versión) no da OK; MUST NOT enviar el formulario de ClickUp, leer `.env` ni sumar peticiones a hosts distintos de forms.clickup.com y app-cdn.clickup.com"
---

<objective>
Cerrar la fase 2: dar a la landing el movimiento sobrio que fija el UI-SPEC (DSGN-05), resolver los huecos de guardas y limpieza que dejaron los planes 01 a 06, iterar la página completa contra la vibra de las referencias de Ari a cinco anchos (DSGN-03, DSGN-04) y auditar el copy para que Ari reciba una sola lista de decisiones (COPY-01). Es el plan que cierra: consume los ganchos que los otros planes dejaron nombrados y no reinventa ninguno.

Purpose: que la página se sienta viva sin esconder nada ni molestar a quien pide menos movimiento, que las guardas describan el doc real de Ari (`[VERIFICAR: nota]` incluido), que ninguna aserción de la fase 1 contradiga el diseño nuevo y que Juan y Ari tengan evidencia medida (capturas, anchos, axe de humo, pruebas verdes) y una lista clara de lo que falta decidir. Historia de usuario: **As a** dueño de un negocio, **I want to** entender en segundos qué hace Loops Growth y por qué confiar, **so that** llegue al formulario decidido a agendar.

Output: `src/styles/motion.css` (entrada del collage, pupilas y giro del icono del FAQ), la política de movimiento compartida `tests/e2e/lib/motion.ts` y `motion.spec.ts`, la regla VERIFICAR ampliada con sus pruebas, las guardas estáticas `motion-budget.test.mjs` y `phase2-static.test.mjs`, las conexiones de fase (favicon, sprite, collages, CTA, área de salvado, 12 secciones y 11 h2), `phase-closing.spec.ts` (matriz, axe de humo, peso y hoja de contacto), `02-VISUAL-LOG.md` consolidado y `02-ARI-FINDINGS.md`.

Decisiones de CONTEXT.md que implementa (por nombre de sección; CONTEXT no numera D-XX): "Estilo visual, collage y movimiento" (todo movimiento solo bajo `motion-safe`, nunca esconde contenido, `impeccable` con `animate`, `critique`, `polish` y `adapt` más `design-taste-frontend`, iteraciones hasta la vibra), "Textos pending" (relleno visible, `PENDING-COPY.md` determinista, el build de producción sigue fallando mientras haya `pending`), "Morado de marca y logo" (la diferencia #73187F frente a #4228D1 se reporta a Ari) y "Excepciones del contrato de diseño aprobadas por Juan" (96 px de `--section-y` y 4 CTA, verificados en la matriz).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-secciones-marca-y-copy/02-CONTEXT.md
@.planning/phases/02-secciones-marca-y-copy/02-PLANNING-BRIEF.md
@.claude/CLAUDE.md

`02-UI-SPEC.md` (73 KB) y `02-ARI-COPY-V2.md` NO se cargan enteros: cada tarea indica en `<read_first>` la sección exacta (ubicarla con `grep -n '^## \|^### '` y leer con `offset` y `limit`). `PRODUCT.md` y `DESIGN.md` (raíz) los generó el plan 02-01: aquí solo se leen. Los SUMMARY de 02-01 a 02-06 se leen solo por sección (`grep -n -i` de erratas, hallazgos y desviaciones), nunca completos.
</context>

## Artifacts this phase produces

Rutas relativas a la raíz del repositorio. Los nombres de abajo los reutiliza la fase 3 (compuerta de accesibilidad, Lighthouse, SEO) y no se renombran después.

- **Movimiento**: `src/styles/motion.css` con `@keyframes hc-enter` y `hc-look`, las propiedades `--hc-dur` (600ms), `--hc-step` (80ms), `--hc-end` (fin de la entrada, 1000ms), `--look` (4 unidades del viewBox), `--look-dx` y `--look-dy` (dirección hacia el CTA, en -1, 0 o 1), y la transición de `transform` del icono `.faq-icon-v`; todo bajo un único `@media (prefers-reduced-motion: no-preference)`. Ganchos consumidos tal cual de 02-01, 02-02 y 02-06: `#inicio [data-piece]` con `--i`, `--r` y `--r-from`, pupilas `.hc-pupil` (02-01) o `[data-pupil]` (02-02), y `.faq-icon-v`.
- **Política de movimiento de las pruebas**: `tests/e2e/lib/motion.ts` (`MOTION_ALLOWLIST`, `motionSnapshot`, `expectNoMotion`, `expectMotionWithinBudget`) y `tests/e2e/motion.spec.ts`. La fase 3 la reutiliza para cualquier página nueva.
- **Guardas**: `tests/guards/motion-budget.test.mjs` (presupuesto de movimiento sobre `src/`), `tests/guards/phase2-static.test.mjs` (con la constante `RAW_HTML_ALLOW`, vacía: la fase 3 agrega ahí el JSON-LD con su razón si lo necesita) y la regla VERIFICAR ampliada con `tests/guards/fixtures/verificar-nota.yaml`.
- **Pruebas de cierre**: `tests/e2e/phase-closing.spec.ts` con los bloques 'conexiones de fase', 'matriz de desborde', 'espaciado de texto', 'sin JavaScript', 'ClickUp bloqueado', 'axe de humo', 'objetivos, sombras y área de salvado', 'peso y peticiones' y 'hoja de contacto' (esta última solo con `PHASE2_BATCH`, guarda `test-results/phase2/<lote>-<id>-<ancho>.png` y las hojas `<lote>-hoja-1280.png` y `<lote>-hoja-390.png`). Lotes de captura: `CIERRE1`, `CIERRE2` y, si hace falta, `CIERRE3`, con la herramienta de 02-01 (`PHASE2_BATCH=<lote> npx playwright test --project=chromium tests/e2e/page-structure.spec.ts -g "captura"`).
- **Dependencia**: `@axe-core/playwright` 4.13.0 (exacta) en `devDependencies`; la fase 3 la reutiliza para la compuerta.
- **Evidencia**: `02-VISUAL-LOG.md` consolidado, `02-ARI-FINDINGS.md` (una sola lista para Ari), `PENDING-COPY.md` regenerado y `02-07-SUMMARY.md` con el resultado de las pruebas, los cambios de `polish`, las decisiones de criterio de abajo y las verificaciones humanas que quedan para Juan.
- **Notas para la fase 3** (no se hacen aquí): Lighthouse y Core Web Vitals, `REPORT.md`, `EXCEPTIONS.md` y la revisión con lector de pantalla, excluir `/privacidad/` del sitemap mientras siga `noindex`, JSON-LD (agregarlo a `RAW_HTML_ALLOW` con su razón) y la deriva de collage por scroll que el UI-SPEC marca opcional.

## Ranuras de copy de este plan

Fuente única: `02-ARI-COPY-V2.md`. Este plan no agrega ninguna ranura nueva: cambia estilo, pruebas y guardas, y reúne lo que los planes 01 a 06 dejaron `pending`. Casos del brief: **A** existe en el doc y pasa las guardas, **B** nombre estructural de CONTEXT o del UI-SPEC, **C** falta en el doc y se muestra 'FALTA CONFIRMAR', **D** microcopy estructural.

| Ranura | Origen | Caso | Estado en el YAML |
|--------|--------|------|-------------------|
| Toda ranura existente (A, B, C y D de los planes 01 a 06) | YAML tal como lo dejó 02-06 | sin cambios | La tarea 5 la audita: lista determinista, producción falla solo por PENDING y MISSING |
| Cualquier `text` que contenga `[VERIFICAR` y que la regla ampliada detecte | Doc de Ari, Pilar 4 o sección 4 | C (única edición posible del YAML) | Se convierte al patrón de 02-03: 'FALTA CONFIRMAR', `pending`, `confirm_by: Ari` y `reason` con el texto literal; se espera que no haya ninguna porque 02-03 y 02-04 ya lo hicieron |

Ninguna cadena nueva dispara VOSEO, DASH, AEO, VERIFICAR ni INVERSION. Las cadenas de `02-ARI-FINDINGS.md` no son copy de la página: son notas para Ari, en español neutro sin voseo ni guiones largos; las citas del doc van literales y con sus erratas.

## Supuestos y desviaciones (cada una se revierte con un cambio pequeño)

1. **Deriva de collage por scroll (opcional, prioridad baja en el UI-SPEC) no se implementa.** El presupuesto de movimiento del UI-SPEC la marca opcional; `animation-timeline: view()` crearía animaciones enlazadas al scroll que `getAnimations()` cuenta con `no-preference` y una excepción más a la regla de cero animaciones en reposo. Agregarla es un bloque `@supports (animation-timeline: view())` dentro del `@media` de `motion.css` más una entrada en la lista blanca.
2. **Alcance `#inicio`, no `.hero-collage` en general.** La entrada y las pupilas se declaran bajo `#inicio` para que la hoja `/marca/hoja/` (que monta `HeroCollage` tres veces) y cualquier otro collage sigan estáticos; así el cero de 02-02 sigue valiendo en la hoja.
3. **Relleno `backwards`, no `forwards`.** Cada animación es visible en su cuadro inicial durante el retraso y, al terminar, el elemento vuelve a su estilo estático, que es exactamente el estado con `reduce`. Resultado: a los 2,5 s `getAnimations()` vale 0 con ambas preferencias y no hay dos estados finales distintos que mantener.
4. **Pupilas en unidades del viewBox.** El UI-SPEC pide 4 px; el SVG escala con su viewBox, así que 4 unidades son entre 2 y 4 px en pantalla según el ancho del collage. La prueba mide la dirección (producto punto positivo hacia el CTA a 390 y 1280 px) y el tiempo, no los píxeles de pantalla. Revertir: multiplicar `--look` por el factor de escala con una consulta de contenedor (no se recomienda).
5. **Icono del FAQ por rotación de `.faq-icon-v`.** 02-06 ocultaba la barra vertical con `details[open]`, que no admite transición; aquí la misma regla pasa a `transform: rotate(90deg)` (la barra vertical rota sobre la horizontal y queda un minus) y la transición de 150 ms vive en `motion.css`. El estado final es igual con ambas preferencias. Revertir: volver a la regla de ocultar y quitar la transición (se pierde el giro).
6. **Nombres de ganchos.** Los toma de 02-01 y 02-02, que listan `HeroCollage.astro` en la misma wave y que el orquestador ya debía resolver: los grupos son `[data-piece]` en ambos, las pupilas son `.hc-pupil` en 02-01 y `[data-pupil]` en 02-02, y `--i` solo lo promete 02-01. La regla CSS usa ambos selectores de pupila y la tarea 1 agrega `--i` si el archivo vigente no lo trae. Si algún nombre difiere, manda el archivo y se anota en el SUMMARY.
7. **Los cinco specs de las secciones cambian su aserto de movimiento.** 02-01 y 02-03 a 02-06 afirman cero animaciones con `no-preference` en `/`; con la entrada del hero eso deja de ser cierto. Se conserva el cero con `reduce` y con `no-preference` en `/privacidad/` y en la hoja, y en `/` con `no-preference` se exige que todo lo que corra esté en la lista blanca. Revertir: quitar el movimiento.
8. **El sprite se queda en `SiteHeader`** (decisión 3 de 02-02): `/` y `/privacidad/` usan `BaseLayout` con `SiteHeader`, así que hay exactamente uno por página y la guarda de 02-02 se cumple. Revertir: mover `<CollageSprite />` a `BaseLayout` y ajustar esa guarda (un cambio de una línea).
9. **`AgendaCollage` ya lo monta 02-06** en `#agenda`; aquí solo se comprueba su presencia, sin editar `AgendaSection`.
10. **Clase heredada del texto de marca**: 02-02 la conservó en el contenedor del logo para no editar `a11y-base.spec.ts`; aquí se retira la aserción, la clase y su regla CSS (decisión 8 de 02-02).
11. **Regla VERIFICAR con cierre opcional.** La expresión acepta `[VERIFICAR` seguido de cualquier carácter que no sea `]` y un `]` opcional: marca también una nota sin cerrar, porque un falso negativo dejaría pasar una afirmación sin verificar a producción y un falso positivo solo obliga a convertirla en 'FALTA CONFIRMAR'. Revertir: exigir el `]`.
12. **axe entra ahora como dependencia de desarrollo** (la fase 3 lo necesita de todos modos): versión exacta 4.13.0 como pide `STACK.md`, veredicto de legitimidad OK el 2026-09-19 (existe, unos 7,3 millones de descargas semanales, repositorio de dequelabs, sin postinstall). Revertir: quitar la dependencia y el bloque 'axe de humo'; la fase 3 la instala.
13. **Hojas de contacto para leer la página entera.** Una captura de página completa a 1280 px es demasiado alta para leerla y 24 recortes cuestan demasiado contexto; la herramienta compone dos hojas (1280 y 390 px) y el ejecutor abre como máximo 4 recortes por ronda. Las capturas nunca se versionan (`test-results/`).
14. **640 px en la matriz** representa el zoom de 200 % sobre 1280 px (SC 1.4.4 y 1.4.10); 320 px representa el zoom de 400 %.
15. **`02-ARI-FINDINGS.md` es una nota para una persona, no copy de la página**: se escribe en español neutro con las reglas de estilo de Juan (sin guiones largos, sin voseo) y su prosa propia pasa por `humanizer`; las citas del doc de Ari van literales y no se tocan (COPY-01). Revertir: omitir la pasada de `humanizer`.

## UI Considerations (lift del UI-SPEC para este plan)

- **covered** (ya en `must_haves.truths` como texto): a11y-motion (solo `no-preference`, solo `transform`, cero animaciones con `reduce`, presupuesto cerrado y guarda estática), loading (todo SVG en línea, cero `.js` nuevos), empty (toda ranura sin dato muestra 'FALTA CONFIRMAR' y la auditoría la cuenta), long-text y overflow (matriz a seis anchos con espaciado de texto forzado), error (ClickUp bloqueado con la página completa), a11y-color (sin cambios de tokens; `check-contrast` sigue en verde) y zero-one-many (12 secciones y 11 h2 exactos).
- **backstop** (en `must_haves.truths` como `{statement, verification: backstop}`): interaction del `<summary>` (VoiceOver en Safari, anotado en `02-VISUAL-LOG.md` para Juan) y visual de `HeroCollage` a 320 px (captura de cierre y medición del primer pantallazo).
- **No aplican a este plan**: partial (MetricCard, plan 04; entra en la matriz de anchos).
- **unresolved que este plan asume**: copy y assets siguen abiertos para Ari (texto que falta, colores del logo, licencia de Hurme); se reúnen en `02-ARI-FINDINGS.md` y no se deciden aquí.

## Cobertura de fuentes (este plan)

| Fuente | Ítem | Estado |
|--------|------|--------|
| GOAL (ROADMAP fase 2) | Landing completa con identidad de marca, de 320 a 1280 px sin scroll horizontal, todo el copy tal cual del doc de Ari | Cierre y evidencia aquí (tareas 3 a 5); secciones y activos en 02-01 a 02-06 |
| REQ | DSGN-05 (movimiento desactivable con `reduce`) | Tareas 1 y 2 |
| REQ | DSGN-03 (320 a 1280 px sin scroll horizontal) | Tareas 3 y 4 (estructura, matriz, peso) |
| REQ | DSGN-04 (skills `impeccable` y `design-taste-frontend`) | Tareas 1 y 4 (cierre con `critique`, `polish`, `harden`, `adapt`, `audit`) |
| REQ | COPY-01 (copy tal cual, se pide a Ari lo que falta) | Tareas 3 y 5 (guarda VERIFICAR, limpieza de aserciones, auditoría y hallazgos) |
| RESEARCH | No existe `02-RESEARCH.md`; `STACK.md` (axe 4.13.0, Playwright 1.63.0) y el UI-SPEC son la fuente | Sin ítems pendientes |
| CONTEXT | Estilo visual y movimiento, textos pending, morado y logo, excepciones de Juan (96 px, 4 CTA) | Este plan |
| UI-SPEC | Presupuesto de movimiento, Design Method (fila Cierre, ciclo, lista de vibra, 3 ciclos máximo), Reference Synthesis, Verification Hooks (uno por uno en las tareas 2 a 5) | Este plan |
| Fuera de este plan (no son huecos) | Lighthouse, Core Web Vitals, compuerta completa de accesibilidad, sitemap, JSON-LD y `noindex` de `/privacidad/` (fase 3); deriva por scroll (opcional en el UI-SPEC, decisión 1); ojos que siguen el puntero (diferido a v2 en el UI-SPEC); verificaciones humanas de la fase 1 (siguen diferidas) | Diferido a esos planes o a Juan |

## Presupuesto de contexto (27 archivos en `files_modified`, unos 13 con autoría real)

Casi no tienen autoría: `global.css` (una línea), `HeroCollage.astro` (solo si falta `--i`), `BaseLayout.astro` (dos enlaces), `SiteHeader.astro` y `Logo.astro` (retirar una clase), `landing.es.yaml` (solo si aparece una marca), los cinco specs de las secciones (una o dos líneas cada uno), `package.json` y `package-lock.json`, `PENDING-COPY.md` (lo genera `npm run pending`) y `02-VISUAL-LOG.md` (registro). Estimación de 98k tokens (confianza baja, sin historial de calibración, factor 1): tarea 1 unos 20k (incluye la política compartida y los cinco specs), tarea 2 unos 15k, tarea 3 unos 23k, tarea 4 unos 27k (dos skills, dos rondas de captura y las hojas de contacto) y tarea 5 unos 13k. El costo variable es la iteración visual: el ejecutor lee las dos hojas de contacto por ronda y como máximo 4 recortes, y las mediciones de los specs reemplazan a las capturas.

Puntos de corte: cada tarea termina con el build verde y commiteada. Tras las tareas 1 y 2 (movimiento completo) y tras la 3 (guardas y conexiones), si el contexto ya pasó de 50 %, el ejecutor hace commit, escribe `02-07-SUMMARY.md` con `status: partial` (hecho y pendiente) y la tarea siguiente corre en un ejecutor nuevo. La tarea 4 no se parte a mitad de un ciclo: si al empezarla el contexto pasa de 50 %, se corta antes. Si el checker exige partir el plan por tamaño (27 archivos, cinco tareas), el corte limpio es el límite de la tarea 3: **07a** = tareas 1 a 3 (movimiento, guardas y conexiones) y **07b** = tareas 4 y 5 (iteración de cierre y auditoría), en waves distintas y sin archivos compartidos salvo `tests/e2e/phase-closing.spec.ts` (07a lo crea, 07b lo extiende).

## Reglas de ejecución heredadas de la fase 1

- Playwright: `astro preview` se va a segundo plano dentro de un agente y rompe el `webServer`. Antes de correr specs: `npx astro build && npx astro preview --port 4322`; tras cada cambio de `src/`, `public/` o `astro.config.mjs`, `npx astro build` de nuevo (`tests/global-setup.ts` falla si `dist` está viejo) y reiniciar el preview; al terminar, `npx astro preview stop`. Proyectos: `chromium` (no depende de ClickUp) y `live` (necesita `forms.clickup.com`; antes, `curl -s -o /dev/null -w '%{http_code}' https://forms.clickup.com`, y si no da 200, reintentar más tarde sin culpar al código). Scripts: `test:e2e`, `test:e2e:offline`, `test:e2e:live` y `test:e2e:isolated`.
- Guardas que siguen en verde en cada tarea: `node --test tests/guards/*.test.mjs`, `node scripts/check-contrast.mjs`, `npm run build`, `npm run pending` y `node scripts/list-pending.mjs --check`; `PUBLIC_ENV=production` falla solo por PENDING y MISSING (y por 'FALTA CONFIRMAR' en `dist`).
- Las pruebas derivan los textos y las cantidades esperadas del YAML (lectura con `yaml`, mismo criterio de `walkClaims` y `fill`); nunca listas de pending ni cadenas resueltas escritas a mano.
- No leer `.env` (un hook de secretos bloquea comandos que lo mencionen; usar `.env.example`), nunca enviar el formulario de ClickUp, detener todo servidor dev o preview antes de volver y quedarse en la rama actual (el protocolo de commit de GSD crea una rama de fase porque `master` está protegida; no cambiar de rama ni tocar la configuración de git).
- Antes de editar en cada tarea, anotar `git rev-parse HEAD` como BASE para los criterios de alcance (`git diff --name-only BASE` debe estar contenido en `files_modified`, más los archivos de `polish` que la tarea 4 declare en el SUMMARY).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visitante a sitio estático | HTML y CSS estáticos; sin entrada del visitante en este plan; el movimiento es CSS puro |
| Sitio a formulario de ClickUp | iframe y script de terceros intactos; este plan no los toca ni agrega peticiones nuevas |
| Equipo de desarrollo a registro npm | una dependencia de desarrollo nueva (`@axe-core/playwright`) entra a la máquina y a las pruebas, no al build |
| Repositorio a Ari | `02-ARI-FINDINGS.md` cita el doc de Ari y rutas del YAML; sin secretos ni datos de personas más allá de los nombres ya publicados en el copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07-01 | Denial of Service (personas con sensibilidad al movimiento) | `src/styles/motion.css` | medium | mitigate | Solo `no-preference`, solo `transform`, finito y menor a 1,4 s, sin parpadeo; guarda estática `motion-budget.test.mjs` y medición con `getAnimations()` en `reduce` (0) |
| T-02-07-02 | Tampering | Regla VERIFICAR ampliada en `copy-rules.mjs` (un falso negativo deja pasar una afirmación sin verificar) | medium | mitigate | Expresión lineal sin cuantificadores anidados, que además marca notas sin cerrar; pruebas por mutación con las cadenas reales del doc y comprobación sobre el YAML real |
| T-02-07-03 | Tampering | Cambios de `polish` que alteren el copy de Ari o el bloque del formulario | medium | mitigate | Regla de alcance con BASE: `git diff BASE -- src/content/landing.es.yaml` limitado a la conversión de una marca; las pruebas de la fase 1 del iframe siguen corriendo; el copy se imprime sin leer el estado |
| T-02-07-04 | Tampering | Inyección de HTML en bruto por descuido en un componente | low | mitigate | `phase2-static.test.mjs` prueba sin inyección de HTML en bruto con lista de excepciones vacía |
| T-02-07-05 | Information Disclosure | Capturas de `test-results/` o `02-ARI-FINDINGS.md` con datos indebidos | low | mitigate | Las capturas se toman con ClickUp bloqueado (sin formulario) y no se versionan; el archivo de hallazgos cita solo copy del doc y rutas del YAML; no se lee `.env` |
| T-02-07-06 | Denial of Service | Peticiones a terceros nuevas o peso extra por el movimiento | low | mitigate | Prueba de peticiones (solo localhost, forms.clickup.com y app-cdn.clickup.com), HTML de menos de 61440 bytes y cero `.js` nuevos en `dist` |
| T-02-07-07 | Repudiation | Iteración visual sin evidencia (nadie sabe qué se cambió ni por qué) | low | mitigate | `02-VISUAL-LOG.md` registra verbos, hallazgos, correcciones y capturas por ronda; cada edición de `polish` se anota con archivo y medida |
| T-02-07-08 | Information Disclosure | Envío accidental del formulario de ClickUp durante las pruebas | low | accept | Ninguna prueba lo envía (regla heredada de la fase 1); los specs solo miden y navegan |
| T-02-07-SC | Tampering | Instalación npm de `@axe-core/playwright` 4.13.0 (y `axe-core` transitivo) | high | mitigate | Verificador de legitimidad (`package-legitimacy check --ecosystem npm @axe-core/playwright`, sin sufijo de versión) con veredicto OK el 2026-09-19; la tarea 4 lo vuelve a correr y se detiene si no da OK; versión exacta, solo `devDependencies`, sin postinstall, fuera del build de producción |
</threat_model>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer del movimiento: entrada del collage del hero de punta a punta, medida con ambas preferencias</name>
  <files>src/styles/motion.css, src/styles/global.css, src/components/collage/HeroCollage.astro, tests/e2e/lib/motion.ts, tests/e2e/motion.spec.ts, tests/e2e/page-structure.spec.ts, tests/e2e/sections-problem-solution.spec.ts, tests/e2e/results-cases.spec.ts, tests/e2e/team-includes-how.spec.ts, tests/e2e/closing-sections.spec.ts</files>
  <precondition>Existen `src/components/collage/HeroCollage.astro` con 6 elementos `[data-piece]`, `src/components/sections/Hero.astro` con la sección `id="inicio"` que lo monta y los specs de 02-01, 02-03, 02-04, 02-05 y 02-06; si falta alguno, detener la tarea y reportarlo.</precondition>
  <read_first>
    - .planning/phases/02-secciones-marca-y-copy/02-PLANNING-BRIEF.md (reglas obligatorias de forma, copy y lecciones de la fase 1)
    - .planning/phases/02-secciones-marca-y-copy/02-UI-SPEC.md: `### Presupuesto de movimiento (DSGN-05; ...)` y `### JavaScript` de `## Interaction Contract`, y la fila Lote A de `### Verbos de impeccable por lote`; ubicar con `grep -n '^## \|^### '` y leer con `offset` y `limit`
    - src/components/collage/HeroCollage.astro (completo, menos de 8 KB): ganchos `[data-piece]`, `--i`, `--r`, `--r-from`, pupilas y `transform-box`
    - src/components/sections/Hero.astro (id `inicio`, cómo monta el collage y el CTA)
    - src/styles/global.css (líneas 1 a 25: los `@import`) y src/styles/tokens.css (`--ease-out`, `--dur-1` y el bloque `reduce`)
    - `grep -rn "getAnimations" tests/e2e` para ubicar los cinco specs que hoy afirman cero animaciones
    - .planning/phases/02-secciones-marca-y-copy/02-01-SUMMARY.md y 02-02-SUMMARY.md (solo las secciones de desviaciones: qué versión de `HeroCollage.astro` quedó y cuáles son sus ganchos)
    - PRODUCT.md y DESIGN.md (raíz, los generó 02-01)
  </read_first>
  <action>
    Implementa DSGN-05 en su recorte más fino, de punta a punta por todas las capas: el marcado del collage, el CSS global, la medición en navegador y la política de movimiento de la suite. Las pupilas y el icono del FAQ son de la tarea 2. Antes de editar anota `git rev-parse HEAD` como BASE.

    Skills primero (per DSGN-04, regla de Juan). Con la herramienta Skill invoca `impeccable` (carga `reference/craft-floor.md`, corre `context` y lee `PRODUCT.md` y `DESIGN.md`; verbo `animate`, limitado al presupuesto del UI-SPEC) y `design-taste-frontend` con la lectura declarada "landing de captación B2B para dueños de e-commerce, lenguaje collage pop de marca, con reglas de accesibilidad que mandan sobre la estética" y los diales DESIGN_VARIANCE 7, MOTION_INTENSITY 3 y VISUAL_DENSITY 4. Si una skill y el contrato chocan, manda el contrato.

    Sondeo de ganchos (per la desviación 6 de Supuestos). Lee `HeroCollage.astro`: confirma 6 grupos `[data-piece]` con `--r` y `--r-from` en línea (con unidad `deg`) y el reposo estático `rotate(var(--r))` con `transform-box: fill-box` y `transform-origin: center`. Si los grupos no traen `--i`, agrégalo (de 0 a 5 en el orden del DOM) al estilo en línea de cada grupo: es la única edición permitida en ese archivo (no cambies geometría, colores ni el tope de 8192 bytes). Si algún nombre difiere de los de este plan, manda el archivo: adapta los selectores de `motion.css` y anótalo en el SUMMARY.

    Política de movimiento primero (la suite sigue verde). Crea `tests/e2e/lib/motion.ts` (no es un spec: Playwright solo corre `*.spec.ts`) con `MOTION_ALLOWLIST`, `motionSnapshot(page)` (mapea `document.getAnimations()` a tipo `CSSAnimation` o `CSSTransition`, nombre o propiedad, destino como selector corto, propiedades animadas, duración, retraso, iteraciones y relleno), `expectNoMotion(page)` (la lista es vacía) y `expectMotionWithinBudget(page)` (cada elemento coincide con la lista blanca: `hc-enter` sobre `#inicio [data-piece]`, `hc-look` sobre las pupilas, transiciones de `transform` sobre `.faq-icon-v` y las transiciones de hover del CTA y de `.agenda-link` de la fase 1). Luego edita los cinco specs que afirman cero animaciones (`grep -rn getAnimations tests/e2e`: page-structure (h), sections-problem-solution, results-cases, team-includes-how y closing-sections): conserva el cero con `reduce` usando `expectNoMotion`, y con `no-preference` sustitúyelo por `expectMotionWithinBudget` en `/`; en `/privacidad/` sigue siendo cero con ambas preferencias. No toques otras aserciones de esos archivos. Corre esos specs: deben pasar antes de crear el movimiento.

    Pruebas del tracer (rojo). Crea `tests/e2e/motion.spec.ts` con el bloque 'entrada del collage del hero' y `test.use({ reducedMotion })` por preferencia: con `reduce`, las 6 piezas tienen `animation-name: none` y `getAnimations()` es 0 a la carga y a los 2,5 s; con `no-preference`, cada pieza tiene computed `animation-name` `hc-enter`, `animation-duration` 0.6s, una iteración, relleno `backwards` y `animation-delay` igual a su `--i` por 0.08s, y el máximo de retraso más duración es menor a 1.2s; la regla `@keyframes hc-enter` (leída de `document.styleSheets`) declara solo `transform` en sus cuadros; en el primer cuadro cada pieza tiene `opacity` 1, `visibility` visible y su rectángulo dentro del rectángulo del `svg`; h1, `.hero-sub` y `a[data-cta="hero"]` tienen `animation-name: none` y opacidad 1; el muestreo de `getAnimations()` justo tras cargar (sondear hasta 3 s hasta ver al menos una; ese `describe` lleva `retries: 1` porque depende del reloj) contiene solo `CSSAnimation` cuyo destino está en `#inicio [data-piece]` y a los 2,5 s vuelve a 0; en `/marca/hoja/` `getAnimations()` es 0 con `no-preference`; sin scroll horizontal a 320 px durante la entrada; con `javaScriptEnabled: false` el hero se ve. Confirma que fallan antes del CSS.

    Movimiento (verde). Crea `src/styles/motion.css` con un único bloque `@media (prefers-reduced-motion: no-preference)` que define sobre `#inicio` las propiedades `--hc-dur: 600ms`, `--hc-step: 80ms` y `--hc-end: calc(var(--hc-step) * 5 + var(--hc-dur))`, el `@keyframes hc-enter` (de `scale(0.94) rotate(var(--r-from))` a `scale(1) rotate(var(--r))`) y la regla `#inicio [data-piece]` con `animation: hc-enter var(--hc-dur) var(--ease-out) backwards`, `animation-delay: calc(var(--i, 0) * var(--hc-step))`, `transform-box: fill-box` y `transform-origin: center`. Solo `transform`; nada de `opacity`, `visibility` ni `display`; nada sobre el h1, el subtítulo o el CTA; sin JavaScript. Todo el movimiento nuevo de la fase vive en este archivo. Impórtalo desde `src/styles/global.css` con un `@import './motion.css'` justo después del de `tokens.css` (los `@import` van antes del resto de reglas).

    Ciclo de ejecución de las reglas heredadas: build, preview en el puerto 4322, specs, `npx astro preview stop`.
  </action>
  <acceptance_criteria>
    - `tests/e2e/lib/motion.ts` existe y los cinco specs editados usan `expectNoMotion` con `reduce` y `expectMotionWithinBudget` con `no-preference` en `/`; ninguno afirma ya cero animaciones con `no-preference` en `/` (y siguen pasando con la entrada activa).
    - Con `reduce`: las 6 piezas de `#inicio [data-piece]` tienen `animation-name: none` y `getAnimations().length` vale 0 a la carga y a los 2,5 s, en `/` y en `/privacidad/`.
    - Con `no-preference`: las 6 piezas tienen `animation-name: hc-enter`, duración 0.6s, una iteración, relleno `backwards`, retraso de 0 a 0.4s en pasos de 0.08s, y el fin máximo es 1.0s (menor a 1.2s); `@keyframes hc-enter` declara solo `transform`.
    - En el primer cuadro cada pieza tiene `opacity: 1` y `visibility: visible` y queda dentro del `svg`; h1, subtítulo y CTA del hero tienen `animation-name: none` y opacidad 1; a los 2,5 s `getAnimations()` vale 0.
    - `/marca/hoja/` tiene 0 animaciones con ambas preferencias (el alcance es `#inicio`).
    - Sin scroll horizontal a 320 px durante la entrada; con JavaScript desactivado el hero se ve; `dist` sigue sin archivos `.js`.
    - HTML de `/` de menos de 61440 bytes y el SVG del hero de 8192 bytes o menos (el tope de 02-02).
    - `git diff --name-only BASE` está contenido en `files_modified` de este plan y no toca `landing.es.yaml`.
  </acceptance_criteria>
  <verify>
    <automated>npx astro build && npx playwright test --project=chromium tests/e2e/motion.spec.ts tests/e2e/page-structure.spec.ts tests/e2e/sections-problem-solution.spec.ts tests/e2e/results-cases.spec.ts tests/e2e/team-includes-how.spec.ts tests/e2e/closing-sections.spec.ts</automated>
    <fails_when>salida distinta de 0; una prueba nueva queda en `skip`; algún spec sigue afirmando cero animaciones con `no-preference` en `/` (falla por las animaciones del hero); o `getAnimations()` no vuelve a 0 a los 2,5 s</fails_when>
    <automated>grep -l "hc-enter" dist/_astro/*.css && node -e "const fs=require('fs');const h=fs.readFileSync('dist/index.html','utf8');const m=h.match(/<svg[^>]*(?:class=\"hero-collage\"|data-collage=\"hero\")[\s\S]*?<\/svg>/);const js=fs.readdirSync('dist',{recursive:true}).filter(f=>String(f).endsWith('.js')).length;process.exit(m&&Buffer.byteLength(h)<61440&&Buffer.byteLength(m[0])<=8192&&js===0?0:1)"</automated>
    <fails_when>el CSS construido no contiene `hc-enter` (el `@import` no llegó al build), el HTML pesa 61440 bytes o más, el SVG del hero pasa de 8192 bytes o `dist` tiene archivos `.js`</fails_when>
  </verify>
  <done>La entrada del collage corre solo con `no-preference`, en 1,0 s, con `transform` y sin esconder nada; con `reduce` no hay ninguna animación; la política de movimiento de la suite está en un solo helper y toda la suite que usa `/` sigue en verde; el tracer está commiteado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pupilas, giro del icono del FAQ y guarda estática del presupuesto de movimiento</name>
  <files>src/styles/motion.css, src/components/sections/Faq.astro, tests/e2e/motion.spec.ts, tests/guards/motion-budget.test.mjs</files>
  <precondition>La tarea 1 está commiteada (`motion.css` con `hc-enter` y el `@import` en `global.css`) y existe `src/components/sections/Faq.astro` con `.faq-item`, `.faq-summary` y `.faq-icon-v` (02-06); si falta alguno, detener la tarea y reportarlo.</precondition>
  <read_first>
    - .planning/phases/02-secciones-marca-y-copy/02-UI-SPEC.md: las filas de pupilas y de giro del icono del presupuesto de movimiento, y la fila del `<summary>` de FAQ en `### Estados` de `## Interaction Contract`
    - src/styles/motion.css y tests/e2e/lib/motion.ts (tarea 1)
    - src/components/sections/Faq.astro (el marcado del `svg.faq-icon` y la regla que hoy oculta `.faq-icon-v` cuando el `details` está abierto), src/components/sections/Hero.astro (posición del CTA) y src/components/collage/HeroCollage.astro (las pupilas)
  </read_first>
  <behavior>
    - Pupilas: `hc-look`, 400 ms, una iteración, relleno `backwards`, retraso igual al fin de la entrada (1000 ms); a los 2,5 s ninguna animación sigue viva.
    - Dirección: con `--look-dx` y `--look-dy` leídos de la pupila, el producto punto con el vector del centro de la pupila al centro de `a[data-cta="hero"]` es mayor que 0 a 390 y a 1280 px.
    - Icono del FAQ cerrado: `.faq-icon-v` más alto que ancho; abierto (clic en el primer resumen y 250 ms después): más ancho que alto, con relación de 2 o más.
    - Con `no-preference`, tras el clic `getAnimations()` contiene un `CSSTransition` sobre `.faq-icon-v` con `transitionProperty` `transform` y duración de 150 ms; con `reduce`, `transition-duration` computado `0s` y `getAnimations()` vacío justo tras el clic; Enter y Espacio alternan `open`; sin JavaScript el clic alterna `open`.
    - Guarda estática: el repositorio real cumple las cuatro reglas y cada regla falla con una cadena sintética (mutación).
  </behavior>
  <action>
    Implementa DSGN-05 (pupilas y giro del icono) y su guarda estática, con pruebas primero.

    Pruebas primero (rojo). En `tests/e2e/motion.spec.ts` agrega los bloques 'pupilas del hero' e 'icono del FAQ' con las expectativas de `<behavior>`; las de tiempo y estilo leen `getComputedStyle` y `getComputedTiming` (deterministas) y las que muestrean `getAnimations()` van en un `describe` con `retries: 1`. Crea `tests/guards/motion-budget.test.mjs` (Node test runner, sin dependencias) con un lector mínimo de CSS: quita comentarios, extrae los bloques `<style>` de los `.astro` y los `.css` de `src/`, recorre llaves con una pila de preámbulos (at-rules y selectores) y entrega cada declaración con la lista de `@media` que la encierran y su selector. Reglas: (1) toda declaración cuya propiedad sea `animation`, `transition` o empiece con `animation-` o `transition-` tiene `(prefers-reduced-motion: no-preference)` entre sus `@media`; (2) todo `@keyframes` declara solo `transform` en sus cuadros; (3) ningún valor lleva `infinite` y ninguna propiedad es `animation-timeline`; (4) toda declaración de la familia `animation` (no de `transition`) tiene un selector que empieza con `#inicio` y apunta a `[data-piece]`, `.hc-pupil` o `[data-pupil]`, de modo que el h1, el subtítulo y el CTA no tengan animación de cuadros clave (el hover del CTA de la fase 1 es una transición y sigue permitido). Exporta `scanCss(files)` y prueba cada regla por mutación con cadenas sintéticas (una animación fuera del bloque, un `@keyframes` con `opacity`, `infinite`, `animation-timeline`, una animación sobre `h1`), más la corrida sobre el repositorio real, que debe dar cero violaciones. Confirma que las pruebas de navegador fallan antes del CSS.

    Movimiento (verde). Si esta tarea corre en un ejecutor distinto del de la tarea 1, vuelve a invocar con la herramienta Skill `impeccable` (verbo `animate`, solo pupilas y giro del icono) y `design-taste-frontend` (MOTION_INTENSITY 3). En `motion.css`, dentro del mismo `@media (prefers-reduced-motion: no-preference)`: (a) `--look: 4px` y las direcciones `--look-dx` y `--look-dy` sobre `#inicio`, con un segundo par desde `min-width: 64em` (a partir de 64 em el collage va a la derecha del texto y bajo 64 em va debajo del CTA); el ejecutor mide las posiciones de las pupilas y del CTA a 390 y a 1280 px y elige los signos (-1, 0 o 1) que hagan positivo el producto punto; (b) `@keyframes hc-look` con un solo cuadro `from` que traslada la pupila en sentido contrario al CTA (`calc(var(--look-dx) * -1 * var(--look))` y lo análogo en vertical) para que termine en su posición estática, y la regla `#inicio :is(.hc-pupil, [data-pupil])` con `animation: hc-look 400ms var(--ease-out) backwards` y `animation-delay: var(--hc-end)` (per la desviación 3); (c) `.faq-icon-v` con `transition: transform var(--dur-1) var(--ease-out)`. En `Faq.astro` deja en la regla base de `.faq-icon-v` (todas las preferencias) `transform-box: fill-box` y `transform-origin: center`, y reemplaza la regla que oculta la barra cuando el `details` está abierto por `transform: rotate(90deg)`; si la barra vertical y la horizontal no miden igual, iguala su largo y grosor para que la rotada cubra a la horizontal. No agregues ninguna transición en `Faq.astro`: vive solo en `motion.css` (per la desviación 5 y CONT-11). Sin JavaScript, sin `opacity`, sin cambiar el marcado nativo de `<details>`.

    Ciclo de ejecución de las reglas heredadas: build, preview en el puerto 4322, specs, `npx astro preview stop`.
  </action>
  <acceptance_criteria>
    - Los bloques 'pupilas del hero' e 'icono del FAQ' de `motion.spec.ts` fallaron antes del CSS (salida anotada en el SUMMARY) y ahora pasan.
    - Pupilas: `animation-name: hc-look`, duración 0.4s, una iteración, relleno `backwards`, retraso 1s (igual a `--hc-end`), fin a 1.4s; a los 2,5 s `getAnimations()` vale 0 con ambas preferencias; con `reduce` `animation-name` es `none`.
    - Producto punto de `(--look-dx, --look-dy)` con el vector pupila a CTA mayor que 0 a 390 y a 1280 px.
    - Icono del FAQ: cerrado, `.faq-icon-v` más alto que ancho; abierto, relación ancho a alto de 2 o más; con `no-preference` existe un `CSSTransition` de `transform` de 150 ms tras el clic; con `reduce` no existe ninguno y `transition-duration` es `0s`.
    - Enter y Espacio alternan `open` en un resumen enfocado y el clic también con JavaScript desactivado; los bloques del FAQ de `closing-sections.spec.ts` siguen en verde.
    - `node --test tests/guards/motion-budget.test.mjs` pasa con al menos 5 pruebas de mutación y la corrida del repositorio real con cero violaciones.
    - Los cuatro `@keyframes` o transiciones nuevos declaran solo `transform`; `dist` sigue sin `.js`.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/guards/motion-budget.test.mjs && npx astro build && npx playwright test --project=chromium tests/e2e/motion.spec.ts tests/e2e/closing-sections.spec.ts tests/e2e/page-structure.spec.ts</automated>
    <fails_when>salida distinta de 0, el resumen de `node --test` muestra "# fail" mayor que 0, una prueba de mutación no falla con su cadena sintética (la regla no muerde), o una prueba de navegador queda en `skip`</fails_when>
  </verify>
  <done>Las pupilas hacen su único desplazamiento tras la entrada, el icono del FAQ gira 150 ms solo con `no-preference`, el presupuesto de movimiento está protegido por una guarda estática que muerde, y la suite sigue en verde; commit hecho.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Regla VERIFICAR ampliada (TDD), limpieza de aserciones de la fase 1 y conexiones de fase</name>
  <files>scripts/lib/copy-rules.mjs, tests/guards/copy.test.mjs, tests/guards/fixtures/verificar-nota.yaml, tests/guards/phase2-static.test.mjs, tests/e2e/a11y-base.spec.ts, tests/e2e/page-structure.spec.ts, tests/e2e/phase-closing.spec.ts, src/layouts/BaseLayout.astro, src/components/SiteHeader.astro, src/components/brand/Logo.astro, src/content/landing.es.yaml, PENDING-COPY.md</files>
  <!-- planner-discipline-allow: .wordmark -->
  <precondition>Las tareas 1 y 2 están commiteadas y existen `tests/e2e/page-structure.spec.ts` con `PAGE_ORDER`, `src/components/brand/Logo.astro`, `public/favicon.svg` y `public/favicon.ico` (02-02) y el footer de 02-06; si falta alguno, detener la tarea y reportarlo.</precondition>
  <read_first>
    - .planning/phases/02-secciones-marca-y-copy/02-PLANNING-BRIEF.md
    - .planning/phases/02-secciones-marca-y-copy/02-ARI-COPY-V2.md: solo las líneas 104 y 115 (las dos marcas reales) y 199 a 205 (notas finales del doc)
    - scripts/lib/copy-rules.mjs (líneas 60 a 110: `VERIFICAR_RE` y `matchesOf`; y 185 a 261: `checkCopy`), tests/guards/copy.test.mjs (líneas 1 a 64: helpers `run`, `docWith` y `claimYaml`; la prueba 3 y la 9b tal como quedaron tras 02-03 a 02-06) y tests/guards/fixtures/verificar.yaml
    - tests/e2e/a11y-base.spec.ts (líneas 1 a 30, la prueba '(h)' de pesos y el bloque 'textos de Ari visibles') y tests/e2e/page-structure.spec.ts (`PAGE_ORDER`, la lectura del YAML y la prueba de orden)
    - src/layouts/BaseLayout.astro, src/components/SiteHeader.astro y src/components/brand/Logo.astro (estado tras 02-02 y 02-06) y `git grep -n "\.wordmark" -- src tests`
    - .planning/phases/02-secciones-marca-y-copy/02-02-PLAN.md `## Decisiones de criterio` (decisiones 3 y 8) y 02-UI-SPEC.md `## Verification Hooks`
  </read_first>
  <behavior>
    - 3b: cada una de estas cadenas en `text` da exactamente una violación VERIFICAR: la marca sin nota; la nota real del Pilar 4 del doc (con paréntesis dentro: `[VERIFICAR: solo desglosa por canal (Google vs IA) si de verdad puedes atribuirlo con datos. Si no, repórtalo junto y no inventes el split.]`); `[VERIFICAR rango]`; la marca en minúsculas con nota; una nota que salta de línea; una marca sin corchete de cierre.
    - 3c: no marcan: 'Podemos verificar cada cifra', `[VERIFICADO]`, la palabra sin corchete, y una afirmación cuyo `reason` trae `[VERIFICAR: nota]` sobre un `text` limpio.
    - 3d: dos marcas en un mismo texto dan dos violaciones.
    - 3e (CLI): el fixture nuevo con `[VERIFICAR: ...]` sale 1 en producción con la regla VERIFICAR y sale 0 con WARN fuera de ella.
    - 9b terminada: producción reporta solo PENDING y MISSING con `structural` vacío, las rutas esperadas se derivan del YAML con `walkClaims` y toda afirmación con el relleno es `pending`.
    - Estructura: `main > section` son exactamente las 12 de `PAGE_ORDER` en orden y hay 11 `h2` con textos del YAML y un solo `h1` dentro de `#inicio`.
    - Conexiones: dos enlaces de favicon por página y 200 en cada archivo; un sprite por página con todos sus `use` resueltos; 1 collage del hero, 1 de `#agenda` y 4 avatares; 4 CTA iguales.
    - Higiene: el repositorio real cumple las reglas de `phase2-static.test.mjs` y cada regla falla con una cadena sintética.
  </behavior>
  <action>
    Implementa COPY-01 (guarda, limpieza y auditoría de aserciones) y DSGN-03 y DSGN-04 (conexiones y guardas de fase). No cambia diseño: no requiere invocar las skills, salvo que retirar la clase heredada altere el aspecto del header (no debe). Antes de editar anota `git rev-parse HEAD` como BASE.

    Guarda VERIFICAR, primero rojo (per COPY-01 y el hallazgo 4 de 02-03). En `tests/guards/copy.test.mjs`, tras la prueba 3, agrega las pruebas 3b a 3e de `<behavior>`: 3b a 3d con `checkCopy(parse(docWith(claimYaml(texto))))` y las cadenas literales del doc de Ari (copiadas de las líneas 104 y 115), y 3e con el fixture nuevo `tests/guards/fixtures/verificar-nota.yaml` (misma forma que `verificar.yaml` pero con una nota `[VERIFICAR: ...]`) y el helper `run`. Confirma que fallan. Luego, en `scripts/lib/copy-rules.mjs`, cambia `VERIFICAR_RE` por una expresión con banderas `g` e `i` que reconozca `[VERIFICAR` seguido de cualquier carácter que no sea `]` (incluidos los saltos de línea) y un `]` opcional (per la desviación 11): lineal, sin cuantificadores anidados; no toques `matchesOf` ni las otras reglas y actualiza el comentario de cabecera solo si describe la forma antigua. Comprueba el YAML real con `(PUBLIC_ENV=production node scripts/check-copy.mjs --json || true)`: `structural` vacío y `content` con solo PENDING y MISSING. Si aparece VERIFICAR en el `text` de alguna ruta, es una marca del doc que una guarda anterior no cubrió: conviértela al patrón de 02-03 (`text: FALTA CONFIRMAR`, `status: pending`, `confirm_by: Ari`, `reason` con el texto literal del doc), corre `npm run pending` y anótalo en el SUMMARY; no recortes la nota para publicar el resto.

    Prueba 9b. Léela tal como quedó tras 02-03 a 02-06 y termínala solo si falta algo: (i) producción reporta solo PENDING y MISSING (ni VERIFICAR, AEO, VOSEO, DASH ni INVERSION) con `structural` vacío; (ii) las rutas esperadas se derivan del YAML con `walkClaims` (las `pending` más las que contienen el relleno); (iii) toda afirmación cuyo texto es el relleno tiene `status: pending`; (iv) el archivo no se modifica (`sha`). Si ya cumple, no la toques.

    Aserciones de la fase 1 en `a11y-base.spec.ts` (per COPY-01 y la decisión 10 de Supuestos). En la prueba '(h)' quita la clase heredada del texto de marca de la lista de pesos y de su título (quedan skip 600; CTA, h1 y h2 700; subtítulo y body 400) y retira esa clase del contenedor del logo y su regla CSS donde 02-02 la dejó (`SiteHeader.astro` o `Logo.astro`); `.brand-logo` se queda. En 'textos de Ari visibles', si aún exige que el cuerpo no contenga el relleno, reemplázalo por: el número esperado sale del YAML (afirmaciones cuyo texto normalizado es el relleno, sin las de `privacy.`, que viven en `/privacidad/`) y el cuerpo de `/` lo contiene al menos esa cantidad de veces, y ninguna si el esperado es 0; conserva los asertos de llaves y de 'borrador|TBD|lorem'. Si 02-06 ya lo reemplazó, verifica que derive del YAML y déjalo.

    Estructura de la página (per DSGN-03; 02-01 lo dejó para este plan). En `page-structure.spec.ts` endurece la prueba de orden: los ids de `main > section` son exactamente los 12 de `PAGE_ORDER` en ese orden; hay 11 `h2` cuyos textos, en orden, vienen del YAML con una constante `H2_SOURCE` de id de sección a ruta de su título (`problem.title`, `why_now.title`, `solution.title`, `results.title`, `cases.title`, `team.title`, `includes.title`, `how_it_works.title`, `for_whom.title`, `faq.title`, `agenda.title`; si alguna clave real difiere, usa la del YAML) resueltos con el mismo `resolveText` del spec; hay un solo `h1` y está dentro de `#inicio`. Conserva el aserto de que no hay saltos de nivel.

    Conexiones de fase (per DSGN-04; los pendientes que 02-02 dejó para este plan). En `BaseLayout.astro` agrega al head un `<link rel="icon" href="/favicon.svg" type="image/svg+xml">` y un `<link rel="icon" href="/favicon.ico" sizes="any">`, sin tocar nada más del layout (props `noindex` y `path`, footer). Crea `tests/e2e/phase-closing.spec.ts` (la tarea 4 lo extiende) con el bloque 'conexiones de fase' en `/` y `/privacidad/`: ambos enlaces de favicon están y `page.request.get` de cada `href` responde 200 con `content-type` de `image/svg+xml` y de icono y cuerpo no vacío; hay un solo `.collage-sprite` por página (el sprite se queda en `SiteHeader`, decisión 8 de Supuestos) y todo `use[href^="#"]` resuelve a un id del mismo documento; en `/` hay exactamente un `svg[data-collage="hero"]`, uno `[data-collage="agenda"]` dentro de `#agenda` y cuatro `[data-collage="avatar"]`; y hay 4 `a[data-cta]` con `href="#agenda"`, el texto de `cta.label_template` resuelto desde el YAML y sin `aria-label`. La comprobación del área de salvado `--logo-clear` va en la tarea 4, donde `polish` puede corregir un hueco real.

    Guarda estática de higiene. Crea `tests/guards/phase2-static.test.mjs` (Node test runner) que lee los `.astro` de `src/components`, `src/pages` y `src/layouts` y los `.css` y `.astro` de `src`, con `scan(files)` exportada y probada por mutación con cadenas sintéticas: (1) sin inyección de HTML en bruto (`set:html`, `innerHTML`, `dangerouslySetInnerHTML`) salvo las rutas listadas en `RAW_HTML_ALLOW`, que hoy es vacía; (2) sin literales hex de color (`#` más 3, 4, 6 u 8 dígitos hexadecimales) en los `.astro` de componentes, páginas y layouts, ignorando los que siguen a `href=`, `url(` o `&` y sin escanear `.mjs` (las constantes de `collage-rules.mjs`) ni `src/assets`; (3) sin `outline` en `none` o `0` ni `outline-style: none`; (4) sin `line-height` con `!important`; (5) los únicos `<script` de `src` son los dos de `AgendaSection.astro` y `src/scripts` contiene solo `cta-focus.ts`. Si el repositorio real viola una regla, corrige el código (un color hex se cambia por su `var(--color-brand-*)`); solo se agrega una excepción con razón escrita si el caso es legítimo.

    Ciclo de ejecución de las reglas heredadas: guardas, build, preview en el puerto 4322, specs, `npx astro preview stop`.
  </action>
  <acceptance_criteria>
    - Las pruebas 3b a 3e existen, fallaron antes del cambio de `VERIFICAR_RE` (salida anotada en el SUMMARY) y pasan; `node --test tests/guards/*.test.mjs` sale 0 con las 66 pruebas de la fase 1 más las nuevas.
    - Sobre el YAML real en producción: `structural` vacío, `content` solo con PENDING y MISSING y cero VERIFICAR; la 9b cumple los cuatro puntos de su párrafo.
    - `git grep -n -F ".wordmark" -- src tests` no devuelve resultados; el aserto de 'textos de Ari visibles' deriva del YAML el número de ranuras con el relleno y pasa con JavaScript activado y desactivado.
    - `page-structure.spec.ts` afirma los 12 ids exactos en orden, 11 `h2` con texto del YAML y un `h1` dentro de `#inicio`.
    - Bloque 'conexiones de fase': 2 enlaces de favicon por página con 200 y tipos correctos; 1 sprite por página con todos los `use` resueltos; collages 1, 1 y 4; 4 CTA con el mismo nombre accesible que su texto visible y `href="#agenda"`.
    - `phase2-static.test.mjs` pasa con al menos 5 pruebas de mutación y la corrida real con cero violaciones.
    - `git diff --name-only BASE` está contenido en `files_modified`; `landing.es.yaml` cambia solo si el paso de VERIFICAR lo exigió y, entonces, con el patrón de 02-03.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/guards/*.test.mjs && npm run pending && node scripts/list-pending.mjs --check && npx astro build && npx playwright test --project=chromium tests/e2e/a11y-base.spec.ts tests/e2e/page-structure.spec.ts tests/e2e/phase-closing.spec.ts</automated>
    <fails_when>salida distinta de 0, el resumen de `node --test` muestra "# fail" mayor que 0, `--check` sale distinto de 0, o una prueba de navegador queda en `skip`</fails_when>
    <automated>(PUBLIC_ENV=production node scripts/check-copy.mjs --json || true) | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s);const rules=new Set(r.content.map(v=>v.rule));const ok=r.structural.length===0&&rules.size>0&&[...rules].every(x=>x==='PENDING'||x==='MISSING');process.exit(ok?0:1)})"</automated>
    <fails_when>`structural` no está vacío, o `content` trae una regla distinta de PENDING y MISSING (VERIFICAR, AEO, VOSEO, DASH o INVERSION en un `text`)</fails_when>
    <automated>git grep -n -F ".wordmark" -- src tests; test $? -eq 1</automated>
    <fails_when>`git grep` encuentra la clase heredada en `src` o en `tests`</fails_when>
  </verify>
  <done>La regla VERIFICAR reconoce las formas reales del doc de Ari y está probada por mutación; ninguna aserción de la fase 1 contradice el diseño nuevo; el favicon, el sprite, los collages, los CTA y la estructura de 12 secciones y 11 h2 están afirmados; la higiene de fase la vigila una guarda estática; commit hecho.</done>
</task>

<task type="auto">
  <name>Task 4: Iteración visual de cierre: matriz completa, axe de humo, critique, polish y hojas de contacto de la página entera</name>
  <files>tests/e2e/phase-closing.spec.ts, package.json, package-lock.json, .planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md, y los archivos ya existentes de src/components, src/styles y src/layouts que `polish` toque (se listan en el SUMMARY)</files>
  <precondition>Las tareas 1 a 3 están commiteadas y `npx astro build` pasa; `node ~/.claude/gsd-core/bin/gsd-tools.cjs` responde; si falta algo, detener la tarea y reportarlo.</precondition>
  <read_first>
    - .planning/phases/02-secciones-marca-y-copy/02-UI-SPEC.md: `## Design Method and Iteration Protocol` (verbos por lote, fila Cierre, ciclo, lista de vibra), `## Reference Synthesis` (las 15 filas), `## Verification Hooks` y `## UI Considerations`; ubicar con `grep -n '^## \|^### '` y leer con `offset` y `limit`
    - tests/e2e/page-structure.spec.ts (herramienta de capturas `PHASE2_BATCH` y `PAGE_ORDER`), tests/e2e/phase-closing.spec.ts (tarea 3) y el bloque `recorte` de tests/e2e/closing-sections.spec.ts (patrón de recortes por sección)
    - .planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md (entradas de los lotes A a E) y 02-02-SUMMARY.md (la entrada del lote 0: `grep -n -i "lote 0"`)
    - PRODUCT.md y DESIGN.md (raíz), package.json (`devDependencies`) y .planning/research/STACK.md (líneas de `@axe-core/playwright`)
  </read_first>
  <action>
    Implementa DSGN-03 y DSGN-04 en su cierre: medir la página completa, mirarla con las skills, corregir en un solo lote y confirmar. Antes de editar anota `git rev-parse HEAD` como BASE.

    Dependencia de prueba (per T-02-07-SC). Corre `node ~/.claude/gsd-core/bin/gsd-tools.cjs query package-legitimacy check --ecosystem npm @axe-core/playwright`, sin sufijo de versión (con `@4.13.0` pegado el verificador responde `does-not-exist`). El veredicto debe ser OK (el 2026-09-19: existe, unos 7,3 millones de descargas semanales, repositorio de dequelabs, sin postinstall); si no lo es, detén la tarea y repórtalo. Si lo es, instala solo `@axe-core/playwright` con versión exacta 4.13.0 como dependencia de desarrollo y ninguna otra.

    Medición primero (per DSGN-03). Extiende `tests/e2e/phase-closing.spec.ts` con estos bloques, todos con expectativas derivadas del YAML o de las variables computadas, no de listas escritas a mano: (1) 'matriz de desborde': `/` y `/privacidad/` a 320, 390, 640, 768, 1024 y 1280 px con `reduce` y con `no-preference`; el ancho de desplazamiento del documento es menor o igual que su ancho de cliente y ningún rectángulo de `header`, `main > section` o `footer` cae fuera de `[0, innerWidth]`; (2) 'espaciado de texto': a 320 y 1280 px inyecta `line-height` 1.5, párrafos a 2 veces el cuerpo, `letter-spacing` 0.12em y `word-spacing` 0.16em (SC 1.4.12), sin scroll horizontal y con cero elementos con `overflow` oculto o recortado cuyo alto de contenido supere el alto de su caja en más de 1 px y que contengan texto; (3) 'sin JavaScript': las 12 secciones tienen alto mayor que 0 y son visibles, un `<details>` abre con clic y hay 4 CTA; (4) 'ClickUp bloqueado': con `context.route` abortando todo lo que no sea localhost, las 12 secciones, el footer y el enlace de respaldo de `#agenda` se ven y no hay `pageerror`; (5) 'axe de humo': `AxeBuilder` con las etiquetas wcag2a, wcag2aa, wcag21a, wcag21aa y wcag22aa y `exclude('iframe')` sobre `/` y `/privacidad/` a 320, 390 y 1280 px con ClickUp bloqueado: cero violaciones `critical` o `serious`, y las `moderate` y `minor` (`id` y nodos) se adjuntan como anotaciones para el log; (6) 'objetivos, sombras y área de salvado': en `/` a 390 y 1280 px cada `a`, `summary` y `button` visible (salvo skip links fuera de pantalla y el iframe) mide 44 px o más de alto; ninguna sombra computada de `/` ni de `/privacidad/` tiene desenfoque y ningún `background-image` computado contiene un degradado; y por cada `.brand-logo` (header y footer, a 1280 y 390 px) resuelve `--logo-clear` con un elemento de prueba de `width: var(--logo-clear)` y comprueba que ningún elemento de contenido (`a`, `p`, `h1` a `h6`, `li`, `button`, `summary`, `svg` no decorativo) distinto del propio logo y de sus ancestros toca el rectángulo del logo ampliado por esa medida en los cuatro lados; (7) 'peso y peticiones': HTML de `/` de menos de 61440 bytes, suma de `outerHTML` de todos los `svg[data-collage]` de menos de 40960 bytes, `dist` sin archivos `.js`, el script en línea de `/` de menos de 3072 bytes, y los hosts pedidos por `/` y `/privacidad/` (evento `request`) contenidos en localhost, forms.clickup.com y app-cdn.clickup.com; (8) 'hoja de contacto' (solo con `PHASE2_BATCH`, `test.skip` si no): recortes de `header`, de cada sección de `PAGE_ORDER` y de `footer` a 1280 y 390 px en `test-results/phase2/<lote>-<id>-<ancho>.png`, y dos hojas, `<lote>-hoja-1280.png` (3 columnas) y `<lote>-hoja-390.png` (6 columnas), que se componen abriendo una página en blanco con una rejilla CSS de `<img>` con datos en base64 rotuladas con su id y capturándola. Corre los bloques: lo que falle es un defecto real del layout y se corrige en el ciclo de abajo, no se afloja la prueba.

    Ciclo de cierre (per la fila Cierre del UI-SPEC; una ronda de captura es un pase en lote). (1) Ronda `CIERRE1`: corre la herramienta de 02-01 (`PHASE2_BATCH=CIERRE1 npx playwright test --project=chromium tests/e2e/page-structure.spec.ts -g "captura"`: cinco anchos por base, `reduce` y sin JavaScript, con ClickUp bloqueado) y el bloque 'hoja de contacto'. Mira las dos hojas de contacto y, de las capturas de página completa, solo `CIERRE1-390-nojs.png` y `CIERRE1-320-reduce.png`; abre como máximo 4 recortes individuales donde un rasgo falle. (2) Con la herramienta Skill invoca `impeccable` (carga `reference/craft-floor.md`, corre `context`, lee `PRODUCT.md` y `DESIGN.md`) y corre `critique` de página completa contra las 15 filas de 'Reference Synthesis', los 8 rasgos de vibra y los contratos de sección; luego `audit` (accesibilidad y rendimiento de la página entera, sin Lighthouse, que es de la fase 3). Invoca `design-taste-frontend` con la lectura declarada de la tarea 1 y los diales 7, 3 y 4, y úsala como lista final de antidefectos (sin degradados, sin vidrio, sin tres tarjetas iguales seguidas sin variar, sin animaciones infinitas). (3) Aplica todas las correcciones en un solo lote con `polish`, `harden` (texto largo, relleno 'FALTA CONFIRMAR', `<details>`) y `adapt` (320 a 1280 px, zoom 200 %, espaciado de texto). Reglas del lote: solo archivos que ya existen en `src/components`, `src/styles` y `src/layouts`; ningún cambio de texto (COPY-01), del bloque del iframe, de tokens de color sin par aprobado en `node scripts/check-contrast.mjs`, ni JavaScript o dependencias nuevas; cada edición se anota en el log con archivo, motivo y medida. (4) Reconstruye, repite los bloques de medición y corre la ronda de confirmación `CIERRE2` (una como máximo por ciclo). Si un rasgo de vibra sigue fallando, repite el ciclo con `CIERRE3`; tras 3 ciclos de página completa sin cumplir la lista, detente y muestra a Juan con capturas (regla del UI-SPEC).

    Registro (per DSGN-04). En `02-VISUAL-LOG.md`: (i) primero, la entrada 'Lote 0' que 02-02 dejó en su SUMMARY, copiada con la nota 'consolidada desde 02-02-SUMMARY.md' (decisión 9 de 02-02); (ii) por cada ronda `CIERRE`, los verbos usados, los hallazgos, las correcciones y las capturas; (iii) una tabla de los 8 rasgos de vibra con veredicto y evidencia numérica (primer pantallazo a 1280x800 de `page-structure.spec.ts`, padding de sección de 96 px desde 1024 px, bordes de 3 px y cero sombras difusas, avatares del mismo tamaño, cuatro CTA iguales) o, para los visuales, la captura y la fila de síntesis que los respalda; (iv) una tabla de las 15 filas de la síntesis con sí o no y dónde se ve; (v) los conteos de axe por impacto y las anotaciones `moderate` y `minor`; (vi) los pendientes para Juan: mirada real al movimiento con `reduce` y `no-preference`, VoiceOver sobre `<details>` en Safari, aprobación del set de avatares, del favicon (mesa 13 o 18) y de los colores del logo.

    Ciclo de ejecución de las reglas heredadas: build tras cada cambio, preview en el puerto 4322, specs, `npx astro preview stop` al final.
  </action>
  <acceptance_criteria>
    - `package.json` lista `@axe-core/playwright` en `devDependencies` con versión exacta `4.13.0` y `git diff BASE -- package.json` no muestra ninguna otra dependencia nueva; el veredicto OK de legitimidad está anotado en el SUMMARY.
    - Matriz: `/` y `/privacidad/` sin scroll horizontal y sin rectángulos fuera de `[0, innerWidth]` a 320, 390, 640, 768, 1024 y 1280 px con ambas preferencias; con espaciado forzado a 320 y 1280 px hay cero elementos con texto recortado.
    - Sin JavaScript: 12 secciones visibles y el FAQ abre; con ClickUp bloqueado: 12 secciones, footer y enlace de respaldo visibles y cero `pageerror`.
    - axe: cero violaciones `critical` o `serious` en `/` y `/privacidad/` a 320, 390 y 1280 px; las demás quedan listadas en el log.
    - Cero elementos interactivos visibles de menos de 44 px de alto en `/` a 390 y 1280 px; cero sombras con desenfoque y cero degradados computados; cero intrusiones en el área de salvado del logo del header y del footer.
    - `/` pesa menos de 61440 bytes, los collages suman menos de 40960 bytes, `dist` no tiene `.js` y los únicos hosts pedidos son localhost, forms.clickup.com y app-cdn.clickup.com.
    - `02-VISUAL-LOG.md` tiene el lote 0 consolidado, las rondas `CIERRE1` y `CIERRE2` (y `CIERRE3` si hizo falta) con verbos, hallazgos, correcciones y capturas, la tabla de los 8 rasgos con veredicto de cumple en todos, la tabla de las 15 filas, el conteo de axe y la lista de pendientes para Juan; el log deja constancia de haber invocado `impeccable` y `design-taste-frontend` con la herramienta Skill.
    - `git diff BASE -- src/content/landing.es.yaml` queda vacío y las pruebas de la fase 1 del iframe siguen en verde; `node scripts/check-contrast.mjs` sale 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npx playwright test --project=chromium tests/e2e/phase-closing.spec.ts tests/e2e/motion.spec.ts tests/e2e/page-structure.spec.ts tests/e2e/a11y-base.spec.ts && node --test tests/guards/*.test.mjs && node scripts/check-contrast.mjs</automated>
    <fails_when>salida distinta de 0, una prueba de navegador queda en `skip` (salvo el bloque 'hoja de contacto' sin `PHASE2_BATCH`), axe reporta una violación `critical` o `serious`, o el resumen de `node --test` muestra "# fail" mayor que 0</fails_when>
    <automated>node -e "const p=require('./package.json');const l=require('fs').readFileSync('.planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md','utf8');const ok=p.devDependencies['@axe-core/playwright']==='4.13.0'&&/CIERRE1/.test(l)&&/CIERRE2/.test(l)&&/Lote 0/i.test(l);process.exit(ok?0:1)"</automated>
    <fails_when>la dependencia no está fijada en 4.13.0, o el log no tiene el lote 0 consolidado y las rondas `CIERRE1` y `CIERRE2`</fails_when>
    <human-check>Con `workflow.human_verify_mode=end-of-phase`: Juan abre la página en un navegador real a 390 y 1280 px, alterna la preferencia de movimiento reducido del sistema y confirma que la entrada del hero es sutil y que no falta contenido; además comprueba con VoiceOver en Safari que cada resumen del FAQ anuncia su estado; el resultado se anota en `02-VISUAL-LOG.md`.</human-check>
  </verify>
  <done>La página entera cumple la matriz de anchos, la prueba de humo de axe y los 8 rasgos de vibra con evidencia; el log consolidado y las hojas de contacto documentan cada ronda; ningún texto, formulario ni token de color cambió; commit hecho y verificaciones humanas anotadas para Juan.</done>
</task>

<task type="auto">
  <name>Task 5: Auditoría de fase, hallazgos consolidados para Ari y suite completa</name>
  <files>.planning/phases/02-secciones-marca-y-copy/02-ARI-FINDINGS.md, PENDING-COPY.md</files>
  <precondition>Las tareas 1 a 4 están commiteadas y existen `02-01-SUMMARY.md` a `02-06-SUMMARY.md`; si falta alguno, la fuente de sus hallazgos es el texto de su plan y se marca en el archivo de hallazgos con la nota 'fuente: plan, sin SUMMARY'.</precondition>
  <read_first>
    - .planning/phases/02-secciones-marca-y-copy/02-PLANNING-BRIEF.md (regla de copy y lecciones de la fase 1)
    - .planning/phases/02-secciones-marca-y-copy/02-CONTEXT.md: `### Textos pending`, `### Morado de marca y logo` y `<specifics>` (pendientes de Ari)
    - .planning/phases/02-secciones-marca-y-copy/02-UI-SPEC.md: `### Ranuras que muestran "FALTA CONFIRMAR" hoy`, `### Ítems que se muestran tal cual pero se listan para que Ari los confirme` y `### Hallazgos para Ari`; ubicar con `grep -n '^## \|^### '` y leer con `offset` y `limit`
    - .planning/phases/02-secciones-marca-y-copy/02-01-SUMMARY.md a 02-06-SUMMARY.md: solo las secciones de erratas, hallazgos y desviaciones (`grep -n -i 'errata\|hallazgo\|desviaci\|Ari'` y `offset` y `limit`), nunca completos
    - PENDING-COPY.md (sin leerlo entero: `grep -c '^|'` y el conteo por primer segmento de ruta), scripts/list-pending.mjs (uso de `--check`) y `.planning/phases/02-secciones-marca-y-copy/02-VISUAL-LOG.md` (pendientes para Juan de la tarea 4)
  </read_first>
  <action>
    Implementa COPY-01 en su cierre (auditoría y lista única para Ari) y confirma la fase completa. Antes de editar anota `git rev-parse HEAD` como BASE.

    Lista de pendientes (per el brief, lección 2). Corre `npm run pending`, anota el hash de `PENDING-COPY.md`, córrelo otra vez y comprueba que el hash es idéntico (determinista), y corre `node scripts/list-pending.mjs --check` (debe salir 0). Si `PENDING-COPY.md` difiere del versionado porque algún plan lo dejó viejo, commitea el archivo regenerado.

    Producción (per COPY-02 y COPY-01). Con `PUBLIC_ENV=production` corre `node scripts/check-copy.mjs --json`: sale 1, `structural` está vacío y el conjunto de reglas de `content` es solo PENDING y MISSING; anota cuántas hay de cada una. Corre `npm run build` sin `PUBLIC_ENV` (sale 0) y `PUBLIC_ENV=production node scripts/check-copy.mjs --dist dist` (sale 1 y nombra `index.html` y `privacidad/index.html` por el relleno 'FALTA CONFIRMAR'). Corre `PUBLIC_ENV=production npm run build`: debe salir distinto de 0 en `prebuild` sin reconstruir `dist` (compara la fecha de modificación de `dist/index.html` antes y después). Conclusión que debe quedar escrita en el SUMMARY: el build de producción falla solo por PENDING y MISSING.

    Hallazgos para Ari (per COPY-01 y CONTEXT). Crea `.planning/phases/02-secciones-marca-y-copy/02-ARI-FINDINGS.md` con un encabezado (fecha, número de filas de `PENDING-COPY.md`, fase y plan) y seis secciones numeradas, de las que Ari puede responder por partes. Cada ítem dice qué ve hoy el visitante, qué se necesita de Ari y la ruta del YAML que se edita (en comillas invertidas); los textos del doc se citan literales, con sus erratas, y no se tocan. Toma el contenido de los seis SUMMARY (solo sus secciones de erratas, hallazgos y desviaciones), de `PENDING-COPY.md` y de la lista de abajo; lo que un SUMMARY reporte y no esté en las secciones fijas va en la sección 6. (1) Decisiones de alto impacto: el morado de marca, #73187F del texto del brandbook frente a #4228D1 del vectorial del logo (si el oficial es el azul violeta, cambian los tokens y los pares de contraste de toda la fase); la duración de la llamada, 20 o 30 minutos (`call.duration`, y todos los textos que la usan); la terminología SEO/GEO frente a "AEO" (el titular de La solución y "Auditoría SEO + AEO completa" muestran 'FALTA CONFIRMAR' porque la guarda rechaza "AEO"); la licencia web de Hurme Geometric Sans 3 (mientras tanto se usa Outfit, una desviación de marca que Ari debe aprobar por escrito); el consentimiento del equipo para publicar nombres y, si se quieren, biografías; y la aprobación del set de avatares Loopy y del favicon (mesa 13 o 18). (2) Texto que falta y la página muestra como 'FALTA CONFIRMAR': Para quién es (título, nombres de columna y tres líneas por columna, con el perfil de USD 200k o más al año y sin rangos de inversión mensual), FAQ (título y seis pares de pregunta y respuesta con sus temas), footer (correo y redes), política de privacidad, las dos marcas `[VERIFICAR...]` del doc (Pilar 4 y resultado 2 de la sección 4), el canal de los casos 1 a 4, los plazos y el sector que falten. (3) Texto que se muestra tal cual pero Ari debe confirmar (`pending` con texto del doc): las dos afirmaciones de mercado y la credencial del hero, los plazos de resultados y de fases, los nombres del equipo, las cifras de los casos, el cargo de Verónica en el Pilar 3 frente al de Quiénes somos, y los rótulos usados como titular. (4) Erratas del doc que la página muestra tal cual: cada una con su ruta ('estan' en `hero.description[1]` y en `agenda.title`, 'direcciôn' con circunflejo y sin punto final en el Pilar 3, el espacio doble de `problem.items[2]`, 'trafico', 'organico' y 'exito' en Resultados y Casos, la repetición 'cuándo y cuándo', la falta de punto final del caso 5 y el '$' sin moneda). (5) Afirmaciones que el UI-SPEC daba por existentes y el doc no trae ("los ads se encarecen", "el orgánico se abarata", entre las cinco de COPY-VERIFICATION): preguntar si Ari las quiere. (6) Cómo se aplica cada respuesta: se edita el `text` en `src/content/landing.es.yaml` y se cambia `status` a `verified` cuando Ari lo aprueba, se corre `npm run pending`, y `PUBLIC_ENV=production npm run build` pasa cuando la lista queda vacía. La prosa propia del archivo va en español neutro sin voseo ni guiones largos y pasa por `humanizer` con la herramienta Skill; las citas del doc no (per COPY-01 y la desviación 15).

    Suite completa (per el brief, lección 2). En este orden: `node --test tests/guards/*.test.mjs`, `node scripts/check-contrast.mjs`, `npm run build`, `npx astro preview --port 4322`, `npx playwright test --project=chromium`, `npm run test:e2e:isolated` y, si `curl -s -o /dev/null -w '%{http_code}' https://forms.clickup.com` da 200, `npx playwright test --project=live`; si no da 200, anota 'live no ejecutado: ClickUp no responde' y reintenta más tarde sin culpar al código. Al terminar `npx astro preview stop` y detén todo servidor dev. Escribe `02-07-SUMMARY.md` con el resultado de cada comando, los cambios de `polish`, las decisiones de criterio de este plan, lo que queda para Juan y lo que queda para Ari.
  </action>
  <acceptance_criteria>
    - `npm run pending` ejecutado dos veces produce el mismo hash de `PENDING-COPY.md` y `node scripts/list-pending.mjs --check` sale 0.
    - Con `PUBLIC_ENV=production`, `check-copy --json` sale 1 con `structural` vacío y reglas solo PENDING y MISSING (conteos anotados); `npm run build` sin producción sale 0; `--dist dist` sale 1 nombrando `index.html` y `privacidad/index.html`; `PUBLIC_ENV=production npm run build` sale distinto de 0 sin cambiar `dist/index.html`.
    - `02-ARI-FINDINGS.md` existe con seis secciones numeradas, el encabezado con la cuenta de filas de `PENDING-COPY.md`, el contraste #73187F frente a #4228D1, 20 frente a 30 minutos, AEO, Hurme y Outfit, el consentimiento del equipo y las erratas con su ruta; todo primer segmento de ruta con afirmaciones `pending` o con el relleno aparece citado en comillas invertidas; las citas del doc son literales.
    - La suite completa sale en verde: guardas (66 de la fase 1 más las nuevas), contraste, build, `chromium`, `test:e2e:isolated` y `live` (o su reintento anotado).
    - `git diff --name-only BASE` está contenido en `files_modified`; ningún servidor dev o preview queda corriendo.
  </acceptance_criteria>
  <verify>
    <automated>npm run pending && H1=$(shasum PENDING-COPY.md) && npm run pending && test "$H1" = "$(shasum PENDING-COPY.md)" && node scripts/list-pending.mjs --check && npm run build && (PUBLIC_ENV=production node scripts/check-copy.mjs --dist dist; test $? -eq 1)</automated>
    <fails_when>los dos hashes difieren (la lista no es determinista), `--check` sale distinto de 0, el build sin producción falla, o `--dist dist` no sale 1 (el relleno no llegó a `dist` o la guarda no lo ve)</fails_when>
    <automated>(PUBLIC_ENV=production node scripts/check-copy.mjs --json || true) | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s);const f=require('fs').readFileSync('.planning/phases/02-secciones-marca-y-copy/02-ARI-FINDINGS.md','utf8');const q=String.fromCharCode(96);const hits=r.content.filter(v=>v.rule==='PENDING'||v.rule==='MISSING');const segs=new Set(hits.map(v=>v.path.split(/[.[]/)[0]));const miss=[...segs].filter(x=>!f.includes(q+x));const secs=(f.match(/^## [1-6]\./gm)||[]).length;const ok=r.structural.length===0&&r.content.every(v=>v.rule==='PENDING'||v.rule==='MISSING')&&segs.size>0&&miss.length===0&&secs===6&&f.includes('#73187F')&&f.includes('#4228D1');process.exit(ok?0:1)})"</automated>
    <fails_when>`structural` no está vacío, `content` trae una regla distinta de PENDING y MISSING, el archivo de hallazgos no tiene las seis secciones numeradas, le faltan los dos morados, o algún primer segmento de ruta con afirmaciones pendientes no está citado</fails_when>
    <automated>node -e "const {spawnSync:s}=require('child_process');const fs=require('fs');const t=fs.statSync('dist/index.html').mtimeMs;const r=s('npm',['run','build'],{env:{...process.env,PUBLIC_ENV:'production'},encoding:'utf8'});process.exit(r.status!==0&&fs.statSync('dist/index.html').mtimeMs===t?0:1)"</automated>
    <fails_when>el build de producción sale 0 (la guarda no bloquea) o reconstruyó `dist` (el bloqueo no ocurrió en `prebuild`)</fails_when>
    <automated>node --test tests/guards/*.test.mjs && node scripts/check-contrast.mjs && npx playwright test --project=chromium && npm run test:e2e:isolated</automated>
    <fails_when>salida distinta de 0, el resumen de `node --test` muestra "# fail" mayor que 0 o un spec de navegador falla o queda en `skip` (con el preview de 4322 en marcha y `dist` reconstruido tras el último cambio)</fails_when>
  </verify>
  <done>La lista de pendientes es determinista, producción falla solo por PENDING y MISSING, Ari tiene un único archivo con todo lo que debe decidir, confirmar o corregir, y la suite completa de la fase está en verde (o `live` reintentado y anotado); commit hecho y SUMMARY escrito.</done>
</task>

</tasks>

<verification>
1. `node --test tests/guards/*.test.mjs` sale 0: las 66 pruebas de la fase 1 más las nuevas (VERIFICAR 3b a 3e, `motion-budget`, `phase2-static`).
2. `node scripts/check-contrast.mjs` sale 0 y `npm run build` sin `PUBLIC_ENV` sale 0 (con `prebuild` y `postbuild`).
3. `npm run pending` es determinista y `node scripts/list-pending.mjs --check` sale 0.
4. `PUBLIC_ENV=production node scripts/check-copy.mjs --json` sale 1 con `structural` vacío y solo reglas PENDING y MISSING; `--dist dist` sale 1 por el relleno; `PUBLIC_ENV=production npm run build` falla en `prebuild` sin reconstruir `dist`.
5. Con `npx astro build && npx astro preview --port 4322`: `npx playwright test --project=chromium` (incluidos `motion.spec.ts` y `phase-closing.spec.ts`), `npm run test:e2e:isolated` y `npx playwright test --project=live` si ClickUp responde; `npx astro preview stop` al final.
6. `document.getAnimations().length` vale 0 con `reduce` en `/` y `/privacidad/`, 0 en reposo con `no-preference` y solo animaciones de la lista blanca mientras corre la entrada.
7. `git diff --name-only BASE` de cada tarea está contenido en `files_modified` (más los archivos de `polish` declarados en el SUMMARY) y `git diff BASE -- src/content/landing.es.yaml` solo muestra la conversión de una marca si la tarea 3 la exigió.
8. Verificaciones humanas para Juan, anotadas en `02-VISUAL-LOG.md`: mirada real al movimiento con ambas preferencias, VoiceOver sobre `<details>` en Safari y aprobación de avatares, favicon y colores del logo.
</verification>

<success_criteria>
- La entrada del collage, las pupilas y el giro del icono del FAQ existen solo bajo `no-preference`, solo con `transform`, con un total finito menor a 1,4 s, sin esconder contenido, y con `reduce` la página no tiene ninguna animación (DSGN-05).
- La regla VERIFICAR reconoce `[VERIFICAR: nota]` y `[VERIFICAR rango]`, el YAML real en producción reporta solo PENDING y MISSING, y ninguna aserción de la fase 1 contradice el diseño nuevo (COPY-01).
- El favicon, el sprite, los collages, los CTA, el área de salvado, las 12 secciones y los 11 h2 están afirmados por pruebas, y la higiene de fase (HTML en bruto, hex, `outline`, `line-height`, scripts) y el presupuesto de movimiento los vigilan dos guardas estáticas que muerden (DSGN-03, DSGN-04, DSGN-05).
- La página completa pasa la matriz de seis anchos con ambas preferencias, el espaciado de texto forzado, sin JavaScript, con ClickUp bloqueado y el axe de humo sin violaciones críticas ni serias, y cumple los 8 rasgos de vibra con evidencia en `02-VISUAL-LOG.md` (DSGN-03, DSGN-04).
- Ari tiene un único archivo, `02-ARI-FINDINGS.md`, con decisiones, faltantes, confirmaciones y erratas; `PENDING-COPY.md` es determinista y el build de producción falla solo por lo pendiente (COPY-01).
- La suite completa está en verde y la fase 2 queda lista para la verificación y para la fase 3.
</success_criteria>

<output>
Create `.planning/phases/02-secciones-marca-y-copy/02-07-SUMMARY.md` when done
</output>
