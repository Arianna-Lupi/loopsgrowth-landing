---
phase: 03-seo-medicion-minima-y-qa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/landing.es.yaml
  - src/content.config.ts
  - src/lib/seo.mjs
  - src/lib/schema-graph.mjs
  - src/layouts/BaseLayout.astro
  - src/components/JsonLd.astro
  - src/pages/index.astro
  - src/pages/robots.txt.ts
  - src/pages/marca/og/[card].astro
  - astro.config.mjs
  - scripts/og/export-og.mjs
  - scripts/verify-env-surface.mjs
  - public/og.png
  - package.json
  - package-lock.json
  - .gitignore
  - tests/guards/seo-head.test.mjs
  - tests/guards/seo-jsonld.test.mjs
  - tests/guards/seo-og.test.mjs
  - tests/guards/seo-build.test.mjs
  - tests/guards/phase2-static.test.mjs
  - tests/e2e/seo-structure.spec.ts
autonomous: true
requirements: [SEO-01, SEO-02, SEO-03]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "El title, la meta description y el alt de la imagen OG salen de `landing.es.yaml`, nunca del código. El doc de Ari (`02-ARI-COPY-V2.md`) no trae title ni meta description (búsqueda hecha: solo aparecen el subtítulo y las menciones de SEO en el cuerpo). Por eso `meta.title` y `meta.description` nacen `pending`, con `confirm_by: Ari` y una razón visible en `npm run pending`: son propuestas para su aprobación, no copy verificado. La propuesta de title es `Loops Growth: SEO y GEO para e-commerce` (39 caracteres); la de description es el subtítulo del Copy v2 sin tocar (`Un equipo dedicado y especializado que ejecuta tu {term} y tu visibilidad en asistentes de IA (ChatGPT, Gemini).`, con `{term}` resuelto desde `brand.term`), que cabe en 155. El `meta.title_template` provisional de la fase 1 desaparece (nadie más lo lee: solo `content.config.ts`, el YAML e `index.astro`)."
    - "El alt de la imagen OG no es texto nuevo: se compone en `seo.mjs` como `<brand.name.text>. <hero.h1.text>` con dos afirmaciones que ya están `verified`, porque la imagen solo lleva el H1 de Ari y el logo. Ninguna cadena de texto de cara al visitante vive en `seo.mjs`, `schema-graph.mjs`, `BaseLayout.astro` ni en el script de exportación: todo llega por parámetros desde el YAML."
    - "Límites medidos en caracteres Unicode (`[...texto].length`, no `.length` de UTF-16): title 60 o menos, description 155 o menos y al menos 50. Las constantes `TITLE_MAX = 60` y `DESCRIPTION_MAX = 155` viven solo en `src/lib/seo.mjs`. Un title o una description vacíos, con salto de línea o con 'AEO' son errores; que falte 'SEO y GEO', 'e-commerce' o el nombre de marca es solo advertencia (el texto de Ari manda y la guarda no debe bloquearlo). La propuesta actual pasa con cero advertencias."
    - "La superficie indexable es `isProduction && !noindex && siteUrl`. Solo ahí se emiten `link rel=canonical`, `og:url`, `og:image` (con `og:image:width` 1200, `og:image:height` 630, `og:image:type` image/png y `og:image:alt`), `twitter:image`, `twitter:image:alt` y el JSON-LD. Fuera de esa superficie la página sigue con `noindex`, sin canonical, sin JSON-LD y sin URLs absolutas de imagen (una URL relativa en `og:image` falla en WhatsApp y LinkedIn, así que es mejor no emitirla). `og:title`, `og:description`, `og:type` (website), `og:site_name`, `og:locale` (es_LA), `twitter:card` (summary_large_image), `twitter:title` y `twitter:description` se emiten siempre en `/`. No hay `twitter:site` (Ari no entregó cuenta). `/privacidad/` no emite etiquetas sociales ni JSON-LD y conserva `noindex` sin canonical."
    - "Exactamente un `link rel=canonical` en el HTML de producción de `/`, con la URL absoluta de `PUBLIC_SITE_URL` (con barra final) y sin parámetros. El canonical y `og:url` son iguales."
    - "La imagen OG es `public/og.png`: 1200x630 px exactos, PNG, de 204800 bytes o menos (tope `OG_MAX_BYTES` en `seo.mjs`, objetivo 150 KB), sin metadatos, con la composición estática de marca (collage oficial, el H1 de Ari tal cual en texto real y el logo). Se genera con un script reproducible (`scripts/og/export-og.mjs`) desde una página interna `/marca/og/tarjeta/` que solo existe fuera de producción y es `noindex`. El diseño pasó por `impeccable` y `design-taste-frontend`, usa solo tokens y componentes existentes (sin hex nuevo, sin fotos nuevas) y todo par texto/fondo pasa `check-contrast.mjs`. La aprobación visual de Ari queda registrada como pendiente en el SUMMARY (backstop)."
    - "El JSON-LD es un solo `<script is:inline type=\"application/ld+json\">` con un `@graph` de exactamente cuatro nodos: `Organization` (`@id` `<origen>/#organization`), `ProfessionalService` (`#service`, con `parentOrganization` al `@id` de la organización), `WebSite` (`#website`, `inLanguage` es, `publisher` a la organización) y `WebPage` (`<url>#webpage`, `isPartOf` al sitio, `about` a la organización, `inLanguage` es, `primaryImageOfPage` con la imagen OG y sus dimensiones). Nombre = `brand.name.text`, `description` = la meta description. Prohibidos y verificados por guarda: los tipos `Review`, `AggregateRating`, `Rating` y `Offer`; las claves `review`, `aggregateRating`, `address`, `telephone`, `email` y `logo`; ningún `SearchAction`."
    - "`sameAs` se omite mientras `footer.social` siga `pending` (hoy es 'FALTA CONFIRMAR'). Cuando Ari lo entregue y quede `verified`, `extractSameAs` toma solo las URL `https://` válidas del texto, sin duplicados y en el orden dado; cualquier otro protocolo se descarta. Dirección, teléfono, correo y logo no se agregan hasta que Ari los entregue (cada uno con su cambio deliberado de la guarda)."
    - "El JSON-LD se serializa con `serializeJsonLd`, que escapa `<`, `>`, `&`, U+2028 y U+2029 como `\\uXXXX`: un texto del YAML (lo edita el equipo, incluso desde la web de GitHub) no puede cerrar el `script`. Es el único uso de `set:html` del proyecto y por eso `tests/guards/phase2-static.test.mjs` lo autoriza con nombre propio: `RAW_HTML_ALLOW = ['src/components/JsonLd.astro']` y `SCRIPT_ALLOW['src/components/JsonLd.astro'] = 1`, con una prueba que exige que esas dos listas no crezcan sin editar la prueba."
    - "Un solo `h1` y landmarks semánticos verificados en el HTML de producción y en el navegador: exactamente un `h1` (dentro de `#inicio`), un `header`, un `main#main`, un `footer`, `<html lang=\"es\">`, y todo `nav` con `aria-label`. La guarda de build falla si aparece un segundo `h1`, un `main` duplicado o un `nav` sin nombre."
    - "robots por entorno desde una función pura (`buildRobots`) que consume `src/pages/robots.txt.ts`. Producción: grupo `User-agent: *` con `Allow: /` y un grupo propio con `Allow: /` para GPTBot, ClaudeBot, PerplexityBot y Google-Extended (un grupo con nombre anula al `*`, por eso cada uno repite el `Allow`), más `Sitemap: <origen>/sitemap-index.xml`. Cualquier otro entorno: `User-agent: *` y `Disallow: /`, sin línea `Sitemap`. Cambio deliberado frente a la fase 1 (que permitía rastrear para que Google viera el `noindex`): un entorno no productivo no se publica ni se comparte, y el `noindex` del HTML se conserva como segunda barrera. `scripts/verify-env-surface.mjs` se actualiza a esa regla."
    - "Sitemap solo cuando `PUBLIC_ENV=production` y existe `PUBLIC_SITE_URL` (regla ya vigente en `astro.config.mjs`; con `production` sin URL el build ya lanza error). El filtro excluye `/privacidad/` (WR-04: `noindex` dentro del sitemap es una señal contradictoria) y todo lo que empiece por `/marca/`; `SITEMAP_EXCLUDED_PATHS` vive en `seo.mjs` y `astro.config.mjs` lo importa. `sitemap-0.xml` de producción lista exactamente una URL: `<origen>/`. Fuera de producción no existe `sitemap-index.xml`."
    - "Las páginas internas de marca no salen en producción: `dist/marca/` no existe en un build con `PUBLIC_ENV=production`, y `dist/og.png` sí."
    - "No se rompe nada existente: `node --test tests/guards/*.test.mjs` sigue en verde (incluido el presupuesto del HTML de `/`, `HTML_RAW_MAX` 81920 y `HTML_GZIP_MAX` 25600 en `tests/e2e/lib/budgets.mjs`, que este plan NO modifica; el JSON-LD y las metaetiquetas caben en ese presupuesto o se recortan), `node scripts/check-contrast.mjs` sale 0 y `node scripts/check-copy.mjs` sigue reportando pendientes de Ari sin errores estructurales. El build de producción real (`npm run build` con `PUBLIC_ENV=production`) sigue bloqueado por las guardas mientras haya copy `pending`: es intencional y este plan no lo evita."
    - statement: "La imagen OG, vista a tamaño real y a 600 px de ancho (como la muestra WhatsApp), se lee sin esfuerzo: el H1 completo, el logo y el collage sin recortes, con la misma familia visual que el hero de la página"
      verification: backstop
  artifacts:
    - path: src/lib/seo.mjs
      provides: "Límites, reglas de title y description, alt de OG, metaetiquetas sociales, superficie indexable, buildRobots y rutas excluidas del sitemap (módulo puro, sin dependencias de Astro)"
      contains: "TITLE_MAX"
    - path: src/lib/schema-graph.mjs
      provides: "buildGraph, extractSameAs y serializeJsonLd con los cuatro nodos y las claves prohibidas"
      contains: "serializeJsonLd"
    - path: src/components/JsonLd.astro
      provides: "Único script application/ld+json de la página, con el grafo serializado y escapado"
      contains: "application/ld+json"
    - path: src/layouts/BaseLayout.astro
      provides: "Head con description, canonical, Open Graph, Twitter y JSON-LD según la superficie indexable"
      contains: "buildSocialMeta"
    - path: src/pages/marca/og/[card].astro
      provides: "Tarjeta interna 1200x630 (solo fuera de producción, noindex) que se fotografía para la imagen OG"
      contains: "getStaticPaths"
    - path: scripts/og/export-og.mjs
      provides: "Exportación reproducible de la tarjeta a public/og.png con Playwright y sharp, con validación de medidas y peso"
      contains: "OG_MAX_BYTES"
    - path: public/og.png
      provides: "Imagen OG 1200x630 PNG dentro del presupuesto de peso"
      contains: "PNG"
    - path: src/pages/robots.txt.ts
      provides: "robots.txt por entorno desde buildRobots"
      contains: "buildRobots"
    - path: astro.config.mjs
      provides: "Sitemap con filtro de rutas excluidas (WR-04)"
      contains: "SITEMAP_EXCLUDED_PATHS"
    - path: tests/guards/seo-build.test.mjs
      provides: "Construye producción, no productivo y producción con noindex en dist-seo/ y afirma head, JSON-LD, robots, sitemap, OG, un solo h1 y landmarks"
      contains: "dist-seo"
    - path: tests/e2e/seo-structure.spec.ts
      provides: "Comprobación en navegador de un solo h1, landmarks con nombre y head del build de vista previa"
      contains: "banner"
  key_links:
    - from: src/pages/index.astro
      to: src/content/landing.es.yaml
      via: "fill(data.meta.title / data.meta.description) y ctaLabel: el title y la description llegan a BaseLayout como props"
      pattern: "meta.description"
    - from: src/layouts/BaseLayout.astro
      to: src/lib/seo.mjs
      via: "buildSocialMeta e isIndexable deciden qué etiquetas se emiten"
      pattern: "seo.mjs"
    - from: src/components/JsonLd.astro
      to: src/lib/schema-graph.mjs
      via: "buildGraph y serializeJsonLd; único uso de set:html, autorizado en phase2-static.test.mjs"
      pattern: "serializeJsonLd"
    - from: src/pages/robots.txt.ts
      to: src/lib/seo.mjs
      via: "buildRobots({ isProduction, siteUrl })"
      pattern: "buildRobots"
    - from: astro.config.mjs
      to: src/lib/seo.mjs
      via: "SITEMAP_EXCLUDED_PATHS en el filter del sitemap"
      pattern: "SITEMAP_EXCLUDED_PATHS"
    - from: scripts/og/export-og.mjs
      to: src/pages/marca/og/[card].astro
      via: "Playwright abre /marca/og/tarjeta/ en el preview 4322 y guarda la captura"
      pattern: "marca/og/tarjeta"
---

<objective>
Dejar la cabeza del documento, los datos estructurados y las reglas de rastreo de la landing listos para producción: title, meta description, canonical, Open Graph y Twitter, la imagen OG de 1200x630, el JSON-LD de cuatro nodos, la garantía de un solo H1 con landmarks, `robots.txt` por entorno y sitemap sin páginas `noindex`.

Purpose: SEO-01, SEO-02 y SEO-03. La página vende SEO técnico: su propio `<head>` es código auditable y cada regla (largos, un canonical, tipos permitidos, robots, sitemap, peso de la imagen) queda cubierta por una guarda que falla si alguien la rompe. Ari usará la landing en un evento y se compartirá por WhatsApp y LinkedIn, así que la vista previa social importa.

Output: metadatos y JSON-LD en `BaseLayout`, `public/og.png` con su script de exportación, `robots.txt.ts` y sitemap por entorno, cuatro archivos de guardas y un spec de navegador, y las propuestas de title y description como `pending` para que Ari las apruebe.

Este plan es la ola 1 de la fase 3: es dueño de la cabeza de `BaseLayout.astro`, de `astro.config.mjs` y de `robots.txt.ts`. El plan 03-02 (medición) monta después una línea en `BaseLayout` y el 03-03 (auditoría) mide lo que aquí se construye; ninguno de los dos debe reescribir estas piezas. Modo mvp: la rebanada vertical es "un enlace compartido muestra título, descripción e imagen correctos, y un rastreador ve el grafo y las reglas".
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-seo-medicion-minima-y-qa/03-CONTEXT.md
@.claude/CLAUDE.md

Reglas duras que el ejecutor debe respetar (vienen de CLAUDE.md, del contexto de la fase y de Juan):

- A11Y.md obligatorio. La imagen OG es un recurso de metadatos, no de la página, pero su `og:image:alt` va siempre. La tarjeta interna `/marca/og/tarjeta/` es una herramienta: `noindex`, sin scripts, sin animación, con el texto en HTML real.
- Todo trabajo de diseño (la imagen OG incluida) pasa por las skills `impeccable` y `design-taste-frontend`. Invócalas con la herramienta Skill antes de dibujar la tarjeta (tarea 2) y registra en el SUMMARY qué decidieron. La skill `humanizer` NO se usa: el copy sale de Ari y no se reescribe.
- Ningún texto de cara al visitante se inventa. Lo que falta se propone como `pending` con razón, o se pide a Ari. Se escribe "SEO/GEO" o "SEO y GEO" y nunca "AEO". Español neutro, "tú", nunca voseo. Ni en lo que escribas (código, comentarios, mensajes de commit) ni en el YAML: sin guion largo ni medio.
- Commits atómicos con lista explícita de archivos (`git add <rutas>`, nunca `git add -A` ni `git add .`). El directorio `.planning/phases/02-secciones-marca-y-copy/brand-inventory/` es confidencial: no se lee, no se copia y no se agrega. Los `.ai` y el PDF del BrandBook siguen fuera del repositorio.
- Node >=22.12, ESM, `node:test`. Los módulos que comparten Astro y Node son `.mjs` planos con JSDoc y sin importar nada de `astro:*` (como `src/lib/english-terms.mjs`).
- Navegador: `npx astro build && npx astro preview --port 4322` en segundo plano, `E2E_BLOCK_CLICKUP=1` en Playwright y `npx astro preview stop` al terminar. `tests/global-setup.ts` falla si `dist` es más viejo que `src/`, `public/` o `astro.config.mjs`: reconstruye y reinicia el preview después de cada cambio.
- Las guardas `node --test` corren en paralelo por archivo y varias leen `dist/`. Por eso `seo-build.test.mjs` construye SIEMPRE con `--outDir dist-seo/<caso>` y nunca toca `dist/`.

Archivos que el plan toca y que otros planes de la fase también tocarán (coordinación): `src/layouts/BaseLayout.astro` (03-02 añade una línea de montaje de medición) y `tests/guards/phase2-static.test.mjs` (03-02 añade sus propios scripts a `SCRIPT_ALLOW`). Este plan corre primero: deja los mapas de autorización como objetos con una entrada por archivo para que 03-02 solo añada la suya.

Hallazgos del código que condicionan el trabajo (verificados al planificar):

- `BaseLayout.astro` hoy recibe `title`, `noindex` y `path`; emite `noindex` si `!isProduction || noindex` y el canonical solo en producción, sin `noindex` y con URL. No hay description ni etiquetas sociales ni JSON-LD.
- `astro.config.mjs` ya lanza error si `PUBLIC_ENV=production` sin `PUBLIC_SITE_URL`, y ya activa `sitemap()` solo con producción y URL, sin filtro (WR-04 abierto: `/privacidad/` sale en el sitemap).
- `src/pages/robots.txt.ts` hoy da `Allow: /` en todo entorno y solo añade `Sitemap` en producción.
- `Hero.astro` trae el único `h1` (`#hero-title` dentro de `#inicio`); `SiteHeader`, `SiteFooter`, `SkipLinks` (nav con `aria-label`) y `main#main` ya existen. Este plan no cambia su marcado, solo lo vigila.
- `tests/guards/phase2-static.test.mjs` prohíbe `set:html` y limita los `<script` a `AgendaSection.astro` (2). El JSON-LD necesita una excepción con nombre.
- `sharp` 0.35.4 está instalado pero no declarado (WR-06 de la revisión de la fase 2). La exportación de la imagen lo necesita: se declara con esa versión exacta.
- Ninguna prueba existente lee el title viejo ('agencia de'); `title_template` solo lo usan `content.config.ts`, el YAML e `index.astro`.
- `es_LA` como `og:locale` es una decisión razonable para hispanohablantes de varios países; si Ari prefiere `es_ES` o `es_MX` se cambia la constante `OG_LOCALE` en `seo.mjs`.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Title, description, canonical y etiquetas sociales de punta a punta (claves del YAML, esquema, seo.mjs, BaseLayout y guarda de head)</name>
  <files>src/content/landing.es.yaml, src/content.config.ts, src/lib/seo.mjs, src/layouts/BaseLayout.astro, src/pages/index.astro, tests/guards/seo-head.test.mjs, tests/guards/seo-build.test.mjs</files>
  <read_first>
    - `src/layouts/BaseLayout.astro`, `src/pages/index.astro` y `src/pages/privacidad.astro` (los tres, completos: hoy pasan `title`, `noindex` y `path`).
    - `src/content.config.ts` líneas 40 a 60 (el bloque `meta` del esquema) y la definición de `claim` (líneas 1 a 20).
    - `src/content/landing.es.yaml` líneas 1 a 60 (regla de status al nacer, `brand`, `call` y el bloque `meta`, que se reemplaza).
    - `src/lib/content.ts` líneas 1 a 60 (`getLanding`, `fill`, `textVars`).
    - `src/lib/site.ts` (`isProduction`, `siteUrl`, `canonicalUrl`).
    - `scripts/verify-env-surface.mjs` líneas 1 a 45 (patrón de `spawnSync` para construir por entorno; aquí se usará `--outDir`).
    - `scripts/lib/copy-rules.mjs` líneas 1 a 25 (qué prohíbe el copy: 'AEO', voseo, guion largo) para que las propuestas nuevas pasen la guarda.
  </read_first>
  <behavior>
    Pruebas (RED primero) en `tests/guards/seo-head.test.mjs`, solo puras, sin build:
    - `TITLE_MAX` vale 60, `DESCRIPTION_MAX` 155 y `DESCRIPTION_MIN` 50; `charLength('e-commerce á ñ ¿')` cuenta caracteres Unicode y no unidades UTF-16 (usa una cadena con un carácter fuera del plano básico para que la prueba muerda).
    - `checkTitle(text, { brand })` devuelve `{ errors, warnings }`: error si vacío, más de 60, con salto de línea o con 'AEO' (sin distinguir mayúsculas); advertencia por cada uno de 'SEO y GEO', 'e-commerce' y el nombre de marca que falte. `Loops Growth: SEO y GEO para e-commerce` da cero errores y cero advertencias; una cadena de 61 caracteres da un error que nombra el largo; `Loops Growth` solo da dos advertencias.
    - `checkDescription(text)` da error si tiene menos de 50, más de 155, salto de línea o 'AEO'.
    - `composeOgAlt(brandName, h1)` devuelve `<brandName>. <h1>` sin añadir palabras (con las cadenas del YAML da `Loops Growth. Crecemos tu tienda a través de Google, ChatGPT y Gemini.`).
    - `isIndexable({ isProduction, noindex, siteUrl })` es verdadero solo con producción, sin `noindex` y con URL; falso en las otras siete combinaciones.
    - `buildSocialMeta({ title, description, siteName, alt, url, imageUrl, indexable })`: con `indexable` falso devuelve solo `og:title`, `og:description`, `og:type`, `og:site_name`, `og:locale`, `twitter:card`, `twitter:title` y `twitter:description`; con `indexable` verdadero añade `og:url`, `og:image`, `og:image:width` 1200, `og:image:height` 630, `og:image:type`, `og:image:alt`, `twitter:image` y `twitter:image:alt`. `og:image` y `og:url` son absolutas o la función lanza. Ningún valor trae saltos de línea.
    - El YAML real: `meta.title` y `meta.description` existen, ambos `pending` con `confirm_by: Ari` y `reason`, y `meta.title_template` ya no existe. `checkTitle` y `checkDescription` sobre los textos reales (description con `{term}` resuelto desde `brand.term`) dan cero errores y el title cero advertencias.
    Mutación: una versión de `checkTitle` con el tope en 61 es detectada por la prueba de 61 caracteres.
  </behavior>
  <action>
    1. RED. Crea `tests/guards/seo-head.test.mjs` (node:test, ESM) con los bloques de `<behavior>`. Para leer el YAML usa `parse` de `yaml` sobre `src/content/landing.es.yaml` (como hace `check-copy.mjs`), sin pasar por Astro. Corre `node --test tests/guards/seo-head.test.mjs`: debe fallar porque `seo.mjs` no existe. No commitees en rojo.

    2. Crea `src/lib/seo.mjs` (ESM plano, JSDoc, sin imports de `astro:*` ni de `src/lib/site.ts`). Exporta: `TITLE_MAX`, `DESCRIPTION_MAX`, `DESCRIPTION_MIN`, `OG_WIDTH` 1200, `OG_HEIGHT` 630, `OG_MAX_BYTES` 204800, `OG_PATH` `/og.png`, `OG_LOCALE` `es_LA`, `SITEMAP_EXCLUDED_PATHS` `['/privacidad/', '/marca/']`, `charLength`, `checkTitle`, `checkDescription`, `composeOgAlt`, `isIndexable`, `buildSocialMeta`. `buildSocialMeta` devuelve `{ og: [[property, content], ...], twitter: [[name, content], ...] }` en orden estable. Los mensajes de error van en español. Las tareas 3 y 4 añaden a este mismo archivo `buildRobots` (tarea 4) y usan sus constantes (tarea 3); no lo reorganices después.

    3. YAML y esquema. En `landing.es.yaml` reemplaza el bloque `meta` por:
       - `meta.title`: `text: "Loops Growth: SEO y GEO para e-commerce"`, `status: pending`, `confirm_by: Ari`, `reason: "El doc de Ari no trae title. Propuesta de 39 caracteres con marca, SEO y GEO y e-commerce; Ari aprueba o entrega el suyo (máximo 60)"`.
       - `meta.description`: `text: "Un equipo dedicado y especializado que ejecuta tu {term} y tu visibilidad en asistentes de IA (ChatGPT, Gemini)."`, `status: pending`, `confirm_by: Ari`, `reason: "El doc de Ari no trae meta description. Propuesta: el subtítulo del Copy v2 sin cambios; Ari aprueba o entrega la suya (máximo 155)"`.
       Copia el texto de la description carácter por carácter del subtítulo del YAML (`hero.subtitle`) para no introducir diferencias. En `content.config.ts` cambia `meta` a `z.strictObject({ title: claim, description: claim })` con un comentario de una línea que remita al límite de largo y a `seo.mjs`. Verifica que `npx astro build` compila (Astro valida el YAML contra el esquema).

    4. `BaseLayout.astro`: añade las props opcionales `description?: string`, `social?: { siteName: string, ogAlt: string }` y conserva `title`, `noindex` y `path`. Calcula `const indexable = isIndexable({ isProduction, noindex, siteUrl })`. Emite `<meta name="description">` cuando llega `description`. Mantén el comportamiento actual de `noindex` y canonical (canonical solo si `indexable`, una sola vez). Si llega `social`, recorre `buildSocialMeta(...)` y emite cada `<meta property=...>` y `<meta name=...>`; `imageUrl` es `new URL(OG_PATH, siteUrl).href` y `url` es el canonical. Orden del head: charset, viewport, robots, title, description, canonical, social, iconos, fuente. No pongas `og:image` ni `og:url` si `indexable` es falso. No añadas etiquetas con texto propio.

    5. `index.astro`: calcula `title = fill(data.meta.title, textVars(data), 'meta.title')` y `description = fill(data.meta.description, textVars(data), 'meta.description')`, y pasa a `BaseLayout` `title`, `description` y `social={{ siteName: data.brand.name.text, ogAlt: composeOgAlt(data.brand.name.text, data.hero.h1.text) }}`. `privacidad.astro` no cambia (sin description ni social).

    6. Comprobación de punta a punta y esqueleto de la guarda de build. Crea `tests/guards/seo-build.test.mjs` con un ayudante `build(caso, env)` que ejecuta `npx astro build --outDir dist-seo/<caso>` con `spawnSync`, con valores explícitos de `PUBLIC_ENV` y `PUBLIC_SITE_URL` (cadena vacía en lugar de `delete`, como `verify-env-surface.mjs`) y un `PUBLIC_GA4_ID` vacío por si 03-02 ya existiera. Casos: `prod` (`PUBLIC_ENV=production`, `PUBLIC_SITE_URL=https://example.com`), `dev` (sin entorno) y `noindex` no aplica todavía (se agrega en la tarea 4). Aserciones de esta tarea: en `prod` el `<title>` es el del YAML y mide 60 o menos; hay exactamente una `meta name="description"` de 155 o menos; exactamente un `rel="canonical" href="https://example.com/"`; `og:url` igual al canonical; `og:image` es `https://example.com/og.png` con ancho 1200, alto 630 y `og:image:alt` no vacío; `twitter:card` es `summary_large_image`; en `dev` no hay canonical, no hay `og:image` ni `og:url`, hay `noindex` y `og:title` sí. Nota: `og.png` todavía no existe (tarea 2), así que esta guarda NO comprueba el archivo aquí. Añade `dist-seo/` a `.gitignore`. El build de producción con `PUBLIC_ENV=production` por `npx astro build` directo no pasa por las guardas de `prebuild`, así que funciona aunque haya copy `pending`.

    7. Verifica GREEN y commitea.
  </action>
  <verify>
    <automated>node --test tests/guards/seo-head.test.mjs tests/guards/seo-build.test.mjs && node --test tests/guards/*.test.mjs && node scripts/check-contrast.mjs && node scripts/check-copy.mjs; echo "check-copy exit $?"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/seo.mjs` contiene 'TITLE_MAX = 60', 'DESCRIPTION_MAX = 155' y exporta `buildSocialMeta`, `isIndexable` y `composeOgAlt`; no importa nada de `astro:` ni de `site.ts`.
    - `src/content/landing.es.yaml` contiene `meta:` con las claves `title` y `description`, ambas con `status: pending` y `confirm_by: Ari`, y NO contiene `title_template`.
    - `src/content.config.ts` contiene `title: claim, description: claim` dentro de `meta` y no contiene `title_template`.
    - `BaseLayout.astro` contiene 'buildSocialMeta' y 'isIndexable', y `og:image` solo se emite dentro de la rama indexable (la guarda de `dev` lo prueba).
    - `node --test tests/guards/*.test.mjs` sale 0 (la suite previa sigue en verde) y `check-copy.mjs` no reporta errores estructurales (los pendientes de Ari, incluidos los dos nuevos, aparecen en `npm run pending`).
    - `git status` no muestra `dist-seo/` (está en `.gitignore`).
    - `grep -rn -P '[\x{2013}\x{2014}]' src/lib/seo.mjs tests/guards/seo-head.test.mjs tests/guards/seo-build.test.mjs src/content/landing.es.yaml` no da coincidencias nuevas.
  </acceptance_criteria>
  <done>
    `/` de producción trae title, description, canonical, Open Graph y Twitter salidos del YAML, con la lógica en un módulo puro probado con mutación; fuera de producción sigue `noindex` sin canonical ni URLs absolutas; title y description quedan como propuestas `pending` visibles en la lista de pendientes de Ari. Commit atómico: `git add src/content/landing.es.yaml src/content.config.ts src/lib/seo.mjs src/layouts/BaseLayout.astro src/pages/index.astro tests/guards/seo-head.test.mjs tests/guards/seo-build.test.mjs .gitignore` y mensaje `feat(03-01): title, description, canonical y etiquetas sociales desde el YAML`.
    Punto de corte: si el contexto se agota antes del commit, no dejes nada a medias en `BaseLayout.astro`: `git stash` o `git checkout -- <archivo>` de lo no verde, y anota en el SUMMARY qué pasos (1 a 7) quedaron hechos.
  </done>
</task>

<task type="auto">
  <name>Task 2: Imagen OG 1200x630 con el collage de marca, el H1 de Ari y el logo (tarjeta interna, exportación reproducible y guarda de peso)</name>
  <files>src/pages/marca/og/[card].astro, scripts/og/export-og.mjs, public/og.png, package.json, package-lock.json, tests/guards/seo-og.test.mjs</files>
  <read_first>
    - Invoca primero las skills `impeccable` y `design-taste-frontend` (herramienta Skill) y lee `DESIGN.md` y `PRODUCT.md` de la raíz. La tarjeta es una pieza de diseño: sus decisiones (jerarquía, tono, qué escena del collage) salen de ahí, no de este plan.
    - `src/pages/marca/[sheet].astro` líneas 1 a 45 (patrón de página interna: `getStaticPaths` vacío en producción, `noindex`, lectura de tokens).
    - `src/components/collage/HeroCollage.astro`, `src/components/collage/CollageScene.astro` (líneas 1 a 40) y las claves de `SCENES` en `src/components/collage/scenes.mjs` (solo los nombres de escena, líneas 31 a 60) para saber qué se puede montar sin dibujar nada nuevo.
    - `src/components/brand/Logo.astro` líneas 1 a 50 (props `variant` y `tone`) y `src/components/brand/logo-variants.mjs` (qué combinaciones existen: el horizontal solo sobre `light`).
    - `src/styles/tokens.css` líneas 1 a 60 y el bloque `[data-tone="..."]` (tonos y variables de superficie). `scripts/lib/contrast.mjs` solo el nombre de las funciones exportadas.
    - `scripts/photos/treat.mjs` líneas 1 a 30 (cabecera y estilo de un script reproducible del proyecto, con `sharp`).
    - `playwright.config.ts` líneas 1 a 30 (puerto 4322, `E2E_BLOCK_CLICKUP`).
    - `src/lib/seo.mjs` (creado en la tarea 1: `OG_WIDTH`, `OG_HEIGHT`, `OG_MAX_BYTES`, `OG_PATH`).
  </read_first>
  <action>
    1. RED. Crea `tests/guards/seo-og.test.mjs` (node:test) con estas pruebas, todas sobre `public/og.png` y sobre el código, sin navegador:
       - `public/og.png` existe, empieza con la firma PNG (`89 50 4E 47 0D 0A 1A 0A`), el bloque IHDR (bytes 16 a 23 del archivo, lectura directa con `readUInt32BE`, sin dependencias) da 1200 de ancho y 630 de alto, y pesa `OG_MAX_BYTES` o menos (mensaje con el peso real en bytes).
       - No contiene metadatos: ningún bloque `tEXt`, `iTXt`, `zTXt` ni `eXIf` (recorre los bloques PNG por longitud y tipo).
       - Mutación sobre copias en memoria: un buffer con IHDR de 1200x628 es rechazado por la misma función de validación que usa el script; uno de `OG_MAX_BYTES + 1` bytes también.
       - `src/pages/marca/og/[card].astro` existe, su `getStaticPaths` devuelve `[]` con `isProduction` (el archivo contiene `isProduction ? []`), contiene `noindex`, no contiene `<script`, ni `set:html`, ni literales hex, ni `@keyframes` o `transition`.
       - El texto de la tarjeta sale del YAML: el archivo importa `getLanding` y contiene `hero.h1`; no contiene ninguna cadena literal en español con más de tres palabras seguidas fuera de comentarios (heurística: falla si una línea de marcado, sin `{`, tiene 4 o más palabras alfabéticas seguidas).
       - `scripts/og/export-og.mjs` existe, importa `OG_WIDTH`, `OG_HEIGHT` y `OG_MAX_BYTES` de `src/lib/seo.mjs` y contiene `marca/og/tarjeta`.
       Corre la prueba: falla porque nada existe. No commitees en rojo.

    2. Declara `sharp`: `npm install --save-dev --save-exact sharp@0.35.4` (la versión ya instalada; cierra WR-06 para este uso). Comprueba que `package.json` gana `"sharp": "0.35.4"` en `devDependencies` y que `package-lock.json` solo cambia esa entrada. Añade el script `"og:export": "node scripts/og/export-og.mjs"`.

    3. Diseña la tarjeta con las skills. Crea `src/pages/marca/og/[card].astro`: `getStaticPaths` devuelve `isProduction ? [] : [{ params: { card: 'tarjeta' } }]`. Es un documento propio, sin `BaseLayout` (no lleva header, footer ni skip links): `<!doctype html>`, `lang="es"`, `<meta name="robots" content="noindex">`, `import '../../../styles/global.css'`, el componente `<Font cssVariable="--font-brand" preload />` como en `BaseLayout`, y un solo contenedor de exactamente `1200px` por `630px` con `overflow: hidden` y `margin: 0` en `body`. Contenido, sin texto propio: el H1 de Ari (`hero.h1.text` tal cual, como `h1` real, sin recortar ni reordenar), el logo oficial con `Logo.astro` en la variante y el tono que existan (el horizontal solo va sobre `light`) y el collage con una escena o piezas ya existentes de `src/components/collage/` (sin fotos nuevas, sin dibujar formas nuevas, sin hex: solo `var(--color-brand-*)` y tokens). Requisitos duros de diseño: el H1 cabe completo (que la pieza lo dimensione, no que lo recorte); tamaño mínimo del H1 de 64 px para que se lea a 600 px de ancho; el texto sobre su fondo es un par aprobado de `check-contrast.mjs` (por ejemplo morado sobre blanco, blanco sobre morado u oscuro sobre amarillo; nunca blanco o amarillo sobre naranja); margen de seguridad de 48 px en los cuatro lados (WhatsApp y LinkedIn recortan distinto); sin movimiento. Escribe en un comentario de cabecera las decisiones de diseño en una frase por decisión.

    4. Crea `scripts/og/export-og.mjs` (ESM, cabecera con requisitos y uso como `treat.mjs`). Uso: con el preview arriba (`npx astro build && npx astro preview --port 4322`), `node scripts/og/export-og.mjs [--url http://localhost:4322/marca/og/tarjeta/] [--out public/og.png]`. Pasos: (a) lanza `chromium` de `@playwright/test` con viewport `{ width: 1200, height: 630 }`, `deviceScaleFactor: 1`, `reducedMotion: 'reduce'` y bloqueo de toda petición que no sea `localhost`; (b) `page.goto(url)`, espera `document.fonts.ready` y comprueba que la fuente de marca cargó (si no, falla con mensaje en español: una captura con la fuente de respaldo no es la imagen de marca); (c) captura del elemento contenedor con `page.locator(...).screenshot({ type: 'png' })`; (d) `sharp(buffer).png({ palette: true, colors: 64, effort: 10, compressionLevel: 9 })` y ajusta `colors` hacia abajo (64, 48, 32, 24) hasta cumplir el tope; con `withMetadata` apagado (por defecto no escribe metadatos); (e) valida con la misma función que la guarda (`validateOgPng(buffer)`, exportada desde el script o desde `seo.mjs`, tú decides pero UNA sola implementación) que mide 1200x630 y pesa `OG_MAX_BYTES` o menos, y solo entonces escribe el archivo; si no cabe con 24 colores, falla con el peso obtenido en lugar de escribir. No lee `.env`, no hace peticiones externas y no toca `brand-inventory/`.

    5. Genera la imagen: `npx astro build && npx astro preview --port 4322` en segundo plano, `node scripts/og/export-og.mjs`, `npx astro preview stop`. Mira `public/og.png` con la herramienta Read (renderiza imágenes) a tamaño real y luego reducida a 600 px de ancho (`sharp` a un archivo temporal en el scratchpad, no en el repo). Ciclo visual con `impeccable`: hasta dos rondas de correcciones en un solo lote cada una; registra en el SUMMARY qué se corrigió. Si tras dos rondas algo sigue flojo, publícalo igual y anótalo como pregunta para Ari en el SUMMARY (no gastes contexto en pulido infinito).

    6. GREEN: corre la guarda de OG y la suite completa. Commitea `public/og.png` como binario versionado (Ari y el despliegue de la fase 4 lo necesitan sin regenerarlo).
  </action>
  <verify>
    <automated>node --test tests/guards/seo-og.test.mjs && node --test tests/guards/*.test.mjs && node scripts/check-contrast.mjs && (npx astro build >/dev/null 2>&1; test -f dist/marca/og/tarjeta/index.html && echo "tarjeta dev OK"; PUBLIC_ENV=production PUBLIC_SITE_URL=https://example.com npx astro build --outDir dist-seo/og-prod >/dev/null 2>&1; test ! -e dist-seo/og-prod/marca && test -f dist-seo/og-prod/og.png && echo "prod sin marca y con og.png OK")</automated>
  </verify>
  <acceptance_criteria>
    - `public/og.png` mide exactamente 1200x630 (IHDR), es PNG y pesa 204800 bytes o menos; sin bloques de metadatos.
    - `package.json` contiene `"sharp": "0.35.4"` en `devDependencies` y el script `og:export`.
    - `src/pages/marca/og/[card].astro` contiene 'isProduction ? []', 'noindex' y 'hero.h1', y no contiene `<script`, `set:html` ni hex.
    - `scripts/og/export-og.mjs` contiene 'OG_MAX_BYTES', 'marca/og/tarjeta' y 'document.fonts.ready'.
    - Un build de producción no contiene `dist/marca/` y sí `og.png`.
    - `git ls-files` no lista nada de `brand-inventory/`, ni `.ai`, ni PDF; `git status` limpio tras el commit salvo lo ajeno al plan.
    - Notas de las skills `impeccable` y `design-taste-frontend` registradas para el SUMMARY (una línea por skill).
  </acceptance_criteria>
  <done>
    Existe una imagen OG de marca de 1200x630 y menos de 200 KB, hecha con la skill de diseño, regenerable con un comando y protegida por una guarda de medidas y peso; la tarjeta que la produce no existe en producción. Commit atómico: `git add "src/pages/marca/og/[card].astro" scripts/og/export-og.mjs public/og.png package.json package-lock.json tests/guards/seo-og.test.mjs` y mensaje `feat(03-01): imagen OG 1200x630 con exportación reproducible`.
    Punto de corte: si el contexto se agota, deja hecho y commiteado el paso 2 (sharp declarado) más el RED de la guarda solo si pasa a verde en el mismo commit; si la tarjeta o la imagen quedan a medias, no commitees `public/og.png` incompleto: anota en el SUMMARY el estado exacto y que la tarea 3 y la 4 pueden continuar sin la imagen (la guarda de build exige `og.png` solo desde el paso final de la tarea 4).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: JSON-LD de cuatro nodos (Organization, ProfessionalService, WebSite, WebPage), serialización segura y excepción autorizada de set:html</name>
  <files>src/lib/schema-graph.mjs, src/components/JsonLd.astro, src/layouts/BaseLayout.astro, tests/guards/seo-jsonld.test.mjs, tests/guards/phase2-static.test.mjs, tests/guards/seo-build.test.mjs</files>
  <read_first>
    - `src/lib/seo.mjs` (de la tarea 1: `OG_WIDTH`, `OG_HEIGHT`, `OG_PATH`, `isIndexable`).
    - `src/layouts/BaseLayout.astro` completo (después de la tarea 1).
    - `tests/guards/phase2-static.test.mjs` líneas 1 a 75 y las pruebas de la regla 1 y de la regla 5 (líneas 125 a 175): cómo se declaran `RAW_HTML_ALLOW` y `SCRIPT_ALLOW` y cómo se prueban por mutación.
    - `src/content/landing.es.yaml` bloque `footer:` (líneas 567 a 590: `email` y `social` son `pending` con 'FALTA CONFIRMAR'; de ahí sale `sameAs` cuando exista).
    - `src/lib/content.ts` líneas 1 a 30 (`getLanding`).
    - `tests/guards/seo-build.test.mjs` (el ayudante `build` de la tarea 1).
  </read_first>
  <behavior>
    Pruebas (RED primero) en `tests/guards/seo-jsonld.test.mjs`, puras:
    - `buildGraph({ origin: 'https://example.com/', name, description, imageUrl, sameAs })` devuelve `{ '@context': 'https://schema.org', '@graph': [...] }` con exactamente cuatro nodos y los tipos `Organization`, `ProfessionalService`, `WebSite` y `WebPage`, en ese orden, con los `@id` `https://example.com/#organization`, `#service`, `#website` y `https://example.com/#webpage`, y con las referencias cruzadas de `<must_haves>`.
    - `name` de los cuatro nodos que lo llevan es el recibido; `description` del servicio y de la página es la recibida; `inLanguage` es `es` en `WebSite` y `WebPage`; `primaryImageOfPage.width` 1200 y `height` 630.
    - Prohibidos, recorriendo el grafo completo en profundidad: ningún `@type` en `Review`, `AggregateRating`, `Rating`, `Offer`, `SearchAction`; ninguna clave `review`, `aggregateRating`, `address`, `telephone`, `email`, `logo`, `potentialAction`. Mutación: un grafo copiado con un nodo `AggregateRating` y otro con la clave `address` son rechazados por `assertGraph`.
    - `sameAs`: `extractSameAs('FALTA CONFIRMAR', 'pending')` da `[]`; `extractSameAs('https://www.linkedin.com/company/loops-growth https://www.instagram.com/loops', 'verified')` da las dos URL en orden; descarta `javascript:alert(1)`, `http://x.com`, `ftp://x` y duplicados; con `pending` da `[]` aunque el texto traiga URL. Cuando `sameAs` es `[]` la clave no aparece en `Organization` (ni vacía).
    - `serializeJsonLd(obj)`: para un nombre `Loops </script><script>alert(1)</script> & Co` el resultado no contiene `<`, `>` ni `&` literales, contiene `<`, y `JSON.parse` devuelve el objeto original. U+2028 y U+2029 se escapan.
    - Estático: `JsonLd.astro` contiene `is:inline`, `type="application/ld+json"` y `serializeJsonLd`, y `set:html` aparece exactamente una vez.
    - `phase2-static`: `RAW_HTML_ALLOW` es exactamente `['src/components/JsonLd.astro']` y `SCRIPT_ALLOW['src/components/JsonLd.astro']` es 1 (una prueba que falla si la lista de autorizados crece sin editar la prueba).
    Ampliación de `seo-build.test.mjs`: en `prod` el HTML tiene exactamente un `<script type="application/ld+json">`, su contenido parsea, pasa `assertGraph`, `sameAs` no existe (`footer.social` está pending), los `@id` usan `https://example.com/`, y la description del grafo coincide con la `meta description` del head. En `dev` no hay JSON-LD.
  </behavior>
  <action>
    1. RED: crea `tests/guards/seo-jsonld.test.mjs` con lo de `<behavior>` y corre: falla porque `schema-graph.mjs` no existe. Para la parte de `phase2-static`, edita `tests/guards/phase2-static.test.mjs` de forma que `RAW_HTML_ALLOW` pase a `['src/components/JsonLd.astro']` y `SCRIPT_ALLOW` a `{ 'src/components/AgendaSection.astro': 2, 'src/components/JsonLd.astro': 1 }`, actualiza el comentario de cabecera (la regla 1 ya no es "vacía a propósito": explica la única excepción y su motivo) y añade la prueba que fija el tamaño de ambas listas. No debilites ninguna regla: las pruebas por mutación de la regla 1 y 5 deben seguir mordiendo (siguen usando cadenas sintéticas fuera de la ruta autorizada).

    2. Crea `src/lib/schema-graph.mjs` (ESM plano, JSDoc, sin imports de Astro). Exporta `buildGraph`, `assertGraph`, `extractSameAs`, `serializeJsonLd`, `FORBIDDEN_TYPES` y `FORBIDDEN_KEYS`. `buildGraph` no lleva ningún texto propio: todo llega por parámetros (nombre, description, URL del origen, URL de la imagen). `origin` se normaliza con `new URL('/', origin).href`. `serializeJsonLd` usa `JSON.stringify` y después reemplaza `<`, `>`, `&`, U+2028 y U+2029 por su secuencia `\uXXXX` en minúsculas. `assertGraph` lanza un Error en español que nombra el tipo o la clave prohibida y su ruta.

    3. Crea `src/components/JsonLd.astro`: recibe `{ name, description, sameAs }`, lee `siteUrl` de `../lib/site`, arma el grafo con `buildGraph` (imagen `new URL(OG_PATH, siteUrl).href`), lo valida con `assertGraph` (si falla, el build falla) y emite `<script is:inline type="application/ld+json" set:html={serializeJsonLd(graph)} />`. Comentario de cabecera: único `set:html` del proyecto, por qué es seguro (serialización escapada, cero texto del visitante) y que está autorizado por nombre en `phase2-static.test.mjs`. Si `siteUrl` no existe el componente no emite nada.

    4. Monta `JsonLd` en `BaseLayout.astro` dentro del head, solo cuando `indexable` es verdadero y llega `social` (la home): `{indexable && social && <JsonLd name={social.siteName} description={description ?? ''} sameAs={social.sameAs} />}`. Amplía la prop `social` con `sameAs?: string[]`. En `index.astro` calcula `sameAs = extractSameAs(data.footer.social.text, data.footer.social.status)` y pásalo. La description del grafo es la misma cadena de la meta description (una sola fuente: la prop `description`).

    5. GREEN y presupuesto: corre el build de producción con `--outDir dist-seo/prod` y confirma que `HTML_RAW_MAX` y `HTML_GZIP_MAX` (`tests/e2e/lib/budgets.mjs`, no se modifica) se cumplen con el JSON-LD y las metaetiquetas incluidos: `node --test tests/guards/html-budget.test.mjs tests/guards/brand-assets.test.mjs` sobre un `dist` reconstruido. Si el peso se pasa, recorta: el grafo va sin espacios (ya lo hace `JSON.stringify` sin sangría); no toques el tope. Amplía `seo-build.test.mjs` con lo de `<behavior>`.

    6. Valida el grafo con la herramienta externa de tu preferencia solo si está disponible sin red de pago (por ejemplo el MCP `schema-org-validate-jsonld` si la sesión lo tiene cargado); no es requisito. Anota el resultado en el SUMMARY.
  </action>
  <verify>
    <automated>node --test tests/guards/seo-jsonld.test.mjs tests/guards/phase2-static.test.mjs tests/guards/seo-build.test.mjs && npx astro build >/dev/null 2>&1 && node --test tests/guards/*.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/schema-graph.mjs` contiene 'Organization', 'ProfessionalService', 'WebSite', 'WebPage', 'FORBIDDEN_TYPES' y 'serializeJsonLd'.
    - `src/components/JsonLd.astro` contiene 'is:inline', 'application/ld+json' y exactamente una aparición de 'set:html'.
    - `tests/guards/phase2-static.test.mjs` contiene 'src/components/JsonLd.astro' en `RAW_HTML_ALLOW` y en `SCRIPT_ALLOW`, y `grep -rn "set:html" src` solo lista `src/components/JsonLd.astro` (más comentarios que lo nombren).
    - El HTML de producción de `/` en `dist-seo/prod` tiene un solo bloque JSON-LD con cuatro nodos y sin `AggregateRating`, `Review`, `address`, `telephone`, `email`, `logo` ni `sameAs`.
    - `node --test tests/guards/*.test.mjs` sale 0 y el presupuesto del HTML de `/` se cumple sin cambiar `budgets.mjs` (`git diff --stat tests/e2e/lib/budgets.mjs` vacío).
  </acceptance_criteria>
  <done>
    El HTML de producción lleva un grafo JSON-LD de cuatro nodos generado desde el YAML, imposible de romper con un texto malicioso y sin ningún dato inventado; la única excepción a la regla de no HTML en bruto está autorizada con nombre y limitada por una prueba. Commit atómico: `git add src/lib/schema-graph.mjs src/components/JsonLd.astro src/layouts/BaseLayout.astro src/pages/index.astro tests/guards/seo-jsonld.test.mjs tests/guards/phase2-static.test.mjs tests/guards/seo-build.test.mjs` y mensaje `feat(03-01): JSON-LD de cuatro nodos con serialización segura`.
    Punto de corte: si el contexto se agota, deja commiteado solo el par `schema-graph.mjs` más `seo-jsonld.test.mjs` (verde, sin montar nada) y anota en el SUMMARY que faltan `JsonLd.astro`, el montaje y la excepción de `phase2-static`; no montes el componente sin haber autorizado `set:html`, porque rompería la guarda existente.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: robots por entorno, sitemap sin páginas noindex, un solo H1 con landmarks y guarda de build completa</name>
  <files>src/lib/seo.mjs, src/pages/robots.txt.ts, astro.config.mjs, scripts/verify-env-surface.mjs, tests/guards/seo-head.test.mjs, tests/guards/seo-build.test.mjs, tests/e2e/seo-structure.spec.ts</files>
  <read_first>
    - `src/pages/robots.txt.ts`, `astro.config.mjs` y `src/lib/site.ts` (completos, son cortos).
    - `scripts/verify-env-surface.mjs` completo (se actualiza a la nueva regla de robots).
    - `src/lib/seo.mjs` (`SITEMAP_EXCLUDED_PATHS`, `isIndexable`) y `tests/guards/seo-build.test.mjs` (después de la tarea 3).
    - `.planning/phases/02-secciones-marca-y-copy/02-REVIEW.md`: solo el hallazgo WR-04 (líneas 152 a 162).
    - `tests/e2e/page-structure.spec.ts` líneas 180 a 215 (cómo ya prueba el h1 y las secciones, para no duplicar) y `tests/e2e/lib/` solo los nombres de archivo.
    - `playwright.config.ts` líneas 30 a 60 (proyecto `chromium`, `webServer`).
    - `src/components/SkipLinks.astro`, `src/components/SiteHeader.astro` (líneas 20 a 40) y `src/components/SiteFooter.astro` (líneas 25 a 50): solo para conocer el marcado de landmarks; no se modifican.
  </read_first>
  <behavior>
    Pruebas (RED primero):
    En `tests/guards/seo-head.test.mjs` (puras), bloque `buildRobots`:
    - `buildRobots({ isProduction: true, siteUrl: 'https://example.com/' })` devuelve texto que termina en salto de línea, contiene `User-agent: *` con `Allow: /`, un grupo propio con `Allow: /` para cada uno de `GPTBot`, `ClaudeBot`, `PerplexityBot` y `Google-Extended`, y la línea `Sitemap: https://example.com/sitemap-index.xml`; no contiene `Disallow: /`.
    - `buildRobots({ isProduction: false, siteUrl: 'https://example.com/' })` da exactamente `User-agent: *\nDisallow: /\n`: sin `Sitemap`, sin `Allow`, y lo mismo con `siteUrl` indefinida.
    - `buildRobots({ isProduction: true, siteUrl: undefined })` lanza un Error en español (producción sin URL no debe generar robots sin sitemap en silencio).
    - Mutación: quitar `Google-Extended` de la lista es detectado por la prueba que recorre los cuatro rastreadores.
    - `isSitemapExcluded('https://example.com/privacidad/')`, `.../marca/hoja/` y `.../marca/og/tarjeta/` son verdaderos; `https://example.com/` es falso; una ruta como `/privacidad-y-mas/` es falsa (compara por prefijo de segmento completo, no por subcadena).
    En `tests/guards/seo-build.test.mjs` (amplía; construye tres casos con `--outDir dist-seo/<caso>`):
    - `prod`: `robots.txt` igual a `buildRobots(prod)`; existen `sitemap-index.xml` y `sitemap-0.xml`; `sitemap-0.xml` contiene exactamente un `<loc>` igual a `https://example.com/`; no contiene `privacidad` ni `marca`; `og.png` existe en `dist` y cumple `OG_MAX_BYTES` y las medidas 1200x630; `dist/marca` no existe; `<html lang="es">`; exactamente un `<h1`, un `<main`, un `<header`, un `<footer`; todo `<nav` con `aria-label` no vacío; `dist/privacidad/index.html` tiene `noindex`, no tiene `rel="canonical"`, ni `og:`, ni JSON-LD.
    - `dev` (sin entorno): `robots.txt` es `User-agent: *\nDisallow: /\n`; no existe `sitemap-index.xml` ni `sitemap-0.xml`; el HTML de `/` tiene `noindex`.
    - `noindex` (producción con `PUBLIC_ENV=production` y `PUBLIC_SITE_URL` válida, evaluando `/privacidad/`): ya cubierto arriba; si el ayudante lo permite, no construyas un cuarto caso.
    - Mutación de la regla de landmarks: una función pura `checkLandmarks(html)` (dentro del propio archivo de prueba, exportada para probarla) rechaza un HTML sintético con dos `<h1`, uno con dos `<main` y uno con `<nav>` sin nombre.
  </behavior>
  <action>
    1. RED: añade los bloques de `<behavior>` a `tests/guards/seo-head.test.mjs` y `seo-build.test.mjs`. Corre: fallan por `buildRobots` y `isSitemapExcluded` inexistentes y por el sitemap con `privacidad`. No commitees en rojo.

    2. `src/lib/seo.mjs`: añade `AI_CRAWLERS = ['GPTBot', 'ClaudeBot', 'PerplexityBot', 'Google-Extended']`, `buildRobots({ isProduction, siteUrl })` y `isSitemapExcluded(url)` (parsea con `new URL`, compara el `pathname` contra `SITEMAP_EXCLUDED_PATHS` por prefijo de segmento). `buildRobots` en producción arma: `User-agent: *` / `Allow: /`, un bloque por rastreador de IA con su `Allow: /`, y `Sitemap: <new URL('sitemap-index.xml', siteUrl).href>`; fuera de producción devuelve `User-agent: *\nDisallow: /\n`. Comentario: por qué el no productivo pasa a `Disallow: /` (no se publica; el `noindex` del HTML queda como segunda barrera) y qué riesgo se acepta (una URL ya indexada por error no mostraría su `noindex` a Google mientras esté bloqueada; el despliegue de la fase 4 es siempre con `PUBLIC_ENV=production`).

    3. `src/pages/robots.txt.ts`: `GET` devuelve `buildRobots({ isProduction, siteUrl })` con `Content-Type: text/plain; charset=utf-8`. Elimina el comentario viejo que justificaba el `Allow` fuera de producción.

    4. `astro.config.mjs`: importa `isSitemapExcluded` de `./src/lib/seo.mjs` y cambia `sitemap()` a `sitemap({ filter: (page) => !isSitemapExcluded(page) })`. Mantén `isProduction && site ? [...] : []`. Quita de `privacidad.astro` la nota de "fase 3" solo si ese archivo la trae como pendiente de hacer: la nota pasa a decir que el filtro existe y que hay que retirar `/privacidad/` de `SITEMAP_EXCLUDED_PATHS` el día que exista el texto legal y se quite su `noindex` (es un comentario: el archivo está en `files_modified`? no; si lo cambias, añade `src/pages/privacidad.astro` a tu lista de `git add` y anótalo en el SUMMARY como desviación menor).

    5. `scripts/verify-env-surface.mjs`: actualiza las aserciones de robots a la regla nueva (no productivo: `Disallow: /` y sin `Sitemap`; producción: coincide con `buildRobots`) y añade la del sitemap sin `privacidad`. Corre `node scripts/verify-env-surface.mjs` hasta verde (usa `npx astro build` directo, así que no depende de las guardas de `prebuild`). Recuerda que ese script construye en `dist/`: reconstruye `dist` al final con `npx astro build` para dejar el estado normal.

    6. Un solo H1 y landmarks. Implementa `checkLandmarks(html)` (exportada desde `seo-build.test.mjs`) con las reglas de `<behavior>` y aplícala al HTML de `prod`. Crea `tests/e2e/seo-structure.spec.ts` (proyecto `chromium`, contra el preview del build no productivo): `page.locator('h1')` tiene 1 y está dentro de `#inicio`; `getByRole('banner')`, `getByRole('main')` y `getByRole('contentinfo')` tienen 1 cada uno; todo `nav` tiene nombre accesible (`aria-label` no vacío); `html[lang="es"]`; `title` de 60 caracteres o menos; hay `meta[name="description"]` de 155 o menos; hay `meta[name="robots"][content="noindex"]` y no hay `link[rel="canonical"]` (es el build no productivo); `og:title` existe y `og:image` no. Esto duplica a propósito lo mínimo de `page-structure.spec.ts` solo en el eje SEO; no copies sus otras aserciones.

    7. Corre toda la suite en el orden del `<verify>`, reconstruyendo y reiniciando el preview antes del e2e (regla del `global-setup`). Verifica a mano una vez que `dist-seo/prod/robots.txt` y `dist-seo/prod/sitemap-0.xml` se ven como esperas (`cat`) y anótalo en el SUMMARY.
  </action>
  <verify>
    <automated>node --test tests/guards/*.test.mjs && node scripts/check-contrast.mjs && node scripts/verify-env-surface.mjs && npx astro build >/dev/null 2>&1; g=$?; npx astro preview --port 4322 & sleep 3; E2E_BLOCK_CLICKUP=1 npx playwright test --project=chromium tests/e2e/seo-structure.spec.ts tests/e2e/page-structure.spec.ts tests/e2e/a11y-base.spec.ts; s=$?; npx astro preview stop; [ $g -eq 0 ] && [ $s -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/seo.mjs` contiene 'buildRobots', 'AI_CRAWLERS' con los cuatro nombres exactos ('GPTBot', 'ClaudeBot', 'PerplexityBot', 'Google-Extended') y 'isSitemapExcluded'.
    - `src/pages/robots.txt.ts` contiene 'buildRobots' y no contiene 'Allow: /' escrito a mano.
    - `astro.config.mjs` contiene 'isSitemapExcluded' dentro de `sitemap(` y sigue con `isProduction && site`.
    - `dist-seo/prod/sitemap-0.xml` lista una sola URL, `https://example.com/`; `dist-seo/dev/robots.txt` es `User-agent: *` más `Disallow: /`.
    - `node scripts/verify-env-surface.mjs` sale 0 con la regla nueva y `grep -n "Disallow" scripts/verify-env-surface.mjs` la reconoce como esperada en no productivo.
    - `tests/e2e/seo-structure.spec.ts` pasa con `E2E_BLOCK_CLICKUP=1` y `page-structure.spec.ts` sigue verde.
    - `node --test tests/guards/*.test.mjs` sale 0 y `git diff --stat tests/e2e/lib/budgets.mjs` está vacío.
  </acceptance_criteria>
  <done>
    robots y sitemap dependen del entorno y de la URL, el sitemap ya no contradice al `noindex` de `/privacidad/` (WR-04 cerrado), la landing tiene un solo H1 y landmarks con nombre vigilados en el HTML y en el navegador, y una guarda de build recorre los tres entornos completos. Commit atómico: `git add src/lib/seo.mjs src/pages/robots.txt.ts astro.config.mjs scripts/verify-env-surface.mjs tests/guards/seo-head.test.mjs tests/guards/seo-build.test.mjs tests/e2e/seo-structure.spec.ts` y mensaje `feat(03-01): robots y sitemap por entorno, un solo H1 y landmarks vigilados`.
    Punto de corte: si el contexto se agota, commitea en este orden lo que esté verde: (a) `seo.mjs` con `buildRobots` más `robots.txt.ts` más su prueba pura, (b) el filtro del sitemap en `astro.config.mjs`, (c) el spec e2e. Anota en el SUMMARY cuál falta; ninguna de las tres depende de las otras. Cierra siempre con `npx astro preview stop` y con un `dist/` reconstruido.
  </done>
</task>

</tasks>

<threat_model>

Límites de confianza: el YAML (lo edita el equipo, incluso desde la web de GitHub) entra al `<head>` y al JSON-LD; los rastreadores públicos leen `robots.txt`, el sitemap y el grafo; las páginas internas de marca (`/marca/`) solo existen fuera de producción; el navegador headless de la exportación solo debe hablar con `localhost`.

| ID | Categoría STRIDE | Amenaza | Disposición | Mitigación en este plan |
|----|------------------|---------|-------------|--------------------------|
| T-03-01 | Tampering (inyección) | Un texto del YAML con `</script>` cierra el bloque JSON-LD y ejecuta HTML | mitigate | `serializeJsonLd` escapa `<`, `>`, `&`, U+2028 y U+2029; prueba de mutación con un nombre malicioso; `set:html` limitado a `JsonLd.astro` por `phase2-static.test.mjs` |
| T-03-02 | Tampering (atributos) | Un salto de línea o una comilla en `title` o `description` rompe una metaetiqueta | mitigate | Astro escapa los atributos; además `checkTitle` y `checkDescription` rechazan saltos de línea y la guarda de build prueba que el head parsea con un solo `description` |
| T-03-03 | Information disclosure | `/marca/hoja/` y `/marca/og/tarjeta/` (herramientas internas) llegan a producción y se indexan | mitigate | `getStaticPaths` devuelve `[]` con `isProduction`; la guarda de build falla si existe `dist/marca`; el sitemap las excluye por prefijo; llevan `noindex` |
| T-03-04 | Information disclosure | Los originales confidenciales del BrandBook (`.ai`, PDF, `brand-inventory/`) terminan en el repositorio o en la imagen OG | mitigate | La tarjeta usa solo componentes y SVG ya versionados; `git add` con listas explícitas; se comprueba `git ls-files` sin esas rutas |
| T-03-05 | Spoofing / claims falsos | Datos estructurados con reseñas, puntuaciones, dirección o teléfono inventados (riesgo de acción manual de Google y de credibilidad) | mitigate | `FORBIDDEN_TYPES` y `FORBIDDEN_KEYS` en `assertGraph` (el build falla); `sameAs` solo desde `footer.social` verificado; sin `logo` ni `email` hasta que Ari los entregue |
| T-03-06 | Repudio de copy / integridad | Un title o description `pending` (propuesta sin aprobar) llega a producción sin que Ari lo apruebe | mitigate | Nacen `pending` con `confirm_by: Ari`; el `prebuild` con `PUBLIC_ENV=production` sigue bloqueando cualquier `pending`; la propuesta reutiliza el subtítulo de Ari sin cambios |
| T-03-07 | Elevation (mala configuración) | Un entorno de pruebas o de vista previa se indexa por error | mitigate | No productivo: `noindex`, sin canonical, sin JSON-LD, sin URLs absolutas de imagen y robots `Disallow: /`; guardas de build por entorno |
| T-03-08 | Denial (indexación) | `/privacidad/` en el sitemap con `noindex` (WR-04) genera el aviso "URL enviada marcada noindex" | mitigate | `isSitemapExcluded` en el filtro del sitemap; guarda que exige una sola URL en `sitemap-0.xml` |
| T-03-09 | Denial (rastreo) | `robots.txt` con `Disallow: /` llega a producción y borra la landing del índice | mitigate | `buildRobots` falla en producción sin URL; la guarda compara `robots.txt` de producción con `buildRobots(prod)` y prueba que no contiene `Disallow: /` |
| T-03-10 | Tampering (cadena de suministro) | La exportación de la imagen hace peticiones externas o usa una fuente de respaldo sin avisar | mitigate | El script bloquea toda petición fuera de `localhost`, espera `document.fonts.ready` y falla si la fuente de marca no cargó; no lee `.env` |
| T-03-11 | Riesgo aceptado | Con `Disallow: /` en no productivo, Google no vería el `noindex` de una URL no productiva ya indexada | accept | Los entornos no productivos no se publican ni se comparten; el despliegue de la fase 4 es siempre `PUBLIC_ENV=production`. Ver la pregunta abierta sobre este cambio |

</threat_model>

<not_doing>

- No hay `Review`, `AggregateRating`, `Person`, `FAQPage`, `SearchAction`, `BreadcrumbList` ni `Service` con precios: los cuatro tipos exactos de la fase y nada más (`Person` y `FAQPage` son SEO-04, fuera de esta fase).
- No se agregan dirección, teléfono, correo, `logo` ni `sameAs` inventados: solo cuando Ari los entregue y queden `verified`.
- No se redacta copy: title y description son propuestas `pending` para Ari; el alt de la imagen usa dos afirmaciones que ya existen.
- No se usan integraciones SEO (`astro-seo` y similares): el head es código propio (decisión de STACK.md).
- No hay `llms.txt`, `hreflang`, manifiesto web, `twitter:site`, `og:image` por página ni imágenes OG distintas por red social.
- No se mide nada ni se carga GA4 (plan 03-02) y no se corre Lighthouse ni axe ni la caminata de teclado (plan 03-03).
- No se toca el marcado de secciones, el copy de las secciones, `budgets.mjs`, las fuentes ni el favicon.
- No se cambian los estados `pending` de ningún texto ni se aprueba nada en nombre de Ari; tampoco se ejecuta un build de producción completo con `npm run build` (sigue bloqueado por las guardas mientras haya copy pendiente, a propósito).
- No se despliega, no se define el dominio y no se genera el QR (fase 4). El `example.com` de las guardas es solo un dato de prueba.
- No se escribe el texto legal de `/privacidad/` ni se le quita el `noindex`.

</not_doing>

<verification>
Antes de dar el plan por completo (todo desde la raíz del repositorio):

- [ ] `node --test tests/guards/*.test.mjs` sale 0 (incluye `seo-head`, `seo-jsonld`, `seo-og`, `seo-build`, `phase2-static`, `html-budget` y `brand-assets`).
- [ ] `node scripts/check-contrast.mjs` sale 0; `node scripts/check-copy.mjs` no reporta errores estructurales y `npm run pending` lista `meta.title` y `meta.description` para Ari.
- [ ] `node scripts/verify-env-surface.mjs` sale 0 con la regla nueva de robots.
- [ ] `npx astro build` pasa y, con el preview en 4322, `E2E_BLOCK_CLICKUP=1 npx playwright test --project=chromium tests/e2e/seo-structure.spec.ts tests/e2e/page-structure.spec.ts tests/e2e/a11y-base.spec.ts` pasa; después `npx astro preview stop`.
- [ ] `PUBLIC_ENV=production PUBLIC_SITE_URL=https://example.com npx astro build --outDir dist-seo/prod`: un canonical, un JSON-LD de cuatro nodos, `og.png` de 1200x630 y 204800 bytes o menos, `sitemap-0.xml` con una sola URL, robots con los cuatro rastreadores de IA y sin `dist/marca`.
- [ ] `git status --short` solo muestra archivos ajenos al plan; `git ls-files | grep -E '\.ai$|\.pdf$|brand-inventory/'` no devuelve nada.
- [ ] Sin guion largo ni medio (U+2014 y U+2013) en los archivos nuevos y modificados por este plan, y sin "AEO" en ningún texto nuevo.
</verification>

<success_criteria>

- SEO-01: `/` de producción tiene title de 60 o menos, description de 155 o menos, un canonical, `og:*` y `twitter:*` completos y `og.png` de 1200x630 con peso dentro del presupuesto y `og:image:alt`.
- SEO-02: el HTML de producción trae un JSON-LD con exactamente `Organization`, `ProfessionalService`, `WebSite` y `WebPage`, sin `Review` ni `AggregateRating`, serializado de forma segura.
- SEO-03: un solo `h1` y landmarks con nombre vigilados; `robots.txt` y sitemap cambian con el entorno; `/privacidad/` y `/marca/` fuera del sitemap (WR-04 cerrado).
- Title y description quedan como propuestas `pending` visibles para Ari; ningún texto de cara al visitante se inventó ni se humanizó.
- Ninguna guarda ni prueba previa se rompió y ningún presupuesto se subió.
- Cada tarea quedó en su propio commit atómico con lista explícita de archivos.
</success_criteria>

<output>
Al terminar, crea `.planning/phases/03-seo-medicion-minima-y-qa/03-01-SUMMARY.md` con: qué se hizo por tarea, las decisiones de las skills `impeccable` y `design-taste-frontend` sobre la imagen OG (una línea por skill), el peso final de `og.png`, el peso del HTML de `/` antes y después, los resultados de cada verificación, las desviaciones y una lista de pendientes para Ari: (1) aprobar o reemplazar `meta.title` y `meta.description`, (2) aprobar la imagen OG, (3) entregar redes para `sameAs`, y dirección, teléfono, correo y logo si quiere que salgan en el schema, (4) confirmar `es_LA` como idioma de Open Graph.
</output>
