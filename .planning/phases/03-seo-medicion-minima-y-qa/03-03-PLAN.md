---
phase: 03-seo-medicion-minima-y-qa
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - package.json
  - package-lock.json
  - .gitignore
  - playwright.config.ts
  - scripts/audit.mjs
  - scripts/lib/audit-rules.mjs
  - tests/guards/audit.test.mjs
  - tests/guards/a11y-docs.test.mjs
  - tests/guards/keyboard-safety.test.mjs
  - tests/e2e/lib/isolated-context.ts
  - tests/e2e/phase-closing.spec.ts
  - tests/e2e/keyboard-walk.spec.ts
  - tests/e2e/keyboard-live.spec.ts
  - docs/a11y/REPORT.md
  - docs/a11y/EXCEPTIONS.md
  - docs/a11y/A11Y-DECISIONS.md
  - docs/a11y/VOICEOVER-CHECKLIST.md
  - docs/a11y/LIGHTHOUSE.md
  - docs/a11y/lighthouse-summary.json
  - src/**   # SOLO correcciones de hallazgos de la tarea 3; cada archivo tocado se lista en el SUMMARY
autonomous: true
requirements: [A11Y-01, A11Y-02, PERF-01, SEO-03]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "`npm run audit` existe y hace, en este orden: (1) refuse si estás en un contexto de despliegue con el modo local, (2) corre las tres puertas de producción y decide el modo, (3) construye con `PUBLIC_ENV=production` y `PUBLIC_SITE_URL=http://localhost:4323` en `dist-audit/` (nunca en `dist/`), (4) sirve con `astro preview --outDir dist-audit --port 4323`, (5) corre Lighthouse móvil y (6) sale distinto de 0 si el rendimiento (mediana de 3 corridas) es menor a 95, o la accesibilidad o el SEO (mínimo de las 3) es menor a 100. Los umbrales 95, 100 y 100 son constantes de `scripts/lib/audit-rules.mjs` y una guarda los fija literalmente: bajarlos para que pase rompe la prueba."
    - "Cómo se audita mientras Ari no responde (decisión de este plan, cierra la línea de STATE 'definir en plan-phase 3'): las puertas `check-copy.mjs` y `check-photos.mjs` NO se tocan y siguen bloqueando `PUBLIC_ENV=production` mientras haya pendientes. El único permiso para medir con pendientes es la bandera de línea de comandos `--allow-pending` de `scripts/audit.mjs` (no una variable de entorno, porque una variable se hereda por procesos hijos y puede llegar a un build de hosting; una bandera solo existe en la línea que la escribe). Sin la bandera y con pendientes, `npm run audit` imprime la lista y sale 1. Con la bandera: se rechaza con salida 2 si hay variable de contexto de despliegue (`CI`, `GITHUB_ACTIONS`, `CF_PAGES`, `VERCEL`, `NETLIFY`, `GITLAB_CI`, `BUILDKITE`, `RENDER`); se rechaza siempre (con o sin bandera) si falla `check-contrast` o una violación estructural de `check-copy` o de `check-photos` (registro incoherente), porque eso no es un pendiente sino un defecto; y el resultado queda marcado como no desplegable (archivo `dist-audit/AUDIT-NOT-DEPLOYABLE.txt`, carpeta que ningún despliegue publica, y `mode: local-pending` en el resumen versionado). Cuando no queden pendientes, las puertas pasan solas, el modo es `strict` y la bandera se ignora con un aviso."
    - "Lo que prueba que el permiso no debilita nada: `tests/guards/audit.test.mjs` verifica (a) que con pendientes reales `PUBLIC_ENV=production node scripts/check-copy.mjs` y `node scripts/check-photos.mjs` siguen saliendo 1 y `npm run build` con `PUBLIC_ENV=production` sigue fallando, (b) que la cadena `allow-pending` no aparece en `package.json` fuera del script `audit`, ni en `astro.config.mjs`, ni en `scripts/check-*.mjs`, (c) que `audit.mjs` lanza procesos con lista de argumentos (sin `shell: true`) y solo escribe el build en `dist-audit`, y (d) que `node scripts/audit.mjs --allow-pending` con `CI=1` sale 2 sin construir nada."
    - "WR-01 (las puertas viven solo en `prebuild` y `postbuild`, así que `astro build` directo las salta): decisión de este plan, NO mover las puertas a una integración de Astro ahora. Razones medidas: (1) las pruebas de 03-01 y de la fase 2 construyen a propósito con `PUBLIC_ENV=production PUBLIC_SITE_URL=... npx astro build` para verificar robots, sitemap, JSON-LD y que `/marca` no se publica; una integración que bloquee esa forma las rompería todas mientras haya pendientes, y para evitarlo necesitaría su propia bandera de escape, un segundo permiso igual al de la auditoría; (2) la auditoría necesita construir con producción y pendientes, y se apoya en que `astro build` directo no las corre, pero lo hace de forma explícita y visible (las corre antes y decide el modo), no por accidente; (3) el riesgo real está en el despliegue, que es de la fase 4. Queda como arrastre obligatorio para LNCH-01: el comando de build del hosting debe ser exactamente `npm run build`, y el lockfile es de npm. Este plan deja escrito el arrastre en el SUMMARY y en `A11Y-DECISIONS.md`."
    - "WR-06 (`sharp` sin declarar): decisión del usuario, no se instala nada. Este plan no agrega `sharp` a `package.json`. La única dependencia nueva es `lighthouse` 13.5.0 exacta en `devDependencies`, con la auditoría de legitimidad de paquetes que se hizo para `@axe-core/playwright` en 02-08 (`gsd-tools query package-legitimacy check --ecosystem npm lighthouse`), sin script de instalación y con `git diff --stat HEAD -- package.json` mostrando solo esa línea más los scripts nuevos. Lighthouse 13.5.0 exige Node 22.19 o más y el equipo tiene 24.13.0; `engines` del proyecto (>=22.12.0) no se cambia y la diferencia queda anotada en `A11Y-DECISIONS.md`."
    - "Cómo trata la auditoría las páginas con `FALTA CONFIRMAR` y la foto pendiente: mide la página real tal como está, con los marcadores visibles. Es la medición honesta de hoy y no se maquilla: el resumen versionado registra cuántos textos pendientes había (`node scripts/check-copy.mjs --json`), qué errores de fotos y el modo. Cuando Ari entregue el copy y apruebe la foto se vuelve a correr y el modo pasa a `strict`; hasta entonces `REPORT.md` no puede declarar nada mejor que CONDITIONAL y `lighthouse-summary.json` con `mode: local-pending` no cuenta como evidencia de salida para la fase 4."
    - "Qué mide la auditoría del iframe de ClickUp (la fase 1 midió cerca de 55 MB de JavaScript descomprimido y `load` de 4,7 s del formulario real). Dos corridas y nunca una sola cifra: (A) compuerta: URL `/`, red real, iframe tal como se sirve (`loading=lazy`). Lighthouse no hace scroll, así que el formulario no se pide (solo el `v1.js` asíncrono de 25 KB), y esa es la experiencia del primer pantallazo, que es lo que el criterio 3 de la fase promete. Se comprueba y se registra si hubo peticiones a `forms.clickup.com` en la corrida (si las hubo, el resumen lo dice y la corrida deja de ser 'primer pantallazo'). (B) informativa, jamás compuerta: URL `/#agenda`, el navegador baja al ancla, el iframe lazy carga y Lighthouse mide el costo real de llegar al formulario: rendimiento, peso total y bytes de JavaScript. Ambas cifras van una junto a la otra en `docs/a11y/LIGHTHOUSE.md`. Bloquear el iframe en Lighthouse no se usa: mediría una página que ningún visitante recibe al bajar. El bloqueo (`E2E_BLOCK_CLICKUP`) se usa en las pruebas de navegador porque ahí sí se quiere aislar el HTML propio."
    - "Dónde se guardan los resultados de Lighthouse: los JSON completos (unos 0,5 MB cada uno) en `test-results/audit/` (ya ignorada por git); lo versionado es un resumen pequeño, `docs/a11y/lighthouse-summary.json` (puntajes por corrida, mediana y mínimos, LCP, TBT, CLS, FCP, peso total y JavaScript, terceros, corrida B, modo, umbrales, versión de Lighthouse, `git rev-parse HEAD` y si el árbol estaba sucio) y su versión legible `docs/a11y/LIGHTHOUSE.md`. Solo se escriben con `--record`, para que correr `npm run audit` a diario no ensucie el árbol."
    - "axe a 320, 390 y 1280 px: se extiende el bloque `axe de humo` de `tests/e2e/phase-closing.spec.ts` (no se crea otro): se agrega la etiqueta `best-practice`, la corrida de `/` a 390 px con todos los `details` del FAQ abiertos, y la regla de qué se tolera. Falla ante `critical` o `serious`. Los `moderate` y `minor` se anotan y solo se acepta la lista `ACCEPTED_MODERATE`. Hallazgo `landmark-unique` (tres secciones se llaman 'FALTA CONFIRMAR'): no se corrige tocando nombres ni inventando títulos, porque el nombre sale del copy pendiente de Ari (regla del proyecto: no se inventa texto). Se tolera solo mientras la página muestre dos o más regiones con ese mismo nombre y la prueba falla si la regla persiste con los títulos reales de Ari. Queda anotado en `REPORT.md` con fecha de revisión."
    - "Recorrido de teclado automatizado (`tests/e2e/keyboard-walk.spec.ts`, proyecto `chromium`, sirve con y sin `E2E_BLOCK_CLICKUP`): a 1280 y 390 px la secuencia de Tab de `/` es exactamente la de las paradas de foco del DOM en orden (sin `tabindex` positivo), la primera parada es el enlace 'Saltar al contenido', el skip link mueve el foco a `#main`, cada parada tiene nombre accesible, contiene su texto visible en el nombre (SC 2.5.3), tiene contorno visible según la regla global `:focus-visible` y no queda tapada por otro elemento (SC 2.4.11), el enlace de respaldo del formulario está antes del iframe, después del iframe la siguiente parada es un elemento de la página (sin trampa) y el recorrido inverso con Shift+Tab reproduce la secuencia al revés. `/privacidad/` recibe el mismo recorrido."
    - "Recorrido con el iframe real (`tests/e2e/keyboard-live.spec.ts`, proyecto `live`, se agrega a `LIVE_SPECS` en `playwright.config.ts`): con el formulario real de ClickUp cargado, se entra al iframe con Tab, se cuentan las paradas dentro (lectura de solo lectura del foco interno con la API de frames de Playwright), se sale con Tab hacia el pie y se vuelve con Shift+Tab, con tope de 80 pulsaciones por sentido (más que eso es trampa de foco). Si el formulario no carga por falta de red, la prueba se omite con `test.skip` y el motivo queda en `REPORT.md` como punto `[ ]` (nunca un pase silencioso). Esta prueba NUNCA llena, hace clic, envía ni pulsa Enter o Espacio en el formulario: solo Tab y Shift+Tab y lecturas. `tests/guards/keyboard-safety.test.mjs` lo impide leyendo el código fuente de la prueba."
    - "Documentos versionados, nunca en `.gitignore` (lo verifica `tests/guards/a11y-docs.test.mjs` con `git check-ignore`): `docs/a11y/REPORT.md` con las secciones de la plantilla oficial de A11Y.md (contexto de validación, 1 a 6, notas), estado CONDITIONAL y verificación `self-reported` (nunca PASS mientras quede un `[ ]` o `[!]`); `docs/a11y/EXCEPTIONS.md` con el iframe de ClickUp (dueño persona, quien aprueba, seguimiento, vencimiento 2026-12-19 y enlace de respaldo como mitigación) más las excepciones de la fase 2 (mesas 17 y 22 de logo con aro morado sobre oscuro 1.88, y el relleno naranja decorativo sobre morado 2.95); `docs/a11y/A11Y-DECISIONS.md` (una línea por patrón, indexada por patrón); `docs/a11y/VOICEOVER-CHECKLIST.md` para Camila o Juan (VoiceOver con Safari en macOS y en iPhone, porque el QR abre la página en el móvil) y los dos archivos de Lighthouse."
    - "Sin cambios de copy ni de diseño: el diff de todo el plan contra `plan_head_before` no lista archivos de `src/content`, `PENDING-COPY.md`, `src/assets/brand` ni `src/assets/photos`, y `node scripts/list-pending.mjs --check` sale 0. Las correcciones de la tarea 3 se limitan a defectos de accesibilidad, SEO técnico o rendimiento con causa medida. Lo que exija decisión de diseño o cambiar cómo carga el formulario se registra y se pide a Juan, no se hace en silencio."
    - "Prohibido en todo el plan: enviar, llenar o hacer clic en el formulario real de ClickUp, leer `.env`, versionar `.planning/phases/02-secciones-marca-y-copy/brand-inventory/` (confidencial), `git add -A` o `git add .`, bajar un umbral, agregar `sharp`, agregar la variable de entorno de permiso, y modificar `scripts/check-copy.mjs`, `scripts/check-photos.mjs` o `scripts/check-contrast.mjs`."
    - statement: "Juan abre `docs/a11y/LIGHTHOUSE.md` y ve, lado a lado, el puntaje del primer pantallazo (compuerta) y el costo real de llegar al formulario (corrida B), con la etiqueta de modo (local-pending o strict) y sin ninguna cifra escondida"
      verification: backstop
  artifacts:
    - path: scripts/lib/audit-rules.mjs
      provides: "Reglas puras y probadas: contexto de despliegue, modo de auditoría, umbrales, mediana y mínimos, resumen de red y render del resumen"
      contains: "decideMode"
    - path: scripts/audit.mjs
      provides: "Orquestador de `npm run audit`: puertas, build en dist-audit, preview, Lighthouse móvil y resumen"
      contains: "dist-audit"
    - path: tests/guards/audit.test.mjs
      provides: "Guarda del modo local, de los umbrales y de que las puertas de producción siguen bloqueando"
      contains: "allow-pending"
    - path: tests/e2e/keyboard-walk.spec.ts
      provides: "Recorrido de Tab de `/` y `/privacidad/` con entrada y salida del iframe bloqueado"
      contains: "Shift+Tab"
    - path: tests/e2e/keyboard-live.spec.ts
      provides: "Recorrido de Tab con el formulario real de ClickUp, solo lectura"
      contains: "forms.clickup.com"
    - path: tests/guards/keyboard-safety.test.mjs
      provides: "Prohíbe llenar, hacer clic o enviar en la prueba con el formulario real"
      contains: "keyboard-live"
    - path: docs/a11y/REPORT.md
      provides: "Informe de accesibilidad self-reported, CONDITIONAL, con evidencia reproducible por punto"
      contains: "self-reported"
    - path: docs/a11y/EXCEPTIONS.md
      provides: "Registro de excepciones con el iframe de ClickUp y las de marca de la fase 2"
      contains: "2026-12-19"
    - path: docs/a11y/A11Y-DECISIONS.md
      provides: "Memoria de patrones de accesibilidad de este proyecto"
      contains: "Decisions"
    - path: docs/a11y/VOICEOVER-CHECKLIST.md
      provides: "Lista paso a paso de VoiceOver con Safari para Camila o Juan"
      contains: "VoiceOver"
    - path: docs/a11y/lighthouse-summary.json
      provides: "Resumen versionado de la última corrida de Lighthouse con modo y corrida B"
      contains: "mode"
  key_links:
    - from: scripts/audit.mjs
      to: scripts/lib/audit-rules.mjs
      via: "decideMode, evaluateScores y renderSummary puras y probadas"
      pattern: "audit-rules"
    - from: scripts/audit.mjs
      to: scripts/check-copy.mjs
      via: "las puertas se ejecutan como procesos hijos sin modificarlas; `--json` separa lo estructural de lo pendiente"
      pattern: "check-copy"
    - from: package.json
      to: scripts/audit.mjs
      via: "script `audit`"
      pattern: "scripts/audit.mjs"
    - from: tests/e2e/keyboard-walk.spec.ts
      to: tests/e2e/lib/isolated-context.ts
      via: "contexto con toda petición externa abortada, compartido con el bloque axe"
      pattern: "isolated-context"
    - from: playwright.config.ts
      to: tests/e2e/keyboard-live.spec.ts
      via: "LIVE_SPECS incluye el recorrido con el formulario real"
      pattern: "keyboard-live"
    - from: docs/a11y/REPORT.md
      to: docs/a11y/EXCEPTIONS.md
      via: "cada punto `[!]` o `[~]` enlaza su excepción"
      pattern: "EXC-"
---

<objective>
Verificar con evidencia, no con afirmaciones, que la landing cumple el criterio 3 y el criterio 4 de la fase 3: Lighthouse móvil (rendimiento 95 o más, accesibilidad 100, SEO 100) sobre un build de producción servido en local, axe sin violaciones serias ni críticas a 320, 390 y 1280 px, recorrido completo con teclado incluida la entrada y la salida del iframe de ClickUp, y los documentos `docs/a11y/REPORT.md`, `EXCEPTIONS.md` y `A11Y-DECISIONS.md` versionados.

Purpose: la página vende SEO técnico y sale a un evento donde la gente escanea un QR con el móvil. Además la auditoría tiene un problema de diseño que este plan resuelve: las puertas de producción bloquean `PUBLIC_ENV=production` mientras Ari no entregue 61 textos y no apruebe la foto, y la fase promete medir justo ese build. La solución es un modo local explícito, que se rechaza en despliegue, que deja el resultado marcado como no desplegable y que se prueba con guardas para que el build de producción real siga fallando mientras haya pendientes.

Output: `npm run audit` (Lighthouse 13.5.0 exacto), guardas del modo local y de los umbrales, bloque axe extendido, recorrido de teclado con y sin iframe bloqueado y con el iframe real, cinco documentos en `docs/a11y/` y el resumen de Lighthouse versionado. Correcciones de hallazgos solo con causa medida.

Discovery (nivel 0 con una dependencia nueva): todo el trabajo sigue patrones de las fases 1 y 2 (Playwright con `E2E_BLOCK_CLICKUP`, guardas de Node con `node --test`, `astro preview` en segundo plano, `git add` con lista explícita). La única instalación es `lighthouse` con la auditoría de legitimidad de 02-08. Las plantillas de A11Y.md se descargaron una vez al planificar (fuente: https://github.com/fecarrico/A11Y.md/tree/main/docs/en/templates, REPORT, EXCEPTIONS y A11Y-DECISIONS); la tarea 4 las vuelve a intentar con un solo `curl --max-time 20` cada una y, si no responden, usa las secciones de este plan (copian la plantilla) y lo anota.
</objective>

<!-- planner-discipline-allow: data-tone -->

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-seo-medicion-minima-y-qa/03-CONTEXT.md
@package.json
@playwright.config.ts

<!-- Lecturas por tarea: cada tarea lista sus archivos exactos en `read_first`. NO cargues completos 02-UI-SPEC.md (73 KB), 02-VERIFICATION.md ni 02-REVIEW.md: usa grep -n y Read con offset y limit. -->
<!-- Lo que ya se investigó al planificar (no repetir):
     - Ramas de la puerta: `scripts/check-copy.mjs` con `--json` imprime `{ production, structural: [...], content: [...] }`; `structural` bloquea en cualquier entorno y `content` solo con `PUBLIC_ENV=production`. `scripts/check-photos.mjs` no separa clases en su código de salida: se separa corriéndolo primero con `PUBLIC_ENV=development` (si sale 0 el registro es coherente y cualquier fallo en producción es de aprobación pendiente) y luego con `production`. Ambos leen el entorno con `loadEnv` más `process.env` y solo el valor exacto `production` bloquea.
     - `astro build`, `astro preview` y `astro dev` aceptan `--outDir` (verificado en `node_modules/astro/dist/cli/flags.js`, línea 14). Por eso el build de auditoría va a `dist-audit/` y no pisa el `dist/` que usan Playwright y cualquier despliegue.
     - Playwright: `webServer` falla dentro de agentes. Se construye y sirve a mano (`npx astro build && npx astro preview --port 4322`), Astro 7 deja el preview en segundo plano si detecta un agente, y se detiene con `npx astro preview stop`. `tests/global-setup.ts` falla si `dist` es más viejo que `src`, `public` o `astro.config.mjs`.
     - Bloque axe existente: `tests/e2e/phase-closing.spec.ts` líneas 101 a 128 (`AXE_TAGS` sin `best-practice`, `.exclude('iframe')`, `/` y `/privacidad/` a 320, 390 y 1280, falla ante critical o serious). La función `isolatedContext(browser, baseURL, width, height, opts)` que aborta toda petición no local está en el mismo archivo poco antes (líneas 60 a 99).
     - Hallazgo `landmark-unique` (moderate, best-practice) de 02-VERIFICATION: `#solucion`, `#para-quien` y `#faq` se llaman igual ('FALTA CONFIRMAR').
     - Formulario: `src/components/AgendaSection.astro` líneas 20 a 62. Orden en el DOM: h2 con `tabindex="-1"`, enlace de respaldo (`a.agenda-link`, `target=_blank`), collage (`aria-hidden`), `iframe.clickup-embed` (`loading=lazy`, con `title`), script `is:inline async` del `v1.js`, `noscript` con el mismo enlace. `SkipLinks.astro` trae dos enlaces: `#main` y `#agenda`.
     - Medición de la fase 1 (STATE.md línea 133 y `.planning/phases/01-fundaciones-y-formulario-funcionando/01-FORM-MEASUREMENTS.md`): formulario real cerca de 55 MB de JS descomprimido y `load` de 4,7 s; la página propia pesa 66 KB y da FCP de 48 ms.
     - Chromium de Playwright ya está instalado en `~/Library/Caches/ms-playwright`; Lighthouse lo usa con `CHROME_PATH` tomado de `chromium.executablePath()`. No se descarga ningún navegador.
     - Plantillas de A11Y.md (descargadas al planificar): REPORT tiene la leyenda `[x] [!] [~] [ ]`, el contexto (Feature, fecha, cubre la interfaz de, estado, independencia de la verificación) y los bloques 1 a 7; EXCEPTIONS pide por entrada Exception ID, componente, criterio WCAG, severidad, dueño (persona), quien aprueba, seguimiento, qué está roto, por qué, mitigación, vencimiento y criterio de resolución; A11Y-DECISIONS es una lista de una línea por patrón. -->
</context>

<not_doing>
Lista de lo que este plan NO hace (cada punto tiene su razón):
- No mueve las puertas a una integración de Astro (WR-01): rompería las pruebas de 03-01 y de la fase 2 que construyen con producción a propósito y exigiría un segundo permiso de escape. Arrastre a la fase 4: el hosting usa `npm run build`.
- No agrega `sharp` (WR-06): decisión del usuario.
- No crea una variable de entorno de permiso (por ejemplo `AUDIT_BYPASS_GATES`): se hereda por procesos hijos. Solo existe la bandera `--allow-pending`, y solo en `scripts/audit.mjs`.
- No modifica `check-copy.mjs`, `check-photos.mjs` ni `check-contrast.mjs`.
- No baja ningún umbral (95, 100, 100) ni acepta un `moderate` que no esté listado con razón.
- No envía, llena ni hace clic en el formulario de ClickUp. Tampoco lo bloquea en Lighthouse.
- No mide la página desplegada ni con PageSpeed Insights (fase 4, con URL pública). No configura CI (fase 4).
- No agrega GA4 ni toca medición (es el plan 03-02).
- No cambia copy, diseño, logos ni fotos. No inventa títulos para arreglar `landmark-unique`.
- No prueba NVDA ni JAWS (Windows): la checklist cubre VoiceOver con Safari en macOS e iOS; el par que falte queda como `[ ]` en el informe.
- No analiza el interior del iframe con axe (origen cruzado, no es nuestro DOM). Se registra en EXCEPTIONS y se recorre con Tab.
- No agrega overlays de accesibilidad, Pa11y ni WAVE.
- No cambia `engines` de `package.json`.
</not_doing>

<tasks>

<task type="tracer">
  <name>Task 1: `npm run audit` con Lighthouse móvil, modo local explícito y resultado versionado</name>
  <files>package.json, package-lock.json, .gitignore, scripts/lib/audit-rules.mjs, scripts/audit.mjs, tests/guards/audit.test.mjs, docs/a11y/LIGHTHOUSE.md, docs/a11y/lighthouse-summary.json</files>
  <precondition>Los planes 03-01 y 03-02 están commiteados (`git log --oneline | grep -E "03-0[12]"` muestra commits de ambos), `node -v` es 22.19 o más (el equipo tiene 24.13.0), `node --test tests/guards/*.test.mjs` pasa, `git status --short src tests scripts public docs` está vacío, y anotas `git rev-parse HEAD` como `plan_head_before` en el SUMMARY (los criterios de diff del plan lo usan como `PLAN_HEAD_BEFORE`). Puerto 4323 libre (`lsof -i :4323` vacío).</precondition>
  <read_first>
    - package.json (completo, 42 líneas) y `.gitignore` (completo, 30 líneas).
    - scripts/check-copy.mjs líneas 80 a 130 (cómo imprime `--json` y cómo sale) y scripts/check-photos.mjs líneas 1 a 45 (solo cómo lee el entorno y cuándo sale 1). El resto ya se describe en el contexto.
    - `.planning/phases/02-secciones-marca-y-copy/02-08-SUMMARY.md` línea 61 (formato del registro de legitimidad de paquete) y nada más.
    - `.planning/phases/03-seo-medicion-minima-y-qa/03-01-SUMMARY.md` y `03-02-SUMMARY.md`: solo con `grep -n -i "desviaci\|deviation\|files\|archivos\|canonical\|robots\|sitemap\|dataLayer"` para saber qué cambió en el head, robots y scripts (no los leas enteros).
    - `.planning/phases/01-fundaciones-y-formulario-funcionando/01-FORM-MEASUREMENTS.md` solo las 40 primeras líneas (las cifras del formulario real).
    - tests/guards/html-budget.test.mjs líneas 1 a 30 (estilo de las guardas de Node del repo).
  </read_first>
  <behavior>
    RED primero: escribe `tests/guards/audit.test.mjs` antes que el código y comprueba que falla por módulo inexistente. Casos de la parte pura (`scripts/lib/audit-rules.mjs`):
    - `detectDeployContext(env)`: devuelve la lista de variables de despliegue presentes entre `CI`, `GITHUB_ACTIONS`, `CF_PAGES`, `VERCEL`, `NETLIFY`, `GITLAB_CI`, `BUILDKITE`, `RENDER` (vacía, `'0'` y `'false'` no cuentan).
    - `decideMode({ gatesPassed, structuralFailure, allowPending, deployContext })`: (1) `structuralFailure` verdadero da `refuse` con código 1 aunque haya bandera; (2) `gatesPassed` da `strict` (la bandera se ignora con aviso); (3) sin pasar y sin bandera da `refuse` código 1; (4) sin pasar, con bandera y con contexto de despliegue no vacío da `refuse` código 2; (5) sin pasar, con bandera y sin contexto da `local-pending`.
    - `THRESHOLDS` vale exactamente `{ performance: 95, accessibility: 100, seo: 100 }` y `RUNS_DEFAULT` vale 3.
    - `evaluateScores(runs, THRESHOLDS)`: rendimiento por mediana (95 exacto pasa, 94 falla; con corridas 96, 94, 97 pasa), accesibilidad y SEO por mínimo (100, 99, 100 falla). Devuelve `{ pass, failures: [texto legible] }`.
    - `summarizeNetwork(items)`: cuenta peticiones a `forms.clickup.com` (`formRequested`), peticiones a otros terceros (host distinto de localhost), bytes de JavaScript y peso total.
    - `renderSummary(summary)`: markdown que trae el modo, la tabla de corridas, la mediana y los mínimos, la corrida A y la corrida B una junto a la otra (aunque B sea mala), los pendientes, y ningún guion largo ni de rango (busca los caracteres U+2014 y U+2013).
    Parte estática y de comportamiento:
    - En `package.json` la cadena `allow-pending` no aparece fuera del script `audit`; tampoco en `astro.config.mjs` ni en `scripts/check-*.mjs`; el script `build` y los hooks `prebuild` y `postbuild` no cambian de contenido (se comparan con las líneas de la línea base).
    - `scripts/audit.mjs` no contiene `shell: true` ni `exec(` con cadena, contiene `--outDir` con `dist-audit`, y no contiene la ruta `'dist'` como salida de build.
    - Con pendientes reales (`node scripts/check-copy.mjs --json` con `PUBLIC_ENV=production` da `content.length > 0`): `PUBLIC_ENV=production PUBLIC_SITE_URL=http://localhost:4323 node scripts/check-copy.mjs` sale 1, `... node scripts/check-photos.mjs` sale 1 mientras `src/assets/photos/LICENSES.md` tenga aprobación pendiente (elige el marcador leyendo cinco líneas del archivo), y `... npm run build` sale distinto de 0. Si no quedan pendientes las mismas comprobaciones exigen salida 0 (así la guarda sigue siendo válida el día que Ari entregue todo).
    - `CI=1 node scripts/audit.mjs --allow-pending` sale 2 en menos de 10 s y no existe `dist-audit/` después; `node scripts/audit.mjs` sin bandera y con pendientes sale 1 sin construir (omite este caso si las puertas ya pasan).
  </behavior>
  <action>
    Per CONTEXT (Script `npm run audit`: construye con producción, sirve con `astro preview` y falla bajo 95, 100 y 100) y las decisiones de must_haves de este plan.

    1. Legitimidad del paquete `lighthouse` (obligatoria antes de instalar, como en 02-08): `node /Users/juan/.claude/gsd-core/bin/gsd-tools.cjs query package-legitimacy check --ecosystem npm lighthouse` debe devolver `OK`. Además `npm view lighthouse@13.5.0 version engines scripts.postinstall scripts.install dist.integrity --json`: la versión existe, `engines.node` es 22.19 o más y no hay script de instalación. Si el veredicto no es `OK` o la versión no existe, detente: no instales, deja el motivo en el SUMMARY y devuelve el plan como bloqueado (necesita decisión de Juan). Si es `OK`: `npm install --save-dev --save-exact lighthouse@13.5.0`. No agregues nada a `allowScripts` (Lighthouse no lo necesita; si `npm install` avisa de un script, detente y repórtalo). Revisa `git diff --stat HEAD -- package.json`: solo la línea nueva de `devDependencies` (y luego los scripts). Registra en el SUMMARY el veredicto, descargas semanales, repositorio, que no tiene postinstall y el conteo de paquetes nuevos del lockfile. Anota que `sharp` no se instaló (WR-06 es decisión del usuario).

    2. Módulo puro y guarda (RED, luego verde). Crea `tests/guards/audit.test.mjs` con los casos de `<behavior>` y confirma que falla. Luego crea `scripts/lib/audit-rules.mjs` (ESM sin dependencias) con `THRESHOLDS`, `RUNS_DEFAULT`, `DEPLOY_ENV_VARS`, `detectDeployContext`, `decideMode`, `median`, `evaluateScores`, `summarizeNetwork` y `renderSummary`. Ejecuta `node --test tests/guards/audit.test.mjs` hasta verde en los casos puros. Commit atómico A (cut point 1, ver abajo).

    3. `scripts/audit.mjs` (Node, `spawnSync` y `spawn` con lista de argumentos, sin shell). Argumentos: `--allow-pending`, `--record`, `--runs N` (por defecto 3, entero de 1 a 5), `--port N` (por defecto 4323, entero de 1024 a 65535), `--skip-form-run` (omite la corrida B; no se puede usar con `--record`). Flujo:
       a. Origen `http://localhost:<port>`. Si el puerto responde antes de empezar, sal 1 con el mensaje (no reutilices un servidor ajeno).
       b. Puertas, siempre en procesos hijos con `PUBLIC_ENV=production` y `PUBLIC_SITE_URL=<origen>` en el entorno del hijo (nunca se escribe en `.env`): `node scripts/check-contrast.mjs`; `node scripts/check-copy.mjs --json`; `node scripts/check-photos.mjs` y, si falla, `node scripts/check-photos.mjs` con `PUBLIC_ENV=development` para separar. `structuralFailure` es verdadero si falla el contraste, si `structural` de `check-copy` no está vacío o si `check-photos` falla también en desarrollo. `gatesPassed` es verdadero si las tres puertas salen 0 en producción. Imprime las salidas tal cual (los avisos de pendientes se ven, no se silencian) y el conteo `content.length` de textos pendientes.
       c. `decideMode(...)`: si es `refuse`, imprime el motivo en español y cómo seguir (`npm run pending` para la lista; `node scripts/audit.mjs --allow-pending` para medir con pendientes en local) y sal con el código indicado sin construir nada. En `local-pending` imprime un aviso en bloque, en mayúsculas y en las primeras líneas: `AUDITORÍA LOCAL CON PENDIENTES: este build NO es desplegable ni es evidencia de salida`.
       d. Build: borra `dist-audit/` (solo esa carpeta, con `rmSync` sobre la ruta fija), luego `npx astro build --outDir dist-audit` con `PUBLIC_ENV=production` y `PUBLIC_SITE_URL=<origen>`. En `local-pending` escribe `dist-audit/AUDIT-NOT-DEPLOYABLE.txt` (fecha, `git rev-parse HEAD`, conteo de pendientes, errores de fotos). En `strict` corre además `node scripts/check-copy.mjs --dist dist-audit`.
       e. Servidor: `npx astro preview --outDir dist-audit --port <port>` con `spawn` sin esperar su cierre (Astro 7 lo deja en segundo plano dentro de un agente y el proceso hijo termina: no dependas de su vida, sondea `GET <origen>/` cada 500 ms hasta 30 s y falla con el motivo si no responde 200). En un `finally` corre `npx astro preview stop` (y `kill` del hijo si sigue vivo).
       f. Lighthouse: `CHROME_PATH` se toma de `chromium.executablePath()` de `@playwright/test` (si el archivo no existe, sal 1 diciendo `npx playwright install chromium`; no lo descargues tú). Cada corrida es `npx lighthouse <url> --output=json --output-path=test-results/audit/<nombre>.json --only-categories=performance,accessibility,seo,best-practices --quiet --chrome-flags="--headless=new --no-sandbox"` con los valores por defecto de Lighthouse (factor móvil, emulación de Moto G, estrangulamiento simulado 4G lento). Corrida A: `<origen>/` repetida N veces (`lh-a-1.json` a `lh-a-N.json`). Corrida B (informativa, una sola): `<origen>/#agenda` (`lh-b.json`). Si en B `summarizeNetwork` no ve peticiones a `forms.clickup.com`, márcala `formRequested: false` y en el resumen `validity: 'sin formulario cargado'` (no la presentes como costo real). Si en A sí las ve, márcalo también (la corrida A deja de ser el primer pantallazo).
       g. Evaluación con `evaluateScores` sobre las N corridas de A. Imprime tabla de puntajes, mediana y mínimos, LCP, TBT, CLS, FCP, peso total y JavaScript, y la corrida B al lado (rendimiento, peso total, bytes de JavaScript, `formRequested`), más la lista de fallos. Sale 0 si `pass`, 1 si no, y 1 (no 0) ante cualquier error de infraestructura.
       h. Solo con `--record`: escribe `docs/a11y/lighthouse-summary.json` (claves: `generatedAt`, `gitHead`, `dirty`, `lighthouseVersion`, `node`, `mode`, `thresholds`, `runs` con puntajes y métricas, `median.performance`, `min.accessibility`, `min.seo`, `pass`, `failures`, `formGate` con `formRequested` de A, `formRun` con la corrida B completa, `pending` con `copyTexts` y `photoErrors`) y `docs/a11y/LIGHTHOUSE.md` con `renderSummary`. `dirty` es `true` si `git status --porcelain` no está vacío. Nunca escribe otra cosa fuera de `test-results/audit/`, `dist-audit/` y esos dos archivos.
       i. Nunca modifica `dist/`.

    4. `.gitignore`: agrega `dist-audit/` (con un comentario de una línea). NO agregues `docs/`, `docs/a11y` ni ningún nombre de los documentos de accesibilidad (la guarda de la tarea 4 lo comprueba). `package.json`: agrega solo `"audit": "node scripts/audit.mjs"`. No toques `build`, `prebuild` ni `postbuild`.

    5. Guarda completa en verde: `node --test tests/guards/*.test.mjs` (suite completa).

    6. Primera corrida real, registrada: `node scripts/audit.mjs --allow-pending --record` (con las puertas bloqueadas por los pendientes debe entrar en `local-pending`). Puede tardar varios minutos. El resultado (aunque quede bajo un umbral) se guarda; corregir es la tarea 3. Detén el preview si quedó vivo (`npx astro preview stop`). Copia a la nota del SUMMARY los cuatro números que importan: mediana de rendimiento, mínimo de accesibilidad, mínimo de SEO y la corrida B.

    7. Commits atómicos (lista explícita de archivos, nunca `git add -A`): A `feat(03-03): reglas puras de la auditoría con guarda` (`tests/guards/audit.test.mjs`, `scripts/lib/audit-rules.mjs`); B `feat(03-03): npm run audit con Lighthouse móvil y modo local explícito` (`package.json`, `package-lock.json`, `.gitignore`, `scripts/audit.mjs`, y el ajuste de la guarda si cambió); C `docs(03-03): primera corrida de Lighthouse registrada` (`docs/a11y/LIGHTHOUSE.md`, `docs/a11y/lighthouse-summary.json`).

    Cut point: si tu contexto va corto, para tras el commit B y deja anotado en el SUMMARY que falta la corrida real (paso 6); el siguiente ejecutor la hace con el comando exacto. Si la corrida real se cuelga más de 20 minutos, detén el preview, anota el motivo y no repitas más de una vez.
  </action>
  <acceptance_criteria>
    - `node --test tests/guards/audit.test.mjs` pasa y antes de escribir `scripts/lib/audit-rules.mjs` fallaba (evidencia de RED en el SUMMARY).
    - `grep -c "lighthouse" package.json` muestra `"lighthouse": "13.5.0"` sin `^` ni `~` en `devDependencies`, y `git diff <plan_head_before>..HEAD -- package.json` solo agrega esa línea y el script `audit`.
    - `grep -rn "allow-pending" package.json astro.config.mjs scripts/check-copy.mjs scripts/check-photos.mjs scripts/check-contrast.mjs` solo devuelve la línea del script `audit` de `package.json`.
    - `CI=1 node scripts/audit.mjs --allow-pending; test $? -eq 2 && test ! -e dist-audit`.
    - Con pendientes: `PUBLIC_ENV=production PUBLIC_SITE_URL=http://localhost:4323 npm run build` sale distinto de 0 (las puertas siguen bloqueando).
    - Existen `docs/a11y/lighthouse-summary.json` (JSON válido con `mode`, `thresholds` 95, 100 y 100, `runs` de longitud 3, `formRun` y `pending.copyTexts`) y `docs/a11y/LIGHTHOUSE.md` con las dos corridas (A y B) y el modo; `test-results/audit/` tiene los JSON completos y `git check-ignore test-results/audit/lh-a-1.json` confirma que no se versionan.
    - `git ls-files dist-audit` está vacío y `git check-ignore dist-audit` confirma la regla; `dist/` no fue modificado por la auditoría (su `mtime` de `index.html` no cambió respecto de antes de correrla).
    - `tracked=$(git ls-files) && ! printf '%s\n' "$tracked" | grep -Eq "brand-inventory/|\.ai$|\.pdf$"` sale 0 (nada confidencial versionado).
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/guards/*.test.mjs; g=$?; node scripts/audit.mjs --allow-pending --record; a=$?; npx astro preview stop; test -f docs/a11y/lighthouse-summary.json && node -e "const s=JSON.parse(require('fs').readFileSync('docs/a11y/lighthouse-summary.json','utf8'));if(!s.mode||s.thresholds.performance!==95||s.thresholds.accessibility!==100||s.thresholds.seo!==100||!s.formRun||s.runs.length<1)process.exit(1)"; f=$?; [ $g -eq 0 ] && [ $a -le 1 ] && [ $f -eq 0 ]</automated>
    <fails_when>Una puerta de producción quedó modificada o el permiso aparece fuera de `scripts/audit.mjs`; `npm run build` con producción deja de fallar con pendientes; el build de auditoría se escribe en `dist/`; el umbral cambió; `lighthouse` quedó con rango o hay script de instalación no revisado; el resumen omite la corrida B o la etiqueta de modo; el preview queda vivo; se versiona algo de `test-results/` o `dist-audit/`.</fails_when>
  </verify>
  <done>`npm run audit` mide un build de producción servido en local con Lighthouse móvil, con un modo local explícito que se rechaza en despliegue y que no debilita las puertas (probado), y la primera corrida real, con el costo del formulario al lado, queda versionada en un resumen pequeño. Todo en commits atómicos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: axe a 320, 390 y 1280 px y recorrido de teclado (incluido el iframe de ClickUp)</name>
  <files>tests/e2e/lib/isolated-context.ts, tests/e2e/phase-closing.spec.ts, tests/e2e/keyboard-walk.spec.ts, tests/e2e/keyboard-live.spec.ts, tests/guards/keyboard-safety.test.mjs, playwright.config.ts</files>
  <precondition>Tarea 1 commiteada. `dist/` al día: `npx astro build` (sin producción) y `npx astro preview --port 4322` en segundo plano; recuerda `npx astro preview stop` al final y reconstruir tras cada cambio de `src`. `git status --short src tests scripts public` vacío.</precondition>
  <read_first>
    - tests/e2e/phase-closing.spec.ts líneas 1 a 30 (importaciones), 60 a 128 (`isolatedContext`, `AXE_TAGS` y el bloque `axe de humo`). No leas el resto (hoja de contacto y demás).
    - playwright.config.ts (completo, 62 líneas; solo cambia la constante `LIVE_SPECS`).
    - src/components/SkipLinks.astro completo (unas 25 líneas) y src/components/AgendaSection.astro líneas 1 a 62 (orden de las paradas de foco alrededor del iframe).
    - `grep -n "focus-visible" -A6 src/styles/global.css` (define qué es 'contorno visible' en la prueba: no la inventes, léela).
    - tests/e2e/cta-focus.spec.ts líneas 40 a 90 (cómo se aísla ClickUp en un test y qué ya cubre el salto a `#agenda`, para no duplicarlo).
    - tests/e2e/form-live.spec.ts líneas 1 a 40 (cómo el proyecto `live` espera al formulario real y qué evita hacer).
    - 03-01-SUMMARY.md y 03-02-SUMMARY.md solo con `grep -n -i "focus\|tabindex\|script\|noscript"` (por si 03-02 agregó elementos enfocables o scripts).
  </read_first>
  <behavior>
    RED primero por bloque (cada spec nuevo se escribe y se comprueba que falla o que se omite por la razón correcta antes de ajustarlo):
    - Bloque axe (extensión, mismo `describe`): `AXE_TAGS` incorpora `best-practice`; falla ante `critical` y `serious`; los `moderate` y `minor` se anotan y solo pasan si su id está en `ACCEPTED_MODERATE`, hoy `{ 'landmark-unique': 'copy pendiente de Ari' }` con la condición de que la página muestre dos o más regiones con el mismo nombre 'FALTA CONFIRMAR'; si el copy ya no tiene marcadores y `landmark-unique` sigue apareciendo, la prueba falla (no era el copy). Nueva prueba: `/` a 390 px con todos los `details` abiertos (`page.evaluate` que pone `open = true`), mismas reglas. `/privacidad/` mantiene los tres anchos. El `.exclude('iframe')` se conserva y se comenta por qué (origen cruzado).
    - Recorrido `keyboard-walk.spec.ts` (proyecto `chromium`, contexto de `isolated-context.ts`, `/` a 1280 y 390 px, `/privacidad/` a 390 px): (1) cero elementos con `tabindex` positivo; (2) la secuencia de Tab hasta salir de la página es igual, parada por parada, a las paradas de foco visibles del DOM en orden (selector de enlaces con `href`, botones, `summary`, controles de formulario, `iframe` y `[tabindex]` no negativo; se excluyen ocultos, `inert` y el contenido de `details` cerrado salvo su `summary`); (3) primera parada es 'Saltar al contenido' y Enter en ella deja `document.activeElement.id === 'main'`; (4) cada parada tiene nombre accesible no vacío (`aria-label`, texto, `alt` o `title` del iframe); (5) Label in Name (SC 2.5.3): si la parada tiene texto visible y `aria-label`, el `aria-label` contiene ese texto normalizado; (6) contorno de foco visible: `outline-style` distinto de `none` y `outline-width` de 2 px o más (o lo que defina la regla global leída en `read_first`: ajusta la comprobación a lo que esa regla realmente usa); (7) no obstruida: `document.elementFromPoint` en el centro de la parada devuelve la parada o un descendiente; (8) en `/`, el enlace de respaldo (`a.agenda-link` con `href` de ClickUp y `target=_blank`) aparece antes que el iframe y el iframe tiene `title` no vacío; (9) tras la parada del iframe, un Tab más lleva a un elemento del documento principal que no es el iframe (sin trampa) y desde ahí Shift+Tab vuelve; (10) recorrido inverso completo con Shift+Tab desde la última parada reproduce la secuencia al revés. Con `E2E_BLOCK_CLICKUP=1` el iframe muestra una página de error y todo lo anterior sigue igual: es el caso que debe pasar siempre.
    - `keyboard-live.spec.ts` (proyecto `live`): con el formulario real, (a) si el frame de `forms.clickup.com` no aparece en 30 s, `test.skip('formulario de ClickUp no cargó: sin red o bloqueado')`; (b) se enfoca el enlace de respaldo por código (`locator.focus()`), se pulsa Tab hasta que `document.activeElement` es el iframe, y se cuentan las pulsaciones que siguen con el iframe activo (`insideTabs`), con tope de 80; (c) se sale con Tab: la siguiente parada del documento principal existe y no es el iframe (el pie); (d) Shift+Tab desde ahí vuelve al iframe y sale por el otro lado al enlace de respaldo en menos de 80 pulsaciones; (e) lectura de solo lectura del foco interno con `frame.evaluate(() => ({ tag, name }))` en cada parada dentro; los resultados se anotan como `testInfo.annotations` (`Tab dentro del formulario: N`, y la lista de controles sin nombre accesible, para EXCEPTIONS). Falla solo ante trampa de foco (tope superado) o si nunca se entra o sale.
    - `keyboard-safety.test.mjs` (guarda de Node): el texto de `tests/e2e/keyboard-live.spec.ts` no contiene `.fill(`, `.type(`, `.click(`, `.check(`, `.selectOption(`, `.setInputFiles(`, `.dblclick(`, `.tap(`, `.press('Enter'`, `.press("Enter"`, `.press('Space'`, `.press(' '`, `requestSubmit`, `.submit(`, `dispatchEvent`, y solo llama a `keyboard.press` con `Tab` o `Shift+Tab`. Prueba por mutación: la guarda falla si le pasas un texto con `.click(`.
  </behavior>
  <action>
    Per CONTEXT (QA y accesibilidad: axe a 320, 390 y 1280 px, prueba de tabulación más revisión manual de la entrada y salida del iframe, registrado en `EXCEPTIONS.md`).

    1. Extrae `isolatedContext` de `tests/e2e/phase-closing.spec.ts` a `tests/e2e/lib/isolated-context.ts` (misma firma y comportamiento: contexto que deja pasar localhost y aborta el resto) e impórtala en `phase-closing.spec.ts`. Verifica que las pruebas existentes de ese archivo siguen pasando antes de seguir (`E2E_BLOCK_CLICKUP=1 npx playwright test --project=chromium tests/e2e/phase-closing.spec.ts`). Commit `refactor(03-03): contexto aislado de Playwright compartido`.

    2. Extiende el bloque `axe de humo` según `<behavior>` (sin crear otro bloque ni duplicar bucles). Corre solo ese bloque, anota la línea base: cuántas violaciones por ancho y cuáles son moderate o minor. Si aparece un `moderate` que no es `landmark-unique`, arréglalo si es un defecto real y barato de un componente (lista en el SUMMARY) o agrégalo a `ACCEPTED_MODERATE` con razón y una entrada en EXCEPTIONS (tarea 4); no lo silencies sin registro. Commit `test(03-03): axe con best-practice, FAQ abierto y moderate aceptados con razón`.

    3. `tests/e2e/keyboard-walk.spec.ts` según `<behavior>` con un ayudante `walk(page)` que devuelva la lista de paradas (etiqueta, id, href, nombre) y otro `domStops(page)`. Sin esperas fijas de tiempo: espera con `expect.poll` o sobre el `activeElement`. Corre con `E2E_BLOCK_CLICKUP=1` y también sin la variable. Ajusta la comprobación del contorno (6) a lo que la regla global realmente define. Commit `test(03-03): recorrido de teclado de la landing con el iframe aislado`.

    4. `tests/e2e/keyboard-live.spec.ts` y `tests/guards/keyboard-safety.test.mjs` según `<behavior>`; en `playwright.config.ts` cambia solo `LIVE_SPECS` a `/(form-(live|measure)|keyboard-live)\.spec\.ts$/` y actualiza el comentario de arriba en una línea. Corre la guarda (`node --test tests/guards/keyboard-safety.test.mjs`) y luego `npx playwright test --project=live tests/e2e/keyboard-live.spec.ts` UNA vez. NO envíes, llenes ni hagas clic en el formulario. Anota en el SUMMARY el resultado (`insideTabs`, controles sin nombre, o el motivo de la omisión). Commit `test(03-03): recorrido de teclado con el formulario real de ClickUp (solo lectura)`.

    5. Suite de navegador completa con ClickUp bloqueado (`E2E_BLOCK_CLICKUP=1 npx playwright test --project=chromium`) y guardas de Node en verde. Si el recorrido o axe descubren un defecto real, NO lo corrijas aquí: anótalo con archivo y línea en el SUMMARY para la tarea 3 (así los commits de prueba y de corrección quedan separados).

    Protocolo de navegador: `npx astro build && npx astro preview --port 4322` antes de Playwright, `npx astro build` de nuevo tras cada cambio de `src`, y `npx astro preview stop` al final.

    Cut point: tras el commit del paso 3 la parte automatizada del criterio está completa; si el contexto va corto, deja el paso 4 anotado como pendiente en el SUMMARY con los comandos exactos.
  </action>
  <acceptance_criteria>
    - `git diff <plan_head_before>..HEAD -- tests/e2e/phase-closing.spec.ts` muestra un solo bloque `axe de humo` (sin segundo `describe` de axe) que incluye `best-practice`, la corrida con FAQ abierto y `ACCEPTED_MODERATE`; y `grep -c "function isolatedContext" tests/e2e/phase-closing.spec.ts` es 0 (vive en `lib/isolated-context.ts`).
    - `E2E_BLOCK_CLICKUP=1 npx playwright test --project=chromium tests/e2e/phase-closing.spec.ts tests/e2e/keyboard-walk.spec.ts` pasa; el conteo de axe por ancho aparece en las anotaciones y se copia al SUMMARY.
    - `npx playwright test --project=chromium tests/e2e/keyboard-walk.spec.ts` (sin bloquear ClickUp) también pasa.
    - La prueba de teclado falla si se rompe una condición: comprobado por mutación manual una vez y registrada en el SUMMARY (por ejemplo `tabindex="3"` temporal en un enlace, o quitar el `title` del iframe, y revertir el cambio sin commit).
    - `node --test tests/guards/keyboard-safety.test.mjs` pasa y falla ante un texto con `.click(`; `grep -nE "\.(fill|click|type|check|submit)\(" tests/e2e/keyboard-live.spec.ts` no devuelve líneas.
    - `npx playwright test --project=live tests/e2e/keyboard-live.spec.ts --list` lista la prueba (el proyecto `live` la recoge) y su resultado (pasa u omitida con motivo) queda en el SUMMARY.
    - Ningún archivo de `src` cambió en esta tarea (`git diff --name-only <plan_head_before>..HEAD -- src` vacío).
  </acceptance_criteria>
  <verify>
    <automated>npx astro build && node --test tests/guards/*.test.mjs; g=$?; npx astro preview --port 4322; E2E_BLOCK_CLICKUP=1 npx playwright test --project=chromium; s=$?; npx playwright test --project=live tests/e2e/keyboard-live.spec.ts --list >/dev/null; l=$?; npx astro preview stop; [ $g -eq 0 ] && [ $s -eq 0 ] && [ $l -eq 0 ]</automated>
    <fails_when>Hay un segundo bloque de axe; la extensión acepta un moderate sin razón; la secuencia de Tab no se compara con el DOM; la prueba viva llena, hace clic o envía; `LIVE_SPECS` no recoge la prueba nueva; la prueba viva pasa en silencio sin red; se toca `src` en esta tarea; se rompe una prueba previa de la fase 1 o 2.</fails_when>
  </verify>
  <done>axe corre a 320, 390 y 1280 px con reglas de mejores prácticas y el FAQ abierto, el recorrido de Tab de la página completa coincide con el DOM y no tiene trampa alrededor del iframe aislado, existe la prueba con el formulario real que solo lee y solo usa Tab, y los hallazgos que aparezcan quedan anotados con archivo y línea para la tarea 3.</done>
</task>

<task type="auto">
  <name>Task 3: Corregir los hallazgos de la auditoría y dejar `npm run audit` en verde</name>
  <files>src/**, tests/e2e/phase-closing.spec.ts, tests/e2e/keyboard-walk.spec.ts, docs/a11y/LIGHTHOUSE.md, docs/a11y/lighthouse-summary.json</files>
  <precondition>Tareas 1 y 2 commiteadas. Tienes la lista de hallazgos en el SUMMARY parcial: fallos de Lighthouse (`test-results/audit/lh-a-*.json`, auditorías con puntaje menor a 1 en accesibilidad y SEO, y las oportunidades del rendimiento) y defectos que anotó la tarea 2. Si esa lista está vacía y `node scripts/audit.mjs --allow-pending` sale 0, esta tarea solo confirma con una corrida final y registra `sin hallazgos` (no inventes trabajo).</precondition>
  <read_first>
    - El SUMMARY parcial de las tareas 1 y 2 (hallazgos con archivo y línea).
    - Para cada hallazgo de Lighthouse: `node -e` que imprima solo `id`, `title`, `score` y los primeros tres `details.items` de las auditorías fallidas del JSON `test-results/audit/lh-a-1.json` (no abras el JSON entero: pesa cerca de 0,5 MB).
    - Solo los archivos de `src` que cada hallazgo señale, con `grep -n` y `Read` con offset y límite. Componentes candidatos: `src/layouts/BaseLayout.astro`, `src/components/AgendaSection.astro`, `src/styles/global.css`.
    - `.claude/CLAUDE.md` tabla de contraste (no estimes colores: si un hallazgo es de contraste, mide con `node scripts/check-contrast.mjs` y los pares del repo).
    - Skills a invocar solo si un hallazgo toca diseño visible: `impeccable` y `design-taste-frontend` (regla del proyecto). Si tu contexto ya las cargó, reutilízalas.
  </read_first>
  <action>
    Per CONTEXT (Lighthouse móvil 95, 100, 100 y axe sin serias ni críticas) y las decisiones de must_haves.

    Ciclo (máximo dos rondas; una tercera necesita aviso a Juan):
    1. Clasifica cada hallazgo: (a) defecto de accesibilidad o SEO técnico con arreglo local y seguro (atributo, nombre accesible, enlace sin texto, `meta` faltante, orden de foco, contraste medido, `lang`); (b) rendimiento con causa medida en los propios recursos de la página (imagen sin dimensiones o sin `fetchpriority` en el LCP, fuente sin `preload`, CSS o JS propio bloqueante, script propio de 03-02); (c) causa en el formulario de ClickUp o en cómo carga; (d) decisión de diseño o de copy.
    2. Corrige solo (a) y (b), en el archivo dueño, con el cambio mínimo, un commit atómico por causa: `fix(03-03): <hallazgo> (<auditoría de Lighthouse o regla de axe>)`. Después de cada lote reconstruye y vuelve a correr la prueba que lo detectó, más la suite de Chromium con ClickUp bloqueado.
    3. (c) y (d) no se hacen en silencio. Para (c): si la mediana de rendimiento queda bajo 95 por el `v1.js` asíncrono de ClickUp, mide primero con el script diferido de forma provisional (sin commitear) y anota el número; cambiar cuándo se pide ese script toca FORM-03 y la medición de la fase 1, así que se registra como decisión abierta para Juan con las dos cifras (antes y después) y NO se commitea. Para (d): se registra y se pide a Juan o a Ari.
    4. Vuelve a correr `node scripts/audit.mjs --allow-pending --record`. Si algún umbral queda sin cumplir por una causa (c) o (d), NO bajes el umbral ni ocultes la cifra: `LIGHTHOUSE.md` ya muestra el fallo con su causa (agrega una sección `Causas de lo que no cumple` con el dato medido), el SUMMARY marca el criterio 3 del ROADMAP como no cumplido con la decisión pendiente, y el plan termina con `status: needs_decision` en el SUMMARY.
    5. Confirmación de no regresión (todo en verde antes del commit final): `node --test tests/guards/*.test.mjs`, `node scripts/check-contrast.mjs`, `node scripts/list-pending.mjs --check`, `E2E_BLOCK_CLICKUP=1 npx playwright test --project=chromium`, y la comprobación de que las puertas siguen bloqueando: `PUBLIC_ENV=production PUBLIC_SITE_URL=http://localhost:4323 npm run build` sale distinto de 0 mientras haya pendientes, y `npm run build` sin producción sale 0.
    6. Commit final `docs(03-03): resultado de Lighthouse tras las correcciones` con los dos archivos de `docs/a11y/` (lista explícita).

    Nada de copy, marca ni fotos: el diff contra `plan_head_before` no debe listar `src/content`, `src/assets/brand`, `src/assets/photos` ni `PENDING-COPY.md`.

    Cut point: cierra cada ronda con su commit; si vas a pasar de dos rondas o de contexto, para, deja la lista de hallazgos abiertos en el SUMMARY y no empieces tarea 4 con el árbol sucio.
  </action>
  <acceptance_criteria>
    - Alguna de estas dos: `node scripts/audit.mjs --allow-pending` sale 0 con mediana de rendimiento de 95 o más y mínimos de accesibilidad y SEO de 100 (la corrida registrada en `lighthouse-summary.json` con `pass: true`); o el SUMMARY trae cada umbral incumplido con causa medida y decisión pendiente, con `status: needs_decision`. Nunca un umbral cambiado.
    - `grep -n "95\|100" scripts/lib/audit-rules.mjs` sigue mostrando `performance: 95, accessibility: 100, seo: 100`.
    - `[ -z "$(git diff --name-only <plan_head_before>..HEAD -- src/content src/assets/brand src/assets/photos PENDING-COPY.md)" ]` y `node scripts/list-pending.mjs --check` sale 0.
    - Cada archivo de `src` tocado aparece en el SUMMARY con el hallazgo que lo motivó; ningún archivo de `src` cambió sin un hallazgo medido.
    - Con pendientes, `PUBLIC_ENV=production PUBLIC_SITE_URL=http://localhost:4323 npm run build` sale distinto de 0; sin producción, `npm run build` sale 0.
    - `E2E_BLOCK_CLICKUP=1 npx playwright test --project=chromium` y `node --test tests/guards/*.test.mjs` pasan.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/guards/*.test.mjs && node scripts/check-contrast.mjs && node scripts/list-pending.mjs --check; g=$?; npx astro build; npx astro preview --port 4322; E2E_BLOCK_CLICKUP=1 npx playwright test --project=chromium; s=$?; npx astro preview stop; PUBLIC_ENV=production PUBLIC_SITE_URL=http://localhost:4323 npm run build >/dev/null 2>&1; p=$?; npm run build >/dev/null 2>&1; n=$?; [ $g -eq 0 ] && [ $s -eq 0 ] && [ $p -ne 0 ] && [ $n -eq 0 ]</automated>
    <fails_when>Se baja un umbral o se acepta un moderate sin razón; se cambia copy, marca o fotos; se corrige algo sin hallazgo medido; se cambia cómo carga el formulario sin decisión de Juan; el build de producción con pendientes deja de fallar; se rompe una prueba previa; la cifra de la corrida B desaparece del resumen.</fails_when>
  </verify>
  <done>Los hallazgos de Lighthouse, axe y teclado están corregidos con causa medida (o registrados con la decisión que falta), `npm run audit` en modo local da el resultado honesto y versionado, las puertas de producción siguen bloqueando, y la suite previa sigue en verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Documentos de accesibilidad versionados y checklist de VoiceOver</name>
  <files>docs/a11y/REPORT.md, docs/a11y/EXCEPTIONS.md, docs/a11y/A11Y-DECISIONS.md, docs/a11y/VOICEOVER-CHECKLIST.md, tests/guards/a11y-docs.test.mjs</files>
  <precondition>Tareas 1 a 3 commiteadas y `docs/a11y/lighthouse-summary.json` existe. `git status --short` vacío. Tienes a mano los números del SUMMARY (axe por ancho, recorrido de teclado, `insideTabs` o el motivo de la omisión, Lighthouse A y B).</precondition>
  <read_first>
    - `docs/a11y/LIGHTHOUSE.md` y `docs/a11y/lighthouse-summary.json` (las cifras que el informe cita).
    - El SUMMARY parcial de las tareas 1 a 3 (evidencia y hallazgos).
    - `.planning/phases/02-secciones-marca-y-copy/02-VERIFICATION.md` solo `grep -n -i "excepci\|exception\|1.88\|2.95\|landmark\|logo"` (las excepciones de marca de la fase 2 y su razón).
    - `src/components/brand/logo-variants.mjs` solo `grep -n "17\|22\|exception\|excepci"` (cómo la fase 2 registró las mesas 17 y 22).
    - Plantillas oficiales de A11Y.md: intenta UNA descarga por plantilla con `curl -sL --max-time 20 -o <archivo en el scratchpad> https://raw.githubusercontent.com/fecarrico/A11Y.md/main/docs/en/templates/<REPORT|EXCEPTIONS|A11Y-DECISIONS>.md`. Si alguna no responde, usa las secciones de esta tarea (copian la plantilla) y anótalo en el SUMMARY para que se alinee cuando haya red. No dependas de la descarga.
    - `tests/e2e/motion.spec.ts`, `tests/e2e/a11y-base.spec.ts` y `tests/e2e/page-structure.spec.ts` solo con `grep -n "test("` (para citar como evidencia lo que ya existe: H1 único, movimiento reducido, tamaños de objetivo, reflow a 320 px).
  </read_first>
  <behavior>
    RED primero: escribe `tests/guards/a11y-docs.test.mjs` y comprueba que falla (los documentos no existen). Casos:
    - Los cinco archivos de `docs/a11y/` existen y `git check-ignore -q` sobre cada uno sale distinto de 0 (no están ignorados); `.gitignore` no contiene `docs/`, `docs/a11y`, `REPORT.md`, `EXCEPTIONS.md`, `A11Y-DECISIONS.md` ni `VOICEOVER`.
    - `REPORT.md` contiene la leyenda de marcas, `Verification Independence` con `self-reported`, un estado que contiene `CONDITIONAL` y no contiene `PASS` como estado, fecha `YYYY-MM-DD` y `Covers interface as of` con un hash de 7 a 40 caracteres hexadecimales; los seis bloques numerados; y cada línea con `[x]` trae evidencia (texto después de la marca de al menos 20 caracteres y una referencia a un archivo de prueba, comando o número). Ningún `[x]` en Screen Reader.
    - `EXCEPTIONS.md`: cada entrada `### EXC-` tiene los campos Exception ID, Component, WCAG, Severity, Risk Owner, Approved by, Tracking, qué está roto, por qué, workaround y `Expiry` con fecha ISO. Debe existir la entrada del iframe de ClickUp con vencimiento `2026-12-19`, dueño no vacío y el enlace de respaldo como workaround; las de las mesas 17 y 22 y la del naranja decorativo sobre morado. La guarda avisa (falla) si una fecha ya venció respecto de `new Date()`.
    - `A11Y-DECISIONS.md`: entre 5 y 40 líneas de decisión con el formato `- **Patrón** -> elección, razón. (fecha)`, indexadas por patrón (ninguna empieza con nombre de pantalla).
    - `VOICEOVER-CHECKLIST.md` nombra los pares `VoiceOver + Safari` en macOS y en iOS y trae una tabla de resultados con quién y cuándo.
    - Ningún archivo de `docs/a11y/*.md` contiene los caracteres U+2014 ni U+2013, ni voseo (`tenés`, `vos `, `hacé`, `abrí`, `presioná`).
  </behavior>
  <action>
    Per CONTEXT (Los documentos `docs/a11y/REPORT.md`, `EXCEPTIONS.md` y `A11Y-DECISIONS.md` existen y están versionados; `REPORT.md` self-reported; checklist de VoiceOver en Safari para Camila o Juan) y A11Y.md (leyenda `[x] [!] [~] [ ]`, un `[x]` sin evidencia reproducible invalida el informe, un agente sin lector de pantalla marca `[ ]` con motivo y quién debe correrlo). Idioma: español neutro (tú), sin guiones largos ni de rango; los identificadores técnicos y los nombres de las secciones de la plantilla pueden ir en inglés porque son etiquetas de un estándar.

    1. `docs/a11y/REPORT.md` con la estructura de la plantilla oficial: Validation Context (Feature: Landing de captación de Loops Growth; fecha de hoy; Covers interface as of: `git rev-parse --short HEAD` del commit que verificaste; Compliance Status: `CONDITIONAL`; Verification Independence: `self-reported` con el nombre del agente y la sesión, más la nota de A11Y.md de que un solo agente no puede subir a PASS), y los bloques 1 a 6 con cada punto marcado según evidencia real:
       - 1 Technical: axe (`[x]` con el conteo por ancho y el archivo `tests/e2e/phase-closing.spec.ts`), Lighthouse (`[x]` con los puntajes de `lighthouse-summary.json` y el modo; si el modo es `local-pending` dilo), semántica HTML (`[x]` solo si la citas de `page-structure.spec.ts`; si no, `[~]`), jerarquía de encabezados (idem).
       - 2 Tab order: foco visible, orden lógico (evidencia `keyboard-walk.spec.ts`; la medición 3:1 del anillo se cita de `check-contrast` solo si existe ese par; si no, `[~]`), atrapamiento (`[x]` sin trampa alrededor del iframe aislado y, con `keyboard-live.spec.ts`, con el real, o `[ ]` con el motivo si se omitió).
       - 3 Behavior: lector de pantalla `[ ]` con quién debe correrlo (Camila o Juan, `VOICEOVER-CHECKLIST.md`) y los pares no probados; control por voz `[~]` (nombres accesibles contra etiquetas visibles, cubierto por `keyboard-walk.spec.ts` punto 5; iOS Voice Control sin probar); estados interactivos inventariados (FAQ abierto y cerrado, salto de CTA a `#agenda`, iframe cargando, bloqueado y real, `noscript`), cada uno con su método (navegado, leído en código o no verificado); `aria-live` y formularios: N/A para nuestro DOM, el formulario es de ClickUp (enlace a `EXC-001`).
       - 4 Visual: contraste (`[x]` con `node scripts/check-contrast.mjs` y su conteo de pares), redundancia de color, zoom 200 % y reflow a 320 px (cita el spec que lo cubra o `[~]`).
       - 5 Motion: cita `tests/e2e/motion.spec.ts`; N/A donde no haya medios.
       - 6 Cognitive: las líneas que dependen del formulario de terceros `[ ]` con el motivo y `EXC-001`.
       - Notas y bloqueadores: `landmark-unique` (causa: copy pendiente de Ari, se revisa cuando entregue los títulos, fecha de revisión), lo de Lighthouse B (costo del formulario), y que el informe no sube de CONDITIONAL hasta que otra sesión o una persona verifique.
       - Omite el bloque 7 opcional con una línea (`No se emite declaración formal`).
    2. `docs/a11y/EXCEPTIONS.md` con las reglas de la plantilla al inicio y estas entradas (`### EXC-001`, `EXC-002`, ...): EXC-001 formulario de ClickUp en iframe (criterios 2.1.1, 4.1.2 y 1.3.1 no controlables dentro del iframe; severidad Alta; Risk Owner: Juan Angulo; Approved by: Ari (pendiente de firma: anótalo tal cual, la guarda acepta la palabra `pendiente` en este campo); Tracking: este archivo y la decisión abierta en el SUMMARY (Juan crea la tarea de seguimiento en ClickUp, ver pregunta abierta); qué está roto según lo que midió `keyboard-live.spec.ts` (controles sin nombre accesible reales, no supuestos); por qué: DOM de terceros de origen cruzado; workaround: enlace de respaldo visible `target=_blank` con `rel=noopener noreferrer`, el `noscript` con el mismo enlace, `title` descriptivo del iframe; Expiry `2026-12-19`; criterio de resolución: ClickUp corrige su formulario o se reemplaza por un formulario propio). EXC-002 y EXC-003 mesas de logo 17 (isotipo) y 22 (ojo) sobre oscuro: aro morado 1.88 sobre #212121, la figura se lee por el anillo crema 14.37; severidad Baja; workaround: nombre accesible del logo y versión clara en el header; Expiry `2026-12-19`, resolución: Ari entrega versión con aro crema. EXC-004 relleno naranja decorativo del collage sobre morado (2.95): decorativo, `aria-hidden`, con contorno blanco de 3 px que mide 8.55 sobre morado; severidad Baja, registrado por transparencia porque SC 1.4.11 no exige contraste a la decoración pura; Expiry `2026-12-19`. Agrega una entrada por cada `ACCEPTED_MODERATE` distinto de `landmark-unique` que dejó la tarea 2 y por cualquier `[!]` del informe. Las fechas de la plantilla son de revisión, no de olvido.
    3. `docs/a11y/A11Y-DECISIONS.md`: memoria de patrones (una línea por patrón, no por pantalla). Como mínimo: CTA repetido (mismo texto visible y `href="#agenda"`, foco al h2 con `tabindex="-1"`), skip links (dos: contenido y formulario), acordeón (`details` y `summary` nativos), iframe de terceros (`title` descriptivo, enlace de respaldo antes del iframe, sin `frameborder`), decoración (SVG `aria-hidden` y `focusable=false`), pasajes en inglés (`lang` propio), movimiento (visible por defecto y solo `motion-safe`), contraste (pares medidos, nunca a ojo), Lighthouse y modo local (bandera `--allow-pending`, el resultado `local-pending` no es evidencia de salida), arrastre WR-01 (el hosting debe usar `npm run build`), y la nota de Node (Lighthouse exige 22.19 y `engines` pide 22.12: el equipo usa 24.13.0). Menos de 40 líneas.
    4. `docs/a11y/VOICEOVER-CHECKLIST.md` para Camila o Juan, en español neutro con "tú": preparación (macOS: Safari, `Cmd+F5` para activar VoiceOver, abrir `npm run dev:lan` o el preview en la red local; iPhone: Ajustes, Accesibilidad, VoiceOver, abrir la página por la IP de la red local), pasos numerados con lo que debes oír y la marca OK, Falla o No aplica: landmarks con el rotor (`Control+Option+U` en macOS), lista de encabezados y un solo H1, skip link 'Saltar al contenido' y 'Saltar al formulario', CTA 'Agenda tu llamada de 30 minutos' (debe llevarte al formulario y anunciar su título), acordeón (anuncia expandido y contraído), entrada al iframe (debe anunciar su título), moverse dentro con las flechas de VoiceOver sin llenar nada, salida del iframe y llegada al pie, y el enlace de respaldo. Aclara: NO envíes el formulario ni escribas datos reales; si un control del formulario no anuncia su nombre, anótalo, es del proveedor y va a `EXC-001`. Tabla final de resultados con columnas Comprobación, Resultado, Notas, Quién y Fecha, y el par (`VoiceOver + Safari`, versión) que se probó. Al terminar, quien la corrió actualiza el punto de lector de pantalla de `REPORT.md` de `[ ]` a `[x]` o `[!]` con su nombre y fecha.
    5. `tests/guards/a11y-docs.test.mjs` según `<behavior>` (RED antes de escribir los documentos, verde después) y prueba por mutación: la guarda falla si se quita el vencimiento del iframe o si `REPORT.md` cambia el estado a PASS.
    6. Commits atómicos con lista explícita: `test(03-03): guarda de los documentos de accesibilidad` y `docs(03-03): REPORT, EXCEPTIONS, A11Y-DECISIONS y checklist de VoiceOver`.
    7. Cierre del plan: escribe el `03-03-SUMMARY.md` (sección Output: resultados de Lighthouse A y B con el modo, conteos de axe, resultado del recorrido, hallazgos corregidos, decisiones abiertas para Juan y Ari, arrastre WR-01 para la fase 4, evidencia RED y de mutación, y `plan_head_before`). No marques requisitos ni avances contadores: lo hace el orquestador.

    Cut point: commit tras cada documento si el contexto va corto; el informe (REPORT) es lo último que se pierde, escríbelo antes que la checklist si tienes que elegir.
  </action>
  <acceptance_criteria>
    - Existen `docs/a11y/REPORT.md`, `EXCEPTIONS.md`, `A11Y-DECISIONS.md`, `VOICEOVER-CHECKLIST.md`, `LIGHTHOUSE.md` y `lighthouse-summary.json`; `for f in docs/a11y/*; do git check-ignore -q "$f" && echo IGNORED $f; done` no imprime nada y `git ls-files docs/a11y` los lista todos.
    - `grep -n "Verification Independence" docs/a11y/REPORT.md` muestra `self-reported`; el estado es `CONDITIONAL` y `grep -c "PASS" docs/a11y/REPORT.md` no cuenta ningún estado PASS.
    - `grep -n "EXC-001" -A12 docs/a11y/EXCEPTIONS.md` muestra el iframe de ClickUp con dueño, quien aprueba, seguimiento, `2026-12-19` y el enlace de respaldo.
    - `node --test tests/guards/a11y-docs.test.mjs` pasa, y falló antes de crear los documentos.
    - `! grep -rnP "\x{2014}|\x{2013}" docs/a11y` (ningún guion largo ni de rango) y sin voseo.
    - `git diff --name-only <plan_head_before>..HEAD` solo lista archivos de `files_modified` (más el SUMMARY) y ningún archivo de `brand-inventory/`.
    - `03-03-SUMMARY.md` existe con las cifras de Lighthouse A y B, el modo y las decisiones abiertas.
  </acceptance_criteria>
  <verify>
    <automated>node --test tests/guards/*.test.mjs; g=$?; for f in docs/a11y/*; do git check-ignore -q "$f" && exit 1; done; ! grep -rnP "\x{2014}|\x{2013}" docs/a11y; d=$?; [ $g -eq 0 ] && [ $d -eq 0 ]</automated>
    <human-check>Camila o Juan corren `docs/a11y/VOICEOVER-CHECKLIST.md` con VoiceOver y Safari en macOS y en un iPhone antes del evento y completan la tabla; si no la corren, el punto de lector de pantalla del informe queda `[ ]` y el estado sigue en CONDITIONAL. No bloquea el cierre del plan. Juan confirma quién aprueba `EXC-001` (Ari o él) y crea la tarea de seguimiento en ClickUp.</human-check>
    <fails_when>Un documento está en `.gitignore`; el informe marca `[x]` sin evidencia o declara PASS; falta el iframe en EXCEPTIONS o no tiene vencimiento; una entrada no tiene dueño persona; aparece un guion largo o voseo; el informe cita una prueba que no existe; se escribe `brand-inventory/`.</fails_when>
  </verify>
  <done>Los tres documentos de A11Y.md más la checklist de VoiceOver y el resumen de Lighthouse están versionados, honestos (CONDITIONAL, self-reported), con el iframe de ClickUp como excepción conocida con dueño, aprobación, seguimiento y vencimiento, y una guarda impide que se ignoren, se declaren PASS o pierdan su fecha.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Modo local de la auditoría a despliegue | El permiso para medir con pendientes no debe poder llegar a un build publicado |
| Registro npm a repo | `lighthouse` y su árbol de dependencias entran a `devDependencies` |
| Pruebas a formulario real de ClickUp | La prueba viva toca un servicio de terceros que crea tareas reales al enviarse |
| Resultados de auditoría a repo | JSON de Lighthouse y documentos versionados pueden exponer datos o afirmar de más |
| Repo a remoto | El BrandBook y sus renders son confidenciales; las fotos originales no se versionan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03-01 | Elevation of Privilege | Modo `--allow-pending` de `scripts/audit.mjs` | high | mitigate | Solo bandera de línea de comandos (no variable heredable), solo en `audit.mjs`, rechazo con salida 2 si hay variable de despliegue, rechazo siempre ante defecto estructural, build en `dist-audit/` con marcador `AUDIT-NOT-DEPLOYABLE.txt`, puertas intactas y guarda que prueba que `npm run build` con producción sigue fallando con pendientes y que `allow-pending` no aparece fuera del script `audit` |
| T-03-03-02 | Tampering | Umbrales de Lighthouse bajados para que pase | medium | mitigate | `THRESHOLDS` fijado literalmente por la guarda, el resumen versionado guarda los umbrales usados, criterio de aceptación de la tarea 3 lo comprueba con grep |
| T-03-03-03 | Tampering | Cadena de suministro de `lighthouse` | medium | mitigate | Auditoría de legitimidad `gsd-tools query package-legitimacy check`, versión exacta 13.5.0 sin rango, `npm view` de scripts de instalación, `allowScripts` intacto, revisión del diff de `package.json` y del lockfile, dependencia solo de desarrollo (no llega al build ni al sitio) |
| T-03-03-04 | Information Disclosure | Prueba con el formulario real de ClickUp | high | mitigate | Solo Tab, Shift+Tab y lecturas; guarda `keyboard-safety` que prohíbe llenar, hacer clic o enviar y falla por mutación; ningún dato de prueba se escribe; se omite si el formulario no carga |
| T-03-03-05 | Repudiation | Informe que afirma más de lo verificado | medium | mitigate | `REPORT.md` `self-reported` y CONDITIONAL, `[x]` solo con evidencia citada, guarda que rechaza PASS y `[x]` sin evidencia, lector de pantalla siempre `[ ]` hasta que una persona lo corra |
| T-03-03-06 | Information Disclosure | JSON completos de Lighthouse y del build de auditoría | low | mitigate | Los JSON viven en `test-results/audit/` y el build en `dist-audit/`, ambas ignoradas por git; el resumen versionado solo trae puntajes, métricas y nombres de host, sin cabeceras ni cookies |
| T-03-03-07 | Denial of Service | Servidor de vista previa que queda vivo o puerto ajeno | low | mitigate | Puerto fijo 4323 distinto de 4321 y 4322, rechazo si ya responde, `astro preview stop` en `finally`, criterio de aceptación de que no queda vivo |
| T-03-03-08 | Spoofing | Peticiones de terceros durante la auditoría | low | accept | La corrida A pide el `v1.js` de ClickUp por diseño (es parte de la página); el resumen registra los hosts de terceros y si hubo peticiones a `forms.clickup.com` |
| T-03-03-09 | Information Disclosure | Commit accidental de material confidencial | high | mitigate | Todos los commits con lista explícita de archivos, nunca `git add -A`; `.planning/phases/02-secciones-marca-y-copy/brand-inventory/` ignorado y excluido; criterio de aceptación con `git ls-files` |
| T-03-03-10 | Repudiation | Resultado local con pendientes tomado como evidencia de salida | medium | mitigate | `mode: local-pending` en el resumen, aviso en bloque al ejecutar, `REPORT.md` y `A11Y-DECISIONS.md` lo dicen y la fase 4 exige `mode: strict` |
| T-03-03-SC | Tampering | Instalación de paquetes npm | medium | mitigate | Una sola dependencia (`lighthouse`), con el chequeo de legitimidad y sin `sharp` (WR-06, decisión del usuario) |
</threat_model>

<verification>
Al cerrar el plan, en este orden y con las lecciones de las fases 1 y 2 (el preview se va a segundo plano dentro de un agente; reconstruir tras cada cambio; detenerlo al final):
1. `node --test tests/guards/*.test.mjs && node scripts/check-contrast.mjs && node scripts/list-pending.mjs --check`: guardas previas y nuevas en verde, `PENDING-COPY.md` sin cambios.
2. `npx astro build && npx astro preview --port 4322`, luego `E2E_BLOCK_CLICKUP=1 npx playwright test --project=chromium` (axe extendido y recorrido de teclado incluidos) y `npx astro preview stop`. Una corrida más sin bloquear ClickUp de `tests/e2e/keyboard-walk.spec.ts`. El proyecto `live` solo corre `keyboard-live.spec.ts` una vez.
3. `node scripts/audit.mjs --allow-pending --record` (modo `local-pending` mientras haya pendientes): resultado registrado en `docs/a11y/`, con la corrida B visible; `npx astro preview stop`.
4. Las puertas siguen firmes: `PUBLIC_ENV=production PUBLIC_SITE_URL=http://localhost:4323 npm run build` falla con pendientes y `npm run build` sin producción pasa; `CI=1 node scripts/audit.mjs --allow-pending` sale 2.
5. Barridos: `grep -rn "allow-pending" package.json astro.config.mjs scripts/check-*.mjs` solo devuelve el script `audit`; `tracked=$(git ls-files) && ! printf '%s\n' "$tracked" | grep -Eq "brand-inventory/|\.ai$|\.pdf$|dist-audit|test-results"` sale 0; `! grep -rnP "\x{2014}|\x{2013}" docs/a11y`; `[ -z "$(git diff --name-only "$PLAN_HEAD_BEFORE"..HEAD -- src/content src/assets/brand src/assets/photos PENDING-COPY.md)" ]`.
6. `git status --short` limpio y `git diff --name-only "$PLAN_HEAD_BEFORE"..HEAD` solo muestra archivos de `files_modified` (y el SUMMARY).
7. Revisión humana no bloqueante: Camila o Juan corren `docs/a11y/VOICEOVER-CHECKLIST.md` (macOS e iPhone) y Juan decide la aprobación y el seguimiento de `EXC-001`.
</verification>

<success_criteria>
- `npm run audit` existe, mide un build de producción servido en local con Lighthouse móvil (mediana de 3 corridas) y falla bajo 95, 100 y 100; con pendientes solo funciona con `--allow-pending`, nunca en despliegue, y deja el resultado marcado como no desplegable.
- Las puertas de producción siguen bloqueando `PUBLIC_ENV=production` mientras haya pendientes, probado por guarda.
- axe no reporta violaciones serias ni críticas a 320, 390 y 1280 px (ni con el FAQ abierto) y el único moderate tolerado tiene causa, condición y fecha.
- El recorrido de teclado de la página completa coincide con el DOM, no tiene trampa de foco alrededor del iframe, y existe la prueba de solo lectura con el formulario real.
- Lighthouse A (primer pantallazo) y B (costo real del formulario) están versionados uno junto al otro con su modo.
- `docs/a11y/REPORT.md`, `EXCEPTIONS.md` (con el iframe de ClickUp) y `A11Y-DECISIONS.md` están versionados, más la checklist de VoiceOver y el resumen de Lighthouse.
- Cubre A11Y-01, A11Y-02, PERF-01 y la mitad de auditoría de SEO-03; los umbrales de PERF-01 se declaran cumplidos solo si `lighthouse-summary.json` dice `pass: true`, y en modo `local-pending` se dice así.
</success_criteria>

<output>
Al terminar, crea `.planning/phases/03-seo-medicion-minima-y-qa/03-03-SUMMARY.md` siguiendo `@~/.claude/gsd-core/templates/summary.md`, con: `plan_head_before`, veredicto de legitimidad de `lighthouse`, evidencia RED de cada guarda, resultados de Lighthouse A y B con el modo, conteos de axe por ancho, resultado del recorrido con y sin iframe real, hallazgos corregidos con su archivo, decisiones abiertas para Juan y Ari (aprobador y seguimiento de `EXC-001`, cargar el iframe de otra forma si la cifra lo exige, arrastre WR-01 para la fase 4, corrida `strict` cuando Ari entregue el copy y la foto), y qué punto de `REPORT.md` sigue en `[ ]`.
</output>
